# PARENT_AND_FWL2CF00_CLAIM_LEDGER (append-only)

    WSFSCRP00 / FSCMA00 / GIMB00 / WL2SMF00 _DISPOSITION_REWRITTEN = false
    PARENT_THRESHOLDS_RECOMPUTED_OR_CHANGED = false
    PARENT_PANEL_MEMBERSHIP_CHANGED = false
    HISTORICAL_ACTIVE_ROWS_OPENED_OR_REFIT = false

WL2SMF00 prospectively qualified descendant-specific weighted-L2 materiality thresholds and sealed
a G1 x H3 target panel before any active outcome. It did not measure a carrier response. FWL2CF00
reconstructs the missing canonical sham reference series without recalibration, executes only the
two already locked carrier arms, and evaluates the predeclared quotient and factorial objects. No
historical active result or parent threshold is reclassified.

| # | claim | status | evidence |
|---|---|---|---|
| F1 | The seven-commit chain is direct-parent at every arrow and every bound artifact matches its committed blob id. | ESTABLISHED | device rev-parse; 27/27 blob ids; 65/65 WL2SMF00 manifest |
| F2 | The carrier arm identities were fixed strictly before any threshold was computed. | ESTABLISHED | one-way hash chaining arm lock -> panel lock -> threshold lock |
| F3 | The parent's frozen founder-stratum axis was never serialised, so transfer is not evaluable. | ESTABLISHED, declared BEFORE the first replay | no `psi_plus`/`Psi_minus` in any committed parent tree |
| F4 | The pre-execution oracle is non-vacuous. | ESTABLISHED | 15 groups pass, 16 negative controls fire, AST audit rejects an injected self-comparing predicate |
| F5 | `M2` alone is blind to an illegal per-time swap; only the whole-descendant two-arm block invariant detects it. | ESTABLISHED | Q0E, with both illegal actions firing |
| F6 | The canonical `SHAM_0` series was reconstructed and matches every locked scalar exactly. | ESTABLISHED | 16/16 on `B`, `RHO_MED`, `G2^2`, `TAU^2`; independent-process readback |
| F7 | The committed compact raw archive reproduces the full-field reader series exactly. | ESTABLISHED | string-for-string in exact rational form, 48/48 continuations |
| F8 | All 32 active rows completed once, with no retry, and passed the physical/runtime oracle. | ESTABLISHED | touch set `['Mf']`, rho never written, input hash stable, r(h0)=(0,0) exactly |
| F9 | All 32 cells exceed their own prospectively sealed threshold. | ESTABLISHED | exact squares; margins 3.14..10.20 |
| F10 | One affine quotient coordinate is NOT adequate on the fresh panel. | ESTABLISHED (relative) | `R1/R0 = 0.1112` vs 0.05, worst cell 0.4011 |
| F11 | The second increment is material in RELATIVE terms only. | ESTABLISHED | `I2/I1 = 0.0995`, `I2/R0 = 0.0885`, both passing |
| F12 | The second increment does NOT reach the frozen absolute floor. | ESTABLISHED, LIMITING | `sqrt(I2)/A_TAU = 0.570`; `R0` is about 3.1x short of `E_TAU/share` |
| F13 | The quotient optimiser is certified, with the same argmin for k = 0, 1, 2. | ESTABLISHED | exact for `R0`; float-exhaustive for `R1`,`R2` with gaps 6e8-1.8e10 times the error bound |
| F14 | Every fresh G1 x H3 factor object is below its conservative floor. | ESTABLISHED | geometry ratio 0.376 / 0.421; 0 of 8 allocation cells material, both arms |
| F15 | All 256 H3 allocation-label exchanges leave every unoriented object exactly invariant. | ESTABLISHED | exhaustive enumeration |
| F16 | Engine accounting is exact: 16 + 32 + 0 = 48 of 48, zero retries. | ESTABLISHED | fsynced START_ENTER log, driver results, per-archive meta |

## Explicitly NOT claimed

Not claimed: that any historical response now passes a new threshold; that 16 descendants or 32
arms are independent replications; any population claim about NEAR or FAR founders; any signed
history statement under H3; any physical A/B identity; any intrinsic or universal rank; any
environmental extension from a carrier-only programme; life, agency, identity, individuality,
reproduction, memory inheritance, material provenance, component-bath, curvature or multiscale
structure; independent confirmation.

A passed geometry effect would have been a manipulated-geometry effect **within this exact designed
panel** only. None passed.
