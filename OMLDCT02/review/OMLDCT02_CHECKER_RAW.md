# OMLDCT02 — INDEPENDENT ADVERSARIAL CHECKER, VERBATIM RETURN

One checker. No second checker, no review cascade. This is the return exactly as delivered,
written and externalised BEFORE any finding was acted on.

Checker agent id: a33cde3f7a856db05
Target: /home/claude/edl, branch codex/one-matched-locked-daughter-control-test-02, tip a3292f5

---

I have completed the attack. Working tree left clean at a3292f5.

---

# FINDINGS

**1. OMLDCT01 invalid closure — SEVERITY: NONE**
Grepped the whole repository (excluding `.git`) for every `OMLDCT01_*VALID*` and `OMLDCT01_TECHNICALLY_*` token. The disposition string appears in exactly 8 places (`/home/claude/edl/OMLDCT01/out/OMLDCT01_FINAL_DISPOSITION.json`, `OMLDCT01_FINAL_REPORT.md`, `OMLDCT01_MASTER_FREEZE.json`, `OMLDCT01_DEVIATION_RECORD.json`, `OMLDCT01_FIVE_LINE_SELF_TEST.json`, `omldct01_close.py`, `omldct01_selftest.py`, plus the OMLDCT02 freeze and parent binding) and is **always exactly `OMLDCT01_TECHNICALLY_INVALID`**. No softer variant exists anywhere. `NO_SOFTER_STRING_WAS_INVENTED: true`. `/home/claude/edl/OMLDCT01/out/OMLDCT01_PRE_C2_INCIDENT.json` classifies all four pilots as `DEVELOPMENTAL_PILOT_DIAGNOSTIC`, names the barred uses explicitly, and records `PRIMARY_CONFIRMATORY_PAIRS: 0`, `endpoint_values_computed: 0`. I confirmed all four pilot seeds are in OMLDCT02's forbidden set and absent from its manifest. No defect.

**2. New seed disjointness — SEVERITY: NONE**
I reimplemented the derivation from the prose in `OMLDCT02_SEED_MANIFEST.json` without importing any mission code, and regenerated all 1030 seeds: **exact match, field for field, including every recorded nonce** (max nonce 0, 0 collision rejections). `SEED_SET_HASH` = `998f6fab…` and `FORBIDDEN_SET_HASH` = `c8829b25…` both reproduce. Disjointness recomputed by me: 1030/1030 distinct; base∩reserve = 0; ∩ 480 OMLDCT01 seeds = 0; ∩ the 4 pilots = 0; every OMLDCT01 seed is inside the 2554-element forbidden set. I then ran my **own** full-repository harvest (1246 JSON files, *including* the OMLDCT02 subtree that their harvest excludes by rule): 3592 distinct seed integers found, and **zero** OMLDCT02 seed appears anywhere outside the OMLDCT02 subtree.

**3. Hash reproducibility — SEVERITY: NONE (with one prose error, see #4)**
In a fresh `git clone` at a3292f5 I recomputed all five digests with my own canonical encoder, importing none of their code: `FREEZE_CONTENT_HASH`, `FREEZE_FILE_SHA256`, `METHODS_HASH`, `SEED_SET_HASH`, `FORBIDDEN_SET_HASH` all reproduce. All 39 bound method files are byte-identical. I re-derived the methods closure from scratch — it returns the same 39-file union and the same `METHODS_HASH`. Label audit: I collected every hash-named key across `OMLDCT02/out` — 14 distinct labels, **each mapping to exactly one value; zero collisions**. `FREEZE_FILE_SHA256` is correctly absent from inside the freeze. The OMLDCT01 defect (one label over two quantities) does not recur.

**4. `METHODS_HASH` file count misstated in the C2 commit message — SEVERITY: MINOR**
The C2 commit body says "`METHODS_HASH … over 36 files`" and "every one of the **36** bound method files". The manifest and the freeze both say **39** (`N_METHODS_FILES: 39`, `MODULES` length 39), and 39 is what the digest is actually over. Prose error in an immutable commit message; no artefact or digest is affected.

**5. `FREEZE_CONTENT_HASH` does not cover two fields inside the freeze — SEVERITY: MATERIAL**
`H.content_digest` strips `METHODS_HASH` and `SEED_SET_HASH` (they are in `SELF_REFERENTIAL`) before hashing. I demonstrated: setting `METHODS_HASH` or `SEED_SET_HASH` to zeros inside the freeze leaves `FREEZE_CONTENT_HASH` **unchanged**, while changing `ALPHA`, `MINIMUM_VALID_PAIR_COUNT` or `FORBIDDEN_SET_HASH` does change it. Consequence: the guard's drift check (which uses `content_digest`) is blind to a post-C2 edit of those two fields — see #6b. Not load-bearing: the working-tree freeze is byte-identical to the C2 blob (`b709e2ad…` both), the sidecar file digest matches, and the verifier's cross-checks would catch a mismatch. But it is a real hole in a hash that is presented as covering the frozen content.

**6. The scientific-scale guard — SEVERITY: MATERIAL (three demonstrated weaknesses; the core claim holds)**
I attacked `/home/claude/edl/OMLDCT02/code/omldct02_guard.py` in throwaway clones.
- **(a) It cannot be opened by an uncommitted freeze — verified.** At C1 with a valid freeze dropped into the working tree: REFUSED. After committing that freeze *without* the token: REFUSED. Only a commit carrying **both** the token and the freeze file opens it. The core promise holds.
- **(b) DEFEAT: editing `METHODS_HASH` inside the working-tree freeze after C2 does NOT close the gate** (consequence of #5). The C1 commit message's claim "a freeze edited after C2 closes it again" is **false for those two fields**. Editing `ALPHA` does correctly close it.
- **(c) DEFEAT: deleting the working-tree freeze entirely opens the gate.** `live_ok` stays `None` and `assert_allowed` tests `is not False`. Worse, the returned record simultaneously says `reason: "the working-tree freeze differs from the committed one"` while **allowing** — the reason string contradicts the decision.
- **(d) DEFEAT: deleting `OMLDCT02_SEED_MANIFEST.json` silently zeroes the seed leg of the scale rule.** `_manifest_seeds()` returns an empty set on a missing file with no error, so a frozen seed can then be spent at fixture scale undetected.
- **(e)** It reads git for the token/freeze but the **working tree** for the seed manifest, so "it reads git, not the working tree" is only two-thirds true. It also takes the *newest* token-carrying commit and does not pin the C2 hash. Note C1's own message contains the token — harmless only because C1's tree lacks the freeze file, which I verified.
- **(f) Fixtures: adequate for the failure mode that killed OMLDCT01, inadequate for the drift claim.** The nine cases in `OMLDCT02_GUARD_SELF_TEST.json` were generated at 22:30:03, before C2 (22:31:11), with `C2_COMMITTED: False`, and they positively record all four scientific-scale cases **refused**. But every expectation is written as a function of `have`, so post-C2 the suite can never exercise the refusal path, and **no fixture at all covers the drifted/absent-freeze path** — exactly the path with defects (b), (c) and (d).
Not load-bearing: none was exploited. Freeze bytes unchanged since C2; the manifest is present and committed; all 805 constructions ran at L=36, T=11000 in strict frozen index order.

**7. C2 before world 1 — SEVERITY: NONE (with a stated evidentiary limit)**
Proved from git and forensics, not prose. Ancestry verified with `git merge-base --is-ancestor`: C1→C2→C3→C4. C2 (5dded59) contains exactly four files — the freeze, its sidecar, the verification, `SHA256SUMS_C2` — and nothing scientific. I checked out C1 in a fresh clone and ran the guard: **REFUSED**, "the token commit does not contain `OMLDCT02/out/OMLDCT02_MASTER_FREEZE.json`". The pre-C2 guard artefact positively records four refusals. `find` across the whole filesystem returns **no `.npz` and no OMLDCT02 artefact with an mtime before C2**; the earliest raw archive is 22:45:40, fourteen minutes after C2 at 22:31:11. No ref in the repository other than the OMLDCT02 branch carries a raw artefact. **Limit worth stating:** the container reflog was destroyed by the rollback and commit timestamps are author-settable, so the ordering rests on topology + the pre-C2 guard record + mtime forensics, not on an unforgeable clock. Those three agree.

**8. Locked-daughter reconstruction — SEVERITY: NONE**
The daughter is identified **once**, in the shared prefix, before the fork: `dcells = [trig.cells_tm[i] for i in trig.daughter_comp]`. The identical Python list is written to the ledger and `omldct02_c3_raw.measure()` passes **that same list** to both classifiers on **both** arms — I verified the measurement `daughter_cells` equals the ledger `locked_daughter_cells` for all 33. Classifier A refuses unless the locked set matches **exactly one** component at t_m, then follows that single running identity; B independently chases one identity forward. Across all 66 arm measurements I confirmed: `n_rows_in_interval == E3_DURATION + 1`, `contiguous == true`, `interval_end − t_m == E3_DURATION`, and A and B agree on `interval_end`, `n_rows_in_interval`, `min_nY`, `max_nY` and the full `nY_histogram`. No later substitution is representable.

**9. Fork identity — SEVERITY: NONE**
Beyond the ledger flags (all 33 `PHYSICAL_STATE_IDENTICAL` and `RNG_STATE_IDENTICAL` true; 33 distinct phys hashes and 33 distinct rng hashes, so they are not a constant), I **re-executed the prefix, fork and intervention independently for all 33 admissible seeds** from the frozen seed values. Every one reproduced `t_m`, `prefix_steps`, `phys_hash_at_fork`, `rng_hash_at_fork`, `locked_daughter_cells` and `parent_cells` exactly. Daughter and parent cell sets are disjoint in every pair.

**10. SELECTIVE fidelity — SEVERITY: NONE**
Independently replicated for all 33 and byte-compared to the sealed ledger: `parent_emptied` true, `daughter_untouched` true (`daughter_Y_before == daughter_Y_after` everywhere), `occupancy_conserved` true (Y removed = WY gained), `rng_unchanged` **true** — no random number consumed. `phys_unchanged` is false in all 33, as it must be. `removed ≥ 1` in every pair and equals the full parent occupancy.

**11. SHAM inertness — SEVERITY: NONE**
Independently replicated for all 33: `removed == 0`, `phys_unchanged` true, `rng_unchanged` true, `daughter_untouched` true, `Y_before == Y_after` and `WY_before == WY_after`. `FMW.intervene(arm, ())` is genuinely inert. All 33 pairs nonetheless show `ARMS_DIVERGED` true, so the arms are not accidentally identical.

**12. Resource accounting — SEVERITY: NONE for the number; MINOR for the ceiling semantics**
I recomputed `HARD_ARM_INSTANCE_COUNT` from raw fields (`t_m`, `prefix_steps`), not from the recorded `instance_cost`: **510.569 across 805 seeds, 0 mismatches**, matching the manifest's 510.56902; 5,616,259 engine steps ÷ 11000. Ceiling respected with 1.431 headroom, and the runner's `+2.0` lookahead makes a breach structurally impossible (max single-seed cost is 1.9999). Indices are strictly 0..804, **0 duplicates, 0 seeds not matching the frozen manifest, 0 technical failures**. The formula is honest as engine-step accounting. *MINOR:* the destroyed batch-03 work (~78 world-equivalents) is charged zero, so actual machine work was ≈588 world-equivalents. That is consistent with the frozen `ACCRUAL_RULE` ("a technical retry uses the identical seed and arm") and is explicitly disclosed in `OMLDCT02_ROLLBACK_RECOVERY_08.json`; but a reader taking "one full-horizon world-equivalent of engine work" literally would find 512 exceeded.

**13. Raw before analysis — SEVERITY: NONE**
C3 (f36bc09, 18:50:51) is an ancestor of C4 (a3292f5, 19:00:56). `omldct02_c3_raw.py` is **byte-identical at C3, at C4 and in the working tree** (`1e1ebce6…`), and C4 touched no code file at all. I enumerated **every distinct key** in the C3-committed sealed ledger — 53 keys, and **not one** matches duration/exposure/endpoint/termination/log. The only C3-tree files carrying endpoint *keys* are source code plus `E3_QUALIFICATION.json`, `FIVE_LINE_SELF_TEST.json` and `e3_dual26.json`; I checked those last three and they reference **zero** OMLDCT02 archive tags and **zero** OMLDCT02 seed literals — they are the pre-existing TLMR01 worlds and synthetic fixtures. The C3 capsule contains neither `FROZEN_ANALYSIS` nor `PAIR_MEASUREMENTS`.

**14. Pratt implementation — SEVERITY: NONE (one MINOR sub-note)**
I wrote an independent brute-force enumerator (exact `Fraction` arithmetic, own midrank routine, explicit 2^m sign-flip enumeration) and ran **194 cases** — hand-built plus randomized, deliberately loaded with zeros and heavy ties. **0 mismatches** on p, W+, `n_nonzero` and `n_zero`. Cross-checked **120 untied zero-free cases against `scipy.stats.wilcoxon` exact: 0 mismatches**. The implementation is correct Pratt: midranks over **all** pairs including zeros; DP over the doubled non-zero ranks; two-sided by |W+ − tot/2| with a tolerance far smaller than the 0.5 minimum gap. Null calibration at n=20 with zeros: 0.0437 rejection at nominal 0.05 (conservative, as an exact discrete test must be). *MINOR sub-note:* `hl_interval` uses the **untied** signed-rank null over all n pairs, inconsistent with the Pratt ranking used for the p-value. It is reported-only and enters no frozen decision.

**15. The AND rule — SEVERITY: NONE**
`decide()` is exactly the six-condition intersection-union rule the owner froze. I ran **4000 randomized trials** across n ∈ {33,40,41,45}, injected zeros, and independently evaluated all six conditions: **0 rule violations, and the terminal mapping was exact in every one**. 939 trials returned SUPPORTED and every one satisfied all six. Adversarial hand cases behave correctly: n=40 with p≈1.8e-12 both endpoints → **not** SUPPORTED; discordant signs at p≈9e-13 → **not** SUPPORTED; exposure null → not SUPPORTED. It cannot return SUPPORTED unless all six frozen conditions hold.

**16. Terminal mapping — SEVERITY: NONE**
33 < 41 → `INSUFFICIENT_ADMISSIBLE_PAIRED_BLOCKS`, and that is precisely the owner's frozen fifth decision in `OMLDCT02_HUMAN_DECISION_BINDING.json`: "pure scientific under-accrual without technical failure maps to INSUFFICIENT_ADMISSIBLE_PAIRED_BLOCKS". The code also puts the count test **first**, so an under-powered campaign cannot report support even if the p-values had rejected — conservative and correct. I recomputed the entire `DECISION` block from `PAIR_MEASUREMENTS` and it reproduces **byte-for-byte**; `ANALYSIS_CONTENT_HASH` and `RAW_MANIFEST_CONTENT_HASH` both recompute. Exactly four disposition strings exist anywhere in the repository or in any OMLDCT02 commit message; **no fifth**.

**17. Claim ceiling — SEVERITY: NONE (one MINOR wording note)**
Zero occurrences of *organism*, *daughter organism*, *life created*, *self-replication*, *autonomous cohesion*, *alive*, *demonstrates*, *we show that* — in artefacts or commit messages. Every occurrence of *reproduction*, *heredity*, *living*, *turnover* is a `NOT_TESTED`/`NOT_ESTABLISHED` status field or an entry in `IT_DOES_NOT_ESTABLISH`. The single "proves" is `omldct02_seed_manifest.py`'s "proves it disjoint" — a set-theoretic claim I independently verified. The C4 message does report that both medians are positive but immediately states "not distinguishable from noise" and "supports NO claim in either direction". *MINOR wording note:* "Reaching 41 pairs would not have changed the disposition on this data" reads at first glance as a power claim; the qualifier "on this data" carries it (had the threshold been met by these 33, the terminal would be NOT_DETECTED, still not SUPPORTED), and its effect is anti-inflationary.

**18. Windows durability — SEVERITY: NONE for what is in the repository; one MINOR defect**
Supported by what is actually there: all five digests reproduce in a fresh clone by an independent implementation; **`SHA256SUMS_C1` 11/11, `SHA256SUMS_C2` 3/3, `SHA256SUMS_C3` 17/17, `SHA256SUMS_C4` 18/18 all verify**; the C3 increment bundle passes `git bundle verify` and I successfully restored f36bc09 from it onto a C2 clone; the capsule's 46 tracked files are byte-identical to the C3 commit (only `.pyc` differ). All 66 raw archives verify by sha256 **and** size against the sealed ledger, with no extras on disk and none missing. The device-side claims (269 commits, fsck 0, device Python 3.10) are self-reported and unverifiable from the container. *MINOR defect:* `/home/claude/edl/OMLDCT02/code/omldct02_selftest.py` hardcodes `OUT="/home/claude/edl/OMLDCT02/out"` and ignores `OMLDCT02_REPO`. It is the **only** file inside `METHODS_HASH` that is not relocatable — run from a fresh clone it writes back into the original repository. I triggered this and restored it. It does not affect any digest.

**19. Verification scripts mutate the artefacts they verify — SEVERITY: MINOR**
`omldct02_verify_freeze.main()` defaults to `write=True`, so merely running the freeze verification rewrites `OMLDCT02_FREEZE_VERIFICATION.json` with a new `GENERATED_UTC`. I did this by accident on my first command and had to restore it. Same for `omldct02_selftest.py`. Consequence: any independent checker dirties the tree by checking, and the operator's self-declared "rewritten by a post-rollback run" event is one a checker reproduces by accident.

**20. `rng_hash_after` in the archive metadata is misleadingly named — SEVERITY: MINOR**
In `omldct02_fork._write`, the `intervention` block records `"rng_hash_before": rh` (at the fork) and `"rng_hash_after": FMW.rng_hash(arm)` — but that call happens **after the arm has been stepped to the horizon**, so "after" is the end-of-run RNG state, not the post-intervention state. The two differ in both arms, including SHAM. The substantive claim (RNG untouched by the intervention) is recorded correctly in the ledger's `INTERVENTION_AUDIT.rng_unchanged`, which I independently replicated as true for all 66 arm instances. A reader auditing only the archive metadata would draw the wrong conclusion.

---

## The four self-declared items

**A. `omldct02_c3_raw.py` is a post-C2 addition, outside `METHODS_HASH` — declaration ADEQUATE; I escalate to MATERIAL, not load-bearing.**
The operator's framing ("it chooses nothing") is not quite right. The file contains one rule that is genuinely decision-relevant and is *not* in the freeze: the undefined-log policy `dd = log(ds) − log(dh) if ds>0 and dh>0 else (0.0 if ds==dh else None)`, where the `None` branch drives `TERMINAL = OMLDCT02_TECHNICALLY_INVALID`. That rule **fired** — index 664 has duration 0 in both arms. It also holds the classifier-agreement retention rule (which never fired: 0/33 disagreements). Mitigations I verified: the file was committed at C3, before any endpoint value existed anywhere (finding #13); the branch it took is the one the frozen `ZERO_DIFFERENCE_TREATMENT = PRATT_EXACT_SIGN_FLIP` requires, since Pratt keeps ties as valid zero-difference pairs; and I tested the sensitivity — dropping index 664 entirely (n=32) gives p=0.3999/0.2240 and **the same terminal**. The rule was not decision-bearing on this data. Declaring it and inviting attack was the right call; the record should say it contains a decision rule, not that "it chooses nothing".

**B. Eight rollbacks; batch 03 destroyed and re-run — declaration ADEQUATE.**
Independently corroborated. mtime forensics on the 66 archives: 16 carry preserved whole-second mtimes from 22:45 Aug 25 to 02:19 Aug 26 (restored batch 01), and the eight batch-03 pairs (indices 450, 468, 471, 482, 484, 489, 518, 520) all carry mtimes from **13:49 Aug 26 onward** — after the 13:13 restore. That is exactly the declared narrative. The determinism the "re-running reproduces exactly" argument rests on is not asserted here, it is **demonstrated**: I re-derived the fork from the frozen seed for all 33 pairs and reproduced every hash. The ledger is index-contiguous 0..804 with 0 duplicates, 0 seed substitutions, 0 technical failures, so no outcome-informed selection is representable. *MINOR:* the batch-01 and batch-02 run logs are absent from the record (destroyed while untracked); only 03r, 04 and 05 survive, and the ledger plus the recovery note are the sole attestation for the first two batches.

**C. Observed yield 33/805 = 4.10% vs the parent's 22/256 = 8.59% — declaration INADEQUATE as framed; escalate to MATERIAL, not load-bearing. This is my strongest scientific criticism.**
First, what is *not* wrong: I verified the sizing basis is faithfully transcribed. TLMR01's 256 LAW_C worlds split exactly 230 / 22 / 4, and the design's hard-coded t_m lists reproduce TLMR01's 26 trigger times exactly. Both missions use the same `fmrct01_track.Trigger`, the same `LATEST_ALLOWED_TRIGGER = 6500` and the same `NEED = 250`; no parent trigger occurred after step 2379, so the prefix truncation is irrelevant; and OMLDCT02's prefix loop is line-for-line identical to `tlmr01_world.run_world`'s. **The gap is not an implementation divergence.**
Second, its true size. Tested properly as two samples (Fisher exact, not against a point estimate): overall admissibility p = **0.0087**; the trigger stage alone p = 0.054; carried-given-triggered alone p = 0.068. Real but modest; the exact 95% intervals [0.0546, 0.1272] and [0.0284, 0.0571] very nearly touch.
Third, the actual defect. `P_reaching_41_pairs = 0.9999` was computed by plugging the **point estimate** 22/256 into the simulator with **no propagation of its own sampling error** — from a sizing sample of 22 events. I re-ran the frozen simulator at the lower 95% bound of that very estimate: **P(41 pairs) = 0.64**. At the observed rate: **0.076**. So `P_under_accrual = 1e-4` was never robust, and the failure that materialised sat inside the sizing estimate's own confidence interval **before world 1**. The operator's declaration frames this as a yield surprise; it is more precisely a foreseeable failure to propagate uncertainty in the headline pre-run disclosure — the same disclosure the C1 message used to justify the 474→1024 change.
Not load-bearing: the freeze states `these_are_disclosures_not_gates: true`; no frozen design quantity moved (target 41 and ceiling 512 unchanged; only the candidate-list *length*, in the conservative direction); no endpoint, alpha, threshold or decision rule was informed by it; and the outcome maps to the terminal the owner's frozen decision names for exactly this case.

**D. `OMLDCT02_FREEZE_VERIFICATION.json` rewritten post-rollback then restored — declaration ADEQUATE and independently confirmed.**
The blob at HEAD is byte-identical to the blob at C2: `8a9035bebabe5eba89c7c4007a3951e7c3e6b478373d290409e7d14f6edd6f14` in both, and it matches `SHA256SUMS_C4`. C3 and C4 did not touch the file. The restoration claim holds. See finding #19 for the underlying cause, which I reproduced by accident.

---

**LOAD_BEARING_DEFECT_COUNT = 0**

None of the mission's own invalidity triggers fired: C2 provably precedes world 1; there were zero classifier disagreements on retained pairs; zero technical failures; every archive hash verifies; the 512 ceiling is respected; and no seed was duplicated, skipped, replaced or reordered.

**CHECKER_VERDICT** = I attacked every one of the sixteen points and the four self-declared items with independent reimplementations, brute-force enumeration, fresh-clone recomputation, live re-execution of all 33 forks, and deliberate attacks on the guard, and I found **no load-bearing defect** — but four MATERIAL ones the record must carry: `FREEZE_CONTENT_HASH` does not cover `METHODS_HASH` or `SEED_SET_HASH`, so the guard's "an edited freeze closes the gate" claim is false for those two fields and for an absent freeze or absent seed manifest, and no fixture covers that path; `omldct02_c3_raw.py` does contain a decision rule (the undefined-log policy) outside `METHODS_HASH`, though the branch it took is the frozen one and the terminal is unchanged under the alternative; and the pre-run power disclosure of 0.9999 was computed from a point estimate with no uncertainty propagation, a foreseeable weakness that then materialised as the under-accrual the mission terminated on.
