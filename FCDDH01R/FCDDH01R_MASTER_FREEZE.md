# FCDDH01R — MASTER FREEZE (pre-execution)

Written and committed before any new outcome and before any engine start of this child.

## 1. Identity and authority

| | |
|---|---|
| NEXT_PROGRAM | FRESH_CROSSED_DIFFERENTIAL_DISCOVERY_HOLDOUT_01R |
| SHORT_NAME | FCDDH01R |
| BRANCH | `dev/fresh-crossed-differential-discovery-holdout-01r`, append-only child of `93f13f45e6b6550a7ff709768b7b574161ed6a4f` |
| WORKDIR | `/home/claude/sweep/FCDDH01R` ; repository subtree `FCDDH01R/` (the repository's canonical artifact root is its top level: FCRA00/, FSQBT00/, FCDDH00/ all live there) |
| AUTHORIZATION_CLASS | ONE_FRESH_ENGINEERING_CORRECTED_REEXECUTION |
| DIRECT_GIT_PARENT | FCDDH00 tip `93f13f45…`, subtree `16ac1696…`, bundle `2bfda170…`, SHA256SUMS 340/340 |
| SCIENTIFIC_ESTIMAND_PARENT | FCRA00 `334b7c2ba6d97dadb403c7a1ea9700a1c61ad512` |
| TOMMY_MAIN | `f3921a4d2eb4f3c5d8c88855048d32bcd0c02a77`, unchanged, never checked out |
| PUSH / PR / WORKFLOW / TOMMY_ACTION | all false |

Authorization binding, including the unresolved reported-digest discrepancy and its content-based
identity resolution, is in `FCDDH01R_OWNER_REAUTHORIZATION_BINDING.json`.

## 2. Normative precedence (frozen)

1. This reauthorization controls **only**: the fresh authority, direct parent, programme/branch/
   workdir names, the new namespace, the new randomization bytes, the fresh start budget and the
   durable-executor requirements.
2. The **exact committed FCDDH00 master freeze and locked analysis objects** control every
   scientific definition, unit, formula, coefficient, gate, threshold rule, sample size, phase
   order, claim ceiling and exclusion. They are incorporated **by committed identity**, not by
   summary: see `FCDDH01R_SCIENTIFIC_OBJECT_IDENTITY_MANIFEST.json`.
3. The FCDDH00 closure controls the historical failure record, consumed namespace/starts and reuse
   limits.
4. Exact FCRA00 objects control the immutable parent mu/P1/P2/e1/e2, carriers, reader and
   materiality semantics.

No normative conflict was found between (1) and (2): this prompt's §1 and §8 restate the inherited
specification and agree with the committed freeze point for point.

## 3. Science: unchanged, by committed identity

12 discovery + 16 hold-out independent upstream ancestries; 4 descendants per ancestry
(NEAR/FAR × H3 member 0/1) from ONE byte-identical precursor; both historical 1× carriers per
descendant. Maxima 96/96/96 and 128/128/128, hard child total 672. All of
`z / TAU / r / d / x / A_X / A_X_BAR / s / p / A_PAIR / J / m`, the linked descendant gauge, the
twin-sham TAU semantics, the certified-interval comparisons, D0–D11, H0–H9, the exact 2^16
response-only T tail, `K_H ≥ 12/16`, the immutable-P2 summaries, the canonical 44-field schema, the
decision matrix and the claim ceiling are executed from the committed FCDDH00 objects, byte-identical
except one required firewall-root constant in the trainer (`FCDDH01R_ENGINEERING_DELTA.md`).

Sample size is never changed after an outcome. No second axis, trainer, reader, carrier, dose, mask,
window, PCA/P3, nonlinear classifier or selected subset. No threshold is lowered, renormalized or
learned from active data. Direction coherence, absolute materiality, allocation-orbit robustness and
the P2 descriptive summaries remain four separate fields; none rescues another.

## 4. The dead parent is forensic only

`FCDDH00_FINAL_DISPOSITION = DISCOVERY_SHAM_OR_ACTIVE_PANEL_INCOMPLETE__NO_AXIS__ZERO_HOLDOUT_STARTS`.
108 starts reconstructed from the committed WAL (48 construction + 58 paired sham rows + 1 sham
singleton + 1 interrupted billed row + 0 setup + 0 active). `FCDDH01R_NO_LOOK_RETRY_LICENSE = PASS`
with 0 target-response looks, 0 confirmatory tests and 0 alpha spent, so the preregistered 12+16 and
the single 2^16 family carry **no** multiplicity adjustment. No 71000-series byte may enter this
panel in any role. Details: `FCDDH00_FAILURE_AND_REUSE_BOUNDARY.md`.

## 5. Fresh namespace and randomization

`N = 73000`; discovery 73000–73023, hold-out 73024–73055; roles assigned before construction and
never promoted in either direction. 72000 was rejected because the FCDDH00 closure report itself
exposed it as the recommended next namespace, and N was increased by exactly 1000 as the rule
prescribes. One 256-bit OS seed drawn exactly once, fsynced before any derivation, committed before
construction, SHAKE256 domain separation changed only from FCDDH00 to FCDDH01R. No dead-panel
randomization bit or execution order is reused. No redraw after a crash.

The FCDDH00 randomization license is **not** inherited: `FCDDH01R_RANDOMIZATION_LICENSE` is rebuilt
from new-panel evidence at closure and set to `NOT_LICENSED` if any condition fails, in which case
the exact T p-value takes the status the inherited decision matrix requires.

## 6. Durable execution is a scientific gate

No engine may start until the chosen mechanism proves DEX0–DEX16 with dummy fixtures only. The
frozen launch template, the ten monotone states, the append-only WAL directory, the exclusive
`START_GATE` charge, non-overwriting publication, the conservative recovery matrix and the polling
contract are specified in `DURABLE_EXECUTOR_SPEC.md` and evidenced in
`DURABLE_EXECUTOR_PREFLIGHT_REPORT.json`. A safe between-row supervisor restart is **not** an
engine-row retry. Any billed incomplete or uncertain row is charged, never replayed.

## 7. Stops

All inherited fatal stops remain active, plus: unverifiable parent or scientific lock; no-look
license not PASS; any old 71000-series scientific byte entering the panel; namespace or roles chosen
after an outcome; DEX or Q0A–Q0W failed, skipped, vacuous or repaired after the first billed start;
real work launched from an unproven bounded-call background job; two supervisors or rows coexisting;
a blind relaunch after a timeout; replay or replacement of a billed row; automatic crossing of a
phase barrier; post-first-start changes to frozen source, config, queues, schedules or command
templates; decoding partial raw bytes; extending or restarting this panel after any new outcome.

**This authorization grants exactly one fresh panel.** Another billed mid-row failure closes the
programme honestly; no FCDDH02R, no new seeds, no automatic use of remaining starts.
