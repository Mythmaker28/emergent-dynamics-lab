# EXP-SC-SPATIAL-MEMORY-CAUSALITY-01 (SMC-01) — Final Scientific Report

## FINAL VERDICT
**EXP-SC-SPATIAL-MEMORY-CAUSALITY-01: FAIL — SPATIAL MEMORY DOES NOT SURVIVE TURNOVER.**
h2 replicates as a prospectively-decodable, frame-independent coordinate, but it lives in the memory
value-**dispersion** (not a geometric pattern) and **collapses under modest constituent turnover** while the
first coordinate h1 persists intact. h2 is therefore not a turnover-surviving organizational memory. Per the
preregistered stopping rule, the positive-claim structured-transplant/causal battery was not run once this
load-bearing prerequisite failed.

- STORAGE REPLICATION: **PASS** — sealed fresh family (executed once): h1=0.81, h2=0.90 (DIST), shuffle null −0.46.
- ORIGINAL RANK GATE: **STRICT FAIL, MARGINALLY** — σ₂/σ₁=0.213 here (Phase C 0.283), both <0.30, but h2 far above null.
- SPATIAL COORDINATE FRAME: h2 lives in **frame-free value-dispersion** (std/percentiles), not the entity mean
  (0.24) and not a resolvable geometric pattern (entity ≈36 cells → rasters too sparse). "Spatial pattern" framing corrected.
- LAB-FRAME AUDIT: **PASS (H4 rejected)** — frame-free decoders are translation/rotation-invariant by
  construction; the lab-frame decoder's apparent edge (0.84) **collapses to −0.77 under random translation**.
- ENVIRONMENTAL RESET: not run (moot after turnover failure).
- TURNOVER SURVIVAL: **FAIL** — h2: 0.73 (M=1.0) → 0.35 (M=0.77) → −0.01 (M=0.64) → −0.65 (M=0.39); h1: 0.96→0.99 throughout.
- STRUCTURED TRANSPLANT / MATCHED-MOMENT / SPATIAL CAUSAL h2 R² / NECESSITY / SUFFICIENCY: not run (stopping rule).
- CAUSAL RESPONSE DIMENSIONALITY: ≈1 (inherited, Phase C).
- VIABILITY: **PASS** (localized size 41–56 through turnover). LEAKAGE AUDIT: clean (passive tracer; no labels).
- REPRODUCTION READINESS: **NOT READY**. GENOME RELEVANCE: **NOT REQUIRED**. QUANTUM HARDWARE: **NOT USED**.

## OBSERVED
- Fresh sealed family: both coordinates decode from internal memory (h1 0.81, h2 0.90), h2 far above shuffled null.
- Locating h2: entity-mean pair 0.24; frame-free distributional (mean/std/percentiles) 0.73; rotation-invariant
  geometric 0.74 (no extra structure); absolute lab-grid 0.84 but −0.77 after translation.
- Turnover (PulseChase, no drive): as old-material fraction M falls 1.00→0.39, **h1 stays 0.95–0.99** while
  **h2 decays through zero by M≈0.64** and goes strongly negative. Droplet stays viable/localized throughout.

## INFERRED
- h1 (cumulative drive) is a genuine, robust, turnover-surviving 1-D organizational memory (the mean memory level).
- h2 is carried by the **dispersion** of memory values, which is **homogenized by the memory's own
  self-maintenance** (templating eta_t pulls values toward the local mean; diffusion D_m smooths) combined with
  dilution by fresh (m=0) material. Because h1 survives the identical dynamics, the h2 collapse is specific to
  the dispersion coordinate, not generic forgetting. h2 is thus material/maintenance-bound, not organizational.
- This also explains Phase C's mean-transplant failure mechanistically: a uniform-mean transplant has zero
  dispersion, so it necessarily discards h2.

## FALSIFIED
- H1 (h2 as a turnover-surviving organizational spatial memory) — falsified by the turnover collapse.
- H4 (lab-frame artefact) — rejected; the frame-free decode is robust and the lab-frame edge is translation-fragile.
- The "spatial geometric pattern" description of h2 — corrected to "frame-free value-dispersion."

## SPECULATIVE
- A writing/maintenance change that protects a second coordinate from templating/diffusion homogenization
  across turnover might yield a persistent second dimension — but that is a new writing experiment, explicitly
  out of scope here (C1c is frozen).

## Independent scientific judgment — is the central direction still justified?
Largely **no**, at least along the current line. Across WD-01 → Phase C → SMC-01, the robust, reproducible fact
is a **one-dimensional** turnover-surviving organizational memory (cumulative experience, causally expressed via
uptake, transplantable). Every attempt to establish a genuine **second** causal, persistent coordinate has
failed at a different stage: writing (Phase B), causal readout (Phase C), and now organizational persistence
(SMC-01). The second coordinate keeps existing as a *decodable statistic* but never as a *causal, transplantable,
turnover-surviving memory*. The honest reading is that local write + mean-field readout on this droplet
supports one organizational memory dimension. I recommend **stopping the escalation** toward reproduction and
genome, retaining the solid 1-D result, and — only if a second dimension is still wanted — running a focused
writing/maintenance study of dispersion persistence, not another transplant or reproduction experiment. The
project should keep the deflationary language: one-dimensional causal organizational memory; not individuation,
not lineage.

## Next experiment (precise, optional)
Writing/maintenance persistence study (C1c as baseline): does any minimal, viability-preserving change to the
templating/diffusion terms let a second coordinate (dispersion or otherwise) survive to M≤0.25 without
destroying h1 or viability? Decide that before any transplant, reproduction, or genome work.

## Sealed/blocked (unchanged)
HMC PROSPECTIVE SPLIT remains SEALED. SC-PILOT-CAUSAL-FINGERPRINT remains BLOCKED. EXP-SC-01 remains BLOCKED.
REPRODUCTION EXPERIMENT: NOT EXECUTED.
