# H1 Load-Bearing Audit (paper-grade, recomputed from committed raw data)

The positive paper rests entirely on h1 (cumulative-experience coordinate). Audited more strictly than any
single prior experiment; all numbers recomputed here from committed raw `.pkl`/`.json`.

## Decodability (prospective, grouped leave-history-out, 95% bootstrap CI)
- H2-CERT sealed family (4 seeds × 12 hist, held-out): h1 = 0.94, CI [0.89, 0.99] at rest.
- DMM pooled (4 seeds × 14 hist): h1 = 0.98, CI [0.94, 0.99] at rest.

## Turnover survival (deepest valid checkpoint)
- DMM pool: h1 0.98→0.99→0.99 across M = 1.00 → 0.58 → 0.36 (CIs all ≥0.94).
- H2-CERT sealed: h1 0.94→0.93→**0.93 at M=0.21, CI [0.83, 1.00]** (48/48 viable, localized).
→ h1 is prospectively decodable after ~80% constituent replacement, lower CI bound 0.83 ≫ 0.50. **ESTABLISHED.**

## Causal expression (Phase C, common-body & in-place)
- Mean-transplant into a standardized erased body: h1 response R² = 0.61.
- In-place memory-isolated (full − memory-inert on the same body): h1 = 0.50.
- Memory-inert control (both readouts ablated) collapses the h1 response (≈ −0.19) → necessity.
→ h1 causally drives the physical (uptake) response and transplants into fresh material. **ESTABLISHED (qualified: mean-field readout).**

## Trivial-baseline audit (H2-CERT; memory-DIST vs body globals)
- At rest (M=1.00): h1 from internal memory = 0.94 vs from size+mass = 0.86 (body size tracks dose at rest).
- After deep turnover (M=0.21): h1 from internal memory = **0.93** vs from size+mass = **0.64**.
→ The internal memory retains cumulative-experience information that BODY SIZE/MASS LOSE through turnover
(incremental ≈ +0.29 at M=0.21). h1 is a genuine internal memory, not merely current body size — but it IS a
low-dimensional, engineered cumulative integrator (m+ = m1+m2 integrates Ψ ≈ cumulative drive by construction).
State this plainly: the contribution is causal persistence through turnover, not informational richness.

## Cross-experiment replication
h1/dose-type readout is positive and consistent across IOM-00, MCM-00, WD-01 Phase C, DMM-01, H2-CERT-01
(five independent seed families / protocols). No single-family dependence (unlike h2).

## Erase / clone-ceiling / leakage
- MCM certificate: ablate-all → 0.0 (erase removes the effect); D_clone ceiling 0.055 dev / 0.097 prosp.
- No history/cohort/tracker/seed label enters dynamics (passive PulseChase; verified). No row-LOO used.

## Replication decision
All load-bearing h1 controls (prospective decode, deep-turnover survival with CI, causal transplant + inert
control, trivial-baseline, erase, clone ceiling, cross-family replication) EXIST in committed prospective/
sealed data. → **EXISTING H1 EVIDENCE SUFFICIENT. No minimal replication required.**
