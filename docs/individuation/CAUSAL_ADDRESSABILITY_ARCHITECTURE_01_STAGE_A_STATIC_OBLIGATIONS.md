# CAUSAL-ADDRESSABILITY-ARCHITECTURE-01 — Stage A static obligations

These obligations were frozen before any Stage-A fixture or arm output was executed. They implement the accepted
parent `9ec894fbeba7ec2974a917a182d152e9d8074431` without widening `E0` and without defining a replacement E port.
The exact machine-readable edge inventory is committed beside this document.

## Response-specific H* derivation

The frozen step order is transport, uptake/readout, passive inheritance and death, toggle diffusion, memory writer,
`c` update, `N` update, return.

At update 1, transport reads the exact `t0` arrays. Every E reference equals the corresponding `t0` parent, so the
transported target state and the immediately following target `uptake` are identical for `e=0` and `e=1`. Therefore
the first-update `Y_L,direct` is E-invariant and cannot estimate E.

After that uptake, the same frozen update evaluates outside post-growth `u/v`, outside current uptake and alive
membership in `up_ref`, outside post-growth `m_k`, and outside post-uptake `N`. Those values need not equal their
preprobe references. The exact committed E formula can therefore change `U`, `V`, `Mf`, `c`, or `N` in the state
returned by update 1. Those target changes, plus held versus dynamic boundary parents at update 2, reach update-2
transport and uptake. The first E-accessible uptake horizon is consequently frozen as:

`H* = 2`.

This derivation is from source ordering, not arm output.

## Binding first-update distinction

- First-update target uptake invariance: structurally true.
- Complete returned-state invariance after update 1: structurally false for the exact preprobe-reference candidate.

The mission asks for unqualified “first-update E invariance.” Stage A will test both objects and fail closed. Making
the returned state invariant would require lazy first-evaluation references or delaying E activation, neither of
which is present in the committed spec. Such a repair would be a different E port and is forbidden in this mission.

## Exhaustive existing-edge inventory through H*

For every target-destination boundary face, E covers both orientations regardless of realized flux sign:

1. `_face_flux` parents `rho,c` and conditional donor `U/rho`, `V/rho`, `C/rho`, `Mf/rho` for transport;
2. post-growth outside `u,v` in the target toggle Laplacians;
3. post-growth outside `m_k` in target `_tmean` and `lap` writer terms;
4. outside current uptake and outside alive membership in the global `up_ref` numerator and denominator;
5. outside `c` in the target `c` Laplacian;
6. outside post-uptake `N` in the target `N` Laplacian, decomposed exactly as `r_N+P(t)+innovation`.

The target-local reservoir `F*(N0-N)` is an unchanged existing local/global-constant channel. It is logged but not
independently gated. Target-to-environment destination updates remain frozen. The environment port changes reads,
never persistent arrays.

## Arm composition and cloning

`L0E0`, `L1E0`, `L0E1`, and `L1E1` use `1=open/available`. An immutable plan is compiled from the common `t0`
state; target and reference hashes are frozen; then four independent exact clones are made. No arm is descended
from another. The local gate is active only on update 1. The E gate remains assigned through `H*=2`. Local and E
compilers must generate identical canonical plan bytes in either order. The `Kxx` namespace and member-directed
response are absent from the Stage-A code.

## Source, sink, work, and global identities

Every target-boundary contribution records the open and selected target-destination arithmetic. For each extensive
field `rho,U,V,C,Mf`, the sum of selected-minus-open boundary contributions must equal the active-minus-open global
sum after accounting for later local mediators. The remaining residual is bounded by an operation-count error
`gamma_n = n*eps/(1-n*eps)` times the sum of absolute terms; no tolerance is inferred from outputs.

`up_ref` logs target/external numerator and alive-count denominator separately, including `S_B0,n_B0` and the
frozen empty-set branch. `N` logs `r_N`, common exogenous `P(t)`, dynamic residual, recomposed parent, and the
unchanged local reservoir separately. Input-array hashes before and after every step must match. Any unaccounted
work, source/sink, state overwrite, uncovered E path, or boundary-only quasi-autonomous response is a binding
`STOP_ARCHITECTURE`.

## Pre-fixture static warning

The exact committed preprobe sample-and-hold is already predicted to fail complete returned-state invariance on
update 1. Fixtures may confirm arithmetic and coverage, but they may not redefine the invariant, reference, E
semantics, activation window, or disposition rule.
