# HISTORICAL CLAUDE 03A PROTOCOL ADDENDUM — NON-AUTHORITATIVE

Superseded by the actual canonical file `PRESEAL_CANDIDATE_PROTOCOL.md`, which has now been repaired directly.

# LCI-CAUSAL-TURNOVER-PRESEAL-REPAIR-03A — Repaired protocol + decision tree (TASK 5/6)

*Amends `PRESEAL_CANDIDATE_PROTOCOL.md` (parent 244bc32). Only the changes below differ from the parent; everything
else (engine, storage block, probe, tracer, tracker) is inherited unchanged.*

## Central question (exact, frozen)

**Does droplet-specific causal information survive deep material turnover under the passive-copy C1c architecture?**
It is NOT replaced by "does some nonzero memory-coupled feeding effect remain?" (that weaker question is answered
YES by direct coupling and is scientifically empty — see mechanistic null D).

## Five levels, kept explicit (no composite)

1. material replacement `M_i` (≤0.25 per droplet, measured by passive cohorts);
2. persistence of **any** local memory amplitude (m₊ > 0);
3. retention of the droplet's **OWN graded history** vs neighbour/global (the PRIMARY question);
4. **causal behavioural expression** of that retained own information;
5. active reconstruction beyond passive inheritance (**out of scope**; not implemented).

## Frozen geometry & tracking (TASK 5)

- Non-fusing probe **uniform 0.25×5**; probe amplitude is **not** interpreted as R*.
- Longitudinal **bijective** tracking; material tracer with **zero** dynamical feedback (verified max|Δ|=0.0).
- Per-droplet `M_i ≤ 0.25`; explicit geometric censoring (MERGE/SPLIT/LOST/AMBIGUOUS → world invalid).
- **Observation horizon / max waiting time: `TURN_CAP = 1500` steps.** If the all-three-valid deep condition is not
  met by 1500 steps → world = FEASIBILITY INVALID (recorded). Probe, horizon, tracer threshold and tracker are
  **frozen** and may not change after prospective data.

## Writing-during-turnover (frozen, unchanged from parent)

Left active, C1c unchanged, strictly neutral (no-drive) environment; `eta_w=0` and `up_ref:=0` and copy-disabled are
**diagnostic** variants (`TURNOVER_MECHANISTIC_NULLS_03A.md`), not the main line.

## Predeclared decision tree (TASK 6)

| outcome | condition | conclusion |
|---|---|---|
| **A** | own graded history survives deep turnover (G3-OWNERSHIP: own > neighbour AND global) AND causally expressed | turnover-surviving causal individuation **supported** |
| **B** | a local causal feeding effect survives, but own not distinguishable from neighbour/global | passive local causal remnant / homogenized memory — **NOT** individuation |
| **C** | own graded storage survives but causal expression fails | silent individual trace; causality not established |
| **D** | neither own information nor causal expression survives | passive C1c turnover persistence **fails** |
| **E** | too few geometrically valid worlds | feasibility failure; no scientific verdict |
| **F** | up_ref or another global channel explains the result | individuation claim **fails** |

**DEV-predicted most-likely outcome: B or D** (interventional effect = pure direct coupling; passive copy necessary;
ownership unresolved-leaning-absent). ACTIVE-RECONSTRUCTION becomes scientifically justified **only** if a valid
prospective result demonstrates passive copying is insufficient (Outcome B/C/D with adequate power) — and it is
**not** launched in this mission.

## Claim language (TASK 7)

Working title: *"Does droplet-specific causal information survive deep turnover under passive-copy C1c?"* Forbidden
before the prospective result: "individuation survives turnover", "identity survives", "self-reconstruction",
"heredity", "reproduction". Every report separates OBSERVED / ESTABLISHED / SUPPORTED-WITH-QUALIFICATION /
NOT-ESTABLISHED / FALSIFIED / SPECULATIVE.
