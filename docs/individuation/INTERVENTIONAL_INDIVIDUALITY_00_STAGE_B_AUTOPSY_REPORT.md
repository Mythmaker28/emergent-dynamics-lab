# INTERVENTIONAL-INDIVIDUALITY-00 — Stage-B raw-only developmental autopsy

## Disposition

**AUDIT_INVALID**

The frozen Stage-B result remains **DEV_FEASIBILITY_FAIL** exactly. This autopsy neither reclassifies that result nor
supports absence of individuality. It opened no Stage C, new family, seed, LawSpec, checkpoint, causal intervention,
memory analysis, selected subset or threshold revision.

## Scope and frozen evidence

The audit was restricted to the accepted parent `c31bf27ea80a6a3dcc60d0ec5380f668358671ff`, its frozen Stage-B manifest,
root manifest, committed classification and 64 exact Git-bound `physics.npz` shards. It used no engine, runner,
scientific project module, online row, shard manifest or reconstructed checkpoint.

The analysis plan was frozen before any physics archive was opened and preserved in checkpoint commit
`085bf5e9aad9d457512ed6a060dbb828393c3ffa`, pushed to
`origin/codex/interventional-individuality-00-stage-b-autopsy`.

| Pre-raw item | SHA-256 |
|---|---|
| Analysis plan | `3dda24c82936507566e2abe45186876c22c7954d80b8a4af7b75c9e742e91bb9` |
| Reconstruction protocol | `ee7c162755eebbcf57f2ed343c6f5d6bc916b1a971570f4687442cf7f6ff692f` |
| Source allowlist | `75b23538e5c7820d96414696727d65baf4bdccde05b3a41c4e49ae686763061b` |
| Primary analyzer | `d8961cf7e2e9fd2f93b939584d331db97631cb222b5e5064fdb762d736ab23a5` |
| Source-separated reproducer | `fe6e72aa18d9b540220f9f3aae185a6c0baae2da88d5b0b1835dba46c3879667` |
| Focused tests | `9aa429f3c1955b94736a42eec8e41fa64550e3c6eeae3d750123cfa384d96dcd` |

The code-only gate passed 87 focused tests with zero skips. The separate reproducer passed 26 self-test groups. Tests
included raw numerical identities, malicious archive and path rejection, authenticated-byte substitution, frozen
classification aggregation, no-overwrite publication, same-track anti-stitching, precursor-window availability,
outcome precedence, analysis/atlas parity and end-to-end parity on empty and persistent synthetic worlds. The final
adversarial disposition was `PASS_CODE_FOR_RAW`.

## Raw execution and reproduced failure

The primary analyzer was invoked once in raw mode. It authenticated the exact accepted-parent inputs and stopped
before publication with exit status 2 and the exact diagnostic:

```text
AUDIT_ERROR: track 0 ends without dissolution
```

Neither `primary/` nor `primary.partial/` was created.

The independent command first stopped before any physics read because its exact publication parent did not yet exist:

```text
AUDIT_INVALID: planned output parent must already exist
```

Only the exact allowlisted empty parent directory was then created. The reproducer's sole raw invocation independently
stopped with exit status 2 and the exact diagnostic:

```text
AUDIT_INVALID: representative track ended before horizon without dissolution
```

Neither `independent/` nor `independent.partial/` was created. The comparison command was not run and
`QUALIFICATION.json` does not exist.

Both diagnostics instantiate the same frozen rule: a reporting representative that ends before detector frame 159
without an explicit `DISSOLUTION` event is an audit failure. The rule was fixed before raw access and cannot be
relaxed, repaired from outcomes or recoded and retried within this mission. The error does not identify a world in
the immutable output, so no world or law is named, selected or inferred after failure.

## Consequences for the requested analyses

The audit failed before a closed 64-world transition table could be produced. Therefore the following scientific
products are intentionally absent rather than partially reported:

| Requested product | Disposition |
|---|---|
| Independent reproduction of frozen classification | Not completed; lifecycle audit failed first |
| Complete world-level transition table | Not published |
| IC-class and LawSpec trajectory atlas | Not published |
| Candidate-duration and censoring analysis | Not published |
| Compact-static precursor analysis | Not published |
| Parameter-region descriptive structure | Not published |
| Formation-versus-maintenance numerical decomposition | Not estimated |
| Primary/independent byte qualification | Not attempted because neither package closed |

No conclusion is permitted about brief threshold crossings, IC dependence, freezing, destructive-transition
proximity, horizon censoring, candidate coherence or absence of a coherent mechanism. In particular, the audit may
not be relabelled `RAW_INSUFFICIENT` or `TRANSIENT_OR_HETEROGENEOUS_CANDIDATES`: the preregistered integrity and
reproduction branch has precedence and selects `AUDIT_INVALID`.

## Provenance limitations

- An early metadata-only directory listing exposed forbidden filenames but not their contents; those files were never
  opened or used.
- During a later code-only hardening pass, the reproducer agent's accidentally broad `rg` output exposed limited
  primary-code/test snippets containing already-frozen literals. Core mechanics predated the exposure, no raw or
  outcome was exposed, and the incident is journaled. The reproducer is source-separated and project-module-free but
  is not described as pristine clean-room independence.
- The independent publication-parent failure occurred before raw and created no evidence. Creating the exact empty
  parent changed no scientific input, code, plan or threshold.

## Bounded roadmap

The only authorized next action is **human review of this fail-closed autopsy**. Any future attempt to diagnose the
lifecycle mismatch would require a fresh, explicitly authorized, pre-data reconstruction plan and new output
namespace. It cannot be treated as a retry or repair of this autopsy, cannot alter `DEV_FEASIBILITY_FAIL`, and cannot
promote the 11 candidate worlds or open Stage C.
