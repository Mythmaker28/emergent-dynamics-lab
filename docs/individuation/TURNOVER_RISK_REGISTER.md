# Authoritative risk register — PRESEAL 03C

| risk | status | frozen control |
|---|---|---|
| direct-coupling feeding contrast mistaken for independent evidence | high, real | causal contrast secondary; algebraic prediction reported |
| original-world leakage through duplicated bootstrap worlds | repaired | one LOWO prediction per original world; uncertainty from fixed fold losses only |
| arbitrary neighbour label | repaired | periodic geometric-nearest comparator N |
| E scope retains target memory | repaired | exact zero of target `m1,m2` cells; target mask persisted |
| “global” scope is only a small summary | repaired | complete target-centred physical field stack persisted |
| different labels across L/P/E/G | repaired | own cumulative dose for every scope |
| post-outcome feature or decoder selection | controlled | exact feature names, full arrays, lambda=1, and comparison statistics frozen in canonical protocol |
| family plan internally exceeds hard cap | repaired | 50 primary + 46 reserve = 96 total |
| reserve activated by endpoint results | controlled | feasibility projection only; endpoint mutation unit test |
| static token mistaken for human approval | repaired | separate approval JSON bound to exact execution-manifest Git blob |
| lambda-plus ablation also changes lambda-minus | repaired | unit-test every `MCParams` field except `lam_plus` |
| fission conflated with transient fragmentation | repaired prospectively | five-frame descendant evidence |
| loss conflated with death | repaired prospectively | five-frame local-mass evidence |
| merge silently followed as one survivor | controlled | overlap claimants persisted; first event invalidates whole world |
| small DEV diagnostics promoted | prohibited | all `n=4` results remain exploratory |
| insufficient valid worlds | live | minimum 18; hard cap 96; feasibility failure if unmet |
| procedural approval can be forged by a malicious actor | residual | explicit human audit required; mechanism is not a cryptographic signature |
| distributed information mistaken for local storage | live | L must beat N, E, and B; P/G reported separately |

No risk control authorizes execution.
