# DOWNSTREAM-ORDER-READER-01 — prospective package qualification report

Status: **PASS — COMPLETE UNEXECUTED PACKAGE — HUMAN REVIEW REQUIRED**

## Independent scientific judgment

The package is scientifically ready for a prospective **bounded causal-detection** test of the selected first
downstream face-flux interaction. It is not ready for equivalence or practical-importance claims because no
independent scientific margin exists. This is a principled scope limit, not an engineering defect.

Recommendation: **GO FOR HUMAN SEAL REVIEW; STOP FOR EXECUTION.** Do not choose seeds, seal the manifest or run the
family without a new explicit approval.

## Implemented package

- `downstream_order_reader_contract.py`: pure original-world aggregation, float propagation, fixed feasibility
  sensitivity and six-outcome classifier.
- `downstream_order_reader_prospective.py`: import-safe prospective runner, deferred simulator imports, fixed
  4 x 2 x 3 construction, exact gates, world shards, manifest binding, duplicate prevention and atomic resume.
- `downstream_order_reader_reproduce.py`: independent raw-only recomputation with no runner, contract or engine
  import.
- Seedless 48-slot manifest template and JSON schema.
- Final unsealed preregistration, claim table, numerical derivation, family analysis and placeholder-only commands.
- Synthetic tests for all six scientific outcomes, `FEASIBILITY_FAIL`, `NUMERICAL_FAILURE`, template refusal,
  atomic interruption/resume, duplicate rejection and independent reproduction.

The production executor is code-complete but was not invoked. Simulator imports occur only inside
`execute_scientific_world`, after a future sealed manifest passes preflight. Template validation imports no engine
and initializes no world.

Qualification environment: Windows `11-10.0.26200`, Python `3.12.10`, NumPy `2.5.1`, SciPy `1.18.0`, pytest
`8.4.2`, using `C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe`. The branch began at exact accepted
parent `75e405627e0b48428a5c71f31b8a2025726b763c`; the parent runner and accepted instrumentation files were not
modified. Future execution must record the human-sealed manifest bytes/hash, current package commit, Python path
and these dependency versions before starting W001.

## Updated causal audit

The engine order remains:

```text
history order
 -> m_minus
 -> q_c and terminal c update during the source-expression step
 -> matched c ramp
 -> next-step c gradient and saturation-dependent chemotactic face flux
 -> conservative material transport
 -> later movement/morphology/feeding consequences (not selected).
```

The selected logger reads the exact +x face flux before the common response update can write another source. Thus
the endpoint is downstream of `m_minus -> q_c` and is not algebraically fixed by the parent
`chi_source=dt*s*lam_minus*sum(rho0*m_minus)` calibration. Existing gradients, saturation, density and upwind
selection can change or reverse the response. Positive attenuation is therefore a directional hypothesis. A
negative interaction establishes opposite-sign access rather than manipulation failure.

The `lam_minus=0` order response remains secondary: a residual order route narrows source specificity but does not
erase the paired primary source-condition interaction. Direct source susceptibility remains a positive calibration
only.

## Margin rationale

`delta_num` is fully derived from instrument error and raw face sums. It does not encode a desirable effect. No
engine equation or prior accepted result supplies a defensible minimum internal-flux change corresponding to
behavioural relevance, and feeding/morphology endpoints are prohibited. Therefore `m_A`, `m_0` and dimensionless
relevance scaling remain absent. The design can classify positive access, opposite-sign access, or failure to
establish access, but cannot classify scientific equivalence in its present seal candidate.

## Synthetic qualification

The focused package suite passes all of the following without a scientific world:

1. exact six-outcome vocabulary and two administrative dispositions;
2. positive interaction -> `PREDICTED_ATTENUATION`;
3. negative interaction -> `OPPOSITE_SIGN_FUNCTIONAL_ACCESS`;
4. numerical-null/no sign convergence -> `NO_ACCESS_ESTABLISHED`;
5. independently supplied mock margin -> `EQUIVALENT_AT_DECLARED_SCALE`;
6. mechanical invalid flag -> `MANIPULATION_INVALID`;
7. direction/interval conflict -> `UNRESOLVED`;
8. 17 complete of 48 -> `FEASIBILITY_FAIL` plus scientific `UNRESOLVED`;
9. numerical failure -> scientific `UNRESOLVED`;
10. conservative numerical floor increases with raw absolute flux;
11. exact historical-rate sensitivity values;
12. seedless manifest validates but refuses execution before output creation;
13. interrupted mock run resumes at the next ordered slot;
14. duplicate/mismatched world shard is rejected;
15. raw-only classification exactly matches the primary contract;
16. static import audit confirms deferred simulator imports and raw reproducer independence.

The already accepted seven instrumentation fixtures remain unchanged: base/clamp logger identity, revised
schedule, matched ramp, closed first moment, boundary separation and synthetic sign reversal.

## Scope confirmation

No 570xx data were reconstructed or analyzed. No scientific seed value or namespace was selected, reserved,
initialized or run. No scientific outcome was generated. No BODY-EQUALIZATION, feeding, morphology, centroid,
y-axis, extra amplitude, reader battery, decoder, 03G, V5 or prior raw result was changed.
