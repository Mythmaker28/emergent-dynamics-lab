# DOWNSTREAM-ORDER-READER-01 — final unsealed prospective candidate

Status: **EXECUTION-READY PACKAGE — UNSEALED — ZERO SCIENTIFIC SEEDS SELECTED OR INITIALIZED**

Accepted parent: `75e405627e0b48428a5c71f31b8a2025726b763c`. This candidate accepts
`M_MINUS-ORDER-READER-00` only as a constitutive source calibration. It does not alter the parent
`NO_MEMORY_FIRST_STAGE — STOP` verdict, 03G, V5, or any prior raw result.

## 1. Scientific question and bounded claim

Does temporal order prospectively modify the first non-constitutive material-flux response reached through the
local `m_minus -> q_c` attractant-production channel?

The strongest permitted positive claim is a **bounded causal-access claim**: under the frozen standardized
isolation probe, order modifies the first-step internal chemotactic face-flux interaction attributable to the one
source-expression update. No result establishes ownership, identity, individual memory, active reconstruction,
feeding, behaviour, morphology, displacement, or macroscopic importance.

No practical-effect or equivalence margin is frozen:

```text
m_A = NONE
m_0 = NONE
equivalence_margin = null
```

Consequently `EQUIVALENT_AT_DECLARED_SCALE` is logically defined but unreachable in this candidate. It may become
reachable only through a new pre-outcome human justification and reseal, never from observed scientific values.

## 2. Unit, fixed family and histories

The statistical unit is the original source world. Four histories, two source-expression conditions and three
ramp arms are paired potential outcomes and never increase `n`:

```text
H_L_EARLY, H_L_LATE, H_H_EARLY, H_H_LATE
source lam_minus = 0 or 0.15 for one update
ramp arm a = -1, 0, +1
```

If later authorized, run exactly **48 predeclared source worlds** in manifest order. There is no outcome-triggered
reserve, early scientific stop, replacement world, or family extension. At least **18 complete original worlds**
are required. Fewer than 18 gives run disposition `FEASIBILITY_FAIL` and scientific conclusion `UNRESOLVED`.

The seedless template contains only `<SEED_NNN>` placeholders. A later human-sealed manifest must provide 48
unique seeds and a separately documented namespace-audit `PASS`. This package performs neither operation.

## 3. Outcome-independent eligibility and construction

Use the unchanged parent pre-history focal eligibility and geometry/tracker rules. Eligibility is decided before
history assignment and cannot depend on future survival, source calibration, flux, completeness, or any outcome.
No post-history matching, trimming, regression, residualization, or covariance adjustment by mass, body, radius,
geometry, nutrient, energy, neighbour, or global variables is permitted.

For each eligible original world:

1. serialize one pre-history checkpoint and verify four exact state/tracker clones;
2. assign the four frozen histories in a SHA-256 seed-derived order;
3. execute the unchanged fixed deep-turnover interval;
4. construct one same-seed, no-history, no-drive boundary trajectory independent of every response arm;
5. for each history, set only `N := N0`, use `up_ref=0`, and run one common 40-step settle at
   `lam_minus=0.15` inside the qualified radius-10 core and two-cell clamp;
6. hash the settled state, then clone it exactly into the two source-expression conditions;
7. vary `lam_minus` only in the next single update: `0` versus `0.15`, with the identical source collar frame;
8. require every persistent non-`c` field to be exact across the two source outputs; an actual `c` difference is
   allowed but its sign is not a manipulation-validity theorem;
9. clone each source output into the matched `-1/0/+1` ramp arms;
10. apply the ramp, then run exactly one common response update at `lam_minus=0.15` with the identical response
    collar frame and passive flux logger.

The horizon after the common settle is exactly two updates: one source-expression update and one response update.

## 4. Frozen ramp and one primary endpoint

For the integer-centred radius-10 disk `K`, define `G=d_x/10` on `K` and zero outside. Apply

```text
c_a = c_source + 0.01 * 1_K * (1 + a G),  a in {-1,0,+1}.
```

Every arm is nonnegative and adds the same total attractant. The sham is `a=0`; the directional components in
`+1` and `-1` reverse exactly. There is one selected downstream observable:

```text
J_h^lambda(a) = dt/M * sum(F_x)
```

where `F_x` is the exact flux returned to transport on positive-x faces whose two endpoints are in `K`, and `M` is
the positive pre-ramp core `rho` mass. Its exact name is **mass-specific internal +x face-flux sum**. The core is
open, so `J` is not called displacement or net transport. Boundary sums are validity diagnostics only.

Define

```text
D_h^lambda = [J_h^lambda(+1) - J_h^lambda(-1)] / 2
A_h        = D_h^(source lam_minus=0) - D_h^(source lam_minus=0.15)

delta_A_O  = 0.5 * [
  A(H_L_EARLY) - A(H_L_LATE)
  + A(H_H_EARLY) - A(H_H_LATE)
].
```

`delta_A_O` is the sole primary original-world estimand. Report its original-world mean, median, standard
deviation, two-sided 95% Student-t interval, exact sign counts and all world values. There is no arm-, face-,
history-, branch-, or droplet-level inference.

The predeclared directional hypothesis is `delta_A_O > 0`: greater EARLY source-linked saturation attenuates the
matched directional response. A negative interaction is not absence or invalidity; it is downstream functional
access in the direction opposite to the prediction.

## 5. Secondary controls only

- Direct `chi_source = dt*s*0.15*sum_K(rho0*m_minus)` is a positive source-susceptibility calibration. It cannot
  rescue the primary interaction.
- The EARLY-minus-LATE contrast in `D^(source lam_minus=0)` is a secondary other-pathway diagnostic. Its
  non-equivalence cannot invalidate the primary interaction by precedence.
- Body, geometry, neighbour, boundary, nutrient/energy and global-channel values are diagnostics and gates only;
  they do not create endpoints or statistical corrections.
- The sham is a manipulation control, not a second scientific endpoint.

No feeding, morphology, centroid, y-axis repeat, longer horizon, amplitude search, decoder, feature search or
generic reader battery is permitted.

## 6. Frozen numerical and claim rules

Each world obtains a conservative numerical floor `delta_num_world` solely from per-face float64 input tolerance,
summation roundoff, `dt/M` scaling roundoff, fixed linear-contrast propagation, logger identity, deterministic
replay and the closed-domain identity fixture. The family `delta_num` is the maximum over complete worlds.
It is instrument resolution, not relevance.

Directional convergence requires at least `ceil(0.75*n_complete)` world signs in the declared direction. The
two-sided interval and sign count are both required for positive or negative access. Exact rules are in
`DOWNSTREAM_ORDER_READER_01_CLAIM_CLASSIFICATION.md` and the independent reproducer.

## 7. Complete-block and kill switches

An original world is complete only when all four histories, two source conditions and three ramp arms survive the
full schedule and all required fields are finite. Incomplete eligibility/survival worlds remain raw feasibility
records and are never treated as zero. A systemic mechanical invariant failure is `MANIPULATION_INVALID`, not a
mere incomplete world.

The following fail closed before classification:

- exact checkpoint and source-input clone hashes;
- common 40-step settle and source-only intervention ordering;
- source outputs exact in every non-`c` field;
- common response `lam_minus=0.15` and common collar frames;
- core/collar disjointness, 317-cell core and identical 296-face +x mask;
- ramp nonnegativity, equal added mass and exact directional reversal;
- positive finite core mass;
- passive logger state identity and exact axis/face capture;
- viability, no tracker event, coverage cap and body inside the fixed core;
- complete 4 x 2 x 3 factorial block;
- tracker-independent primary endpoint;
- outcome-independent eligibility and original-world aggregation;
- minimum 18 complete worlds;
- exact manifest binding, no duplicate world/seed, atomic ordered-prefix resume;
- deterministic raw-only recomputation of every stored complete-world contrast;
- any nonfinite or failed numerical gate.

An individual pre-history/survival failure makes that world incomplete. A clone, schedule, ramp, logger, face-mask,
manifest, duplication or numerical invariant failure stops the family as manipulation/numerical failure.

## 8. Authorization boundary

This candidate is ready for human scientific review, but is unsealed and cannot execute. Human approval must occur
before a namespace audit, seed entry, manifest sealing, or prospective command. Even after approval, the immutable
manifest must be reviewed before the runner is invoked.

Recommendation: **GO FOR HUMAN SEAL REVIEW; STOP FOR EXECUTION.**
