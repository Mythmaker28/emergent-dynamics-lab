# Agent Journal — RUN-20260710-2030-EXPFL00

## ROLE / MODEL
EXP_FL_00: qualify the Flow-Lenia substrate + field measurement stack (NO law search). Model claude-opus-4-8
(Fable 5 lock lifted by user). No subagents. Start HEAD 4158e24. Substrate confirmed by user (Flow-Lenia candidate).

## SCOPE
Minimal fixed-law single-channel mass-conservative Flow-Lenia core. No parameter localization / evolution /
resource collection / mutation / multi-species. Strictly passive cohort tracers transported by the SAME mass
redistribution operator, forbidden from influencing affinity/flow/dynamics. Preregister a field M(tau) before
results. Preserve the frozen phenotype/P philosophy; do NOT recalibrate metrics to make Flow-Lenia pass. Do not
begin law search until this gate passes.

## CORE (validated)
Potential U=K*A (periodic FFT conv); growth-shaped affinity; concentration-limited flow F=(1-alpha)*grad(U)-
alpha*grad(A); mass-conserving forward-bilinear reintegration transport of A and each cohort by the identical
operator. Mass drift 1.6e-16; cohort partition error 4.5e-15; A nonnegative; deterministic; FFT-vs-direct
potential agreement 1.55e-15; passive-tracer invariance exact (A identical with/without cohorts).

## FIELD M(tau) (preregistered)
Passive-cohort mass Jaccard on a tracked entity: with cohort masses m_c^t = sum over the entity cells of cohort c,
M(tau) = sum_c min(m_c^t, m_c^{t+tau}) / sum_c max(m_c^t, m_c^{t+tau}). 1 = same origin composition, ->0 = full
turnover. P unchanged (frozen phenotype_similarity); field phenotype uses declared scales (length 10 cells, speed 1).

## OBSERVED/DECISION: results section (qualification verdict + EXP-REF-01 rerun).

---
## RESULT + VERDICT
All 9 qualification checks PASS (mass 1.6e-16, cohort partition 4.5e-15, determinism/tracer-invariance exact,
ref-path 1.6e-15, detector/tracker on real dynamics, EXP-REF-01 recognized+separated on the field stack:
reference |circ|=0.99 vdisp=1.22 vs null 0/0, P=1.0, M_min=0.067). VERDICT: QUALIFIED (D-022). NEXT: preregister
and launch EXP_FL_01 blind low-discrepancy regime map. 99/99 tests. Model claude-opus-4-8 (lock lifted).
