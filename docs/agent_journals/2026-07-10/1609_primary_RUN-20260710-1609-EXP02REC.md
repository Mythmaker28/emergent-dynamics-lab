# Agent Journal — RUN-20260710-1609-EXP02REC

## AGENT / ROLE

Primary agent / EXP02 recovery-gate implementer and operational integrator.

## RUN ID

`RUN-20260710-1609-EXP02REC`

## START TIME

2026-07-10 16:09:28 +02:00

## END TIME

In progress.

## STARTING GIT STATE

- Repository: `C:\Users\tommy\Documents\ising v3`
- Branch: `main`
- HEAD: `622ba9c4b9f05360a6eb0f409fe099f5dcaf7866`
- Remote: `origin/main` synchronized; worktree clean before this journal.

## ASSIGNED SCOPE

Continue autonomously from the durable handoff: implement and fault-inject the remaining EXP02 crash/recovery and final `COMPLETE` publication gates, obtain an independent recovery audit, and launch the preregistered 900-run screen only if those gates pass on a clean pushed SHA.

## ACTIONS PERFORMED

- Read the required repository state in prescribed order and inspected clean Git state.
- Read the testing-strategy skill and translated the handoff into integrity, idempotency, crash-injection, and completion-publication acceptance tests.
- Acquired the conservative active-run lock for `EXP02-COREV0-20260710-001`.
- Implemented atomic writes for plans, laws, aggregates, summaries, figures, raw indexes, and the final manifest; the manifest is published last and declares `status=COMPLETE` only after every planned shard is verified.
- Added SHA-256, byte-size, and actual CSV row-count verification for every shard plus cross-checks among shard manifests, `raw_index.json`, summary totals, and final manifest totals.
- Added conservative disk-headroom preflight and quarantine/logging for unpublished temporary files or shard directories left by abrupt termination.
- Added scoped Git cleanliness enforcement to the `stream-screen` CLI so tracked/untracked changes outside the experiment output block execution while a legitimate output-only resume remains possible.
- Added controlled finalizer-crash, orphan quarantine, idempotent completion, derived-output corruption, multi-run index, and real subprocess `os._exit(73)` recovery tests.
- The first independent recovery-audit attempt disconnected before performing work. Retried the same bounded audit; the retry is in progress and may modify only its own journal.
- The retry completed independently with `GO_AFTER_CLEAN_COMMIT`, 34 then-current tests, a real `os._exit(73)` probe, and 165.86 GB free versus a 4.31 GB EXP02 preflight requirement.
- Fixed both non-blocking audit findings before commit: `reservoir_size` is now frozen in the plan/manifest/reproduction command, and raw-level atomic recovery-log temporaries plus unlogged quarantine inventory are reconciled.
- Froze the immutable-shard/manifest-last architecture as decision D-012 and added the exact artifact contract to the EXP02 preregistration before execution.
- Committed/pushed recovery code at `6c593780ab33326fd0957f73bb885bfe1c15ab84`; 35/35 exact-SHA tests and the official clean-scope CLI smoke passed.
- Executed preregistered EXP02 without parameter changes: 300 Halton laws × seeds `{2001,2002,2003}` × 600 steps × cadences `{10,30,60}`. All 900 shards and the final `COMPLETE` manifest verified; raw storage is 571.55 MiB.
- Delegated independent full statistical and candidate-rule audits. Both verified 900/900 shards and all 3,600 raw CSVs by SHA, size, and actual row count.
- Candidate audit observed 10,302 raw probe rows, 2,400 clean-long rows, 94 cross-cadence endpoints, and nine 2/3-seed eligible laws `{0,12,35,52,73,94,209,225,298}`. Every candidate retains unresolved alias risk; no promotion was made.
- Statistical audit observed 648,740 rows, descriptive `r(P,M)=0.731581`, 11.67% horizon-censored tracks, and a non-destructive float-key fragmentation in the parent measurement aggregate.
- Added D-013 and a chunk-aware analysis module that groups lag by integer step delta, produces corrected aggregates, exact joint density/Pareto tables, cadence/flag/law/lineage summaries, figures, and the frozen candidate disposition without a composite score.

## IMPORTANT FILES READ

- `AGENTS.md`
- `docs/RESEARCH_CHARTER.md`
- `docs/PROJECT_STATE.md`
- `docs/DECISION_LOG.md`
- `docs/EXPERIMENT_INDEX.md`
- `docs/RUN_INDEX.md`
- `docs/agent_journals/2026-07-10/0444_streaming-design_RUN-20260710-EXP02-STR.md`
- `results/HOLDOUT-COREV0-20260710-003/manifest.json`
- `results/HOLDOUT-COREV0-20260710-003/summary.json`

## FILES CREATED OR MODIFIED

- This journal.

## COMMANDS / EXPERIMENTS EXECUTED

- `.\.venv\Scripts\python.exe -m edlab.runtime_lock acquire --run-id RUN-20260710-1609-EXP02REC ...`
- `.\.venv\Scripts\python.exe -m pytest -q` — 31 passed before recovery changes; 33, then 34, then 35 passed as crash and multi-run gates were added.
- `.\.venv\Scripts\python.exe -m compileall -q edlab tests` — passed.
- Real child-process fault injection exited with code `73` after shard fsync and before atomic directory publication; the next invocation quarantined the unpublished directory and completed normally.

## OBSERVED

- Starting HEAD is clean and synchronized.
- EXP02 remains preregistered with zero scientific runs.
- The current shard writer has local byte-equivalence, resume, plan-drift, and checksum tests; explicit fault injection and final completion semantics remain open.
- After implementation, 35/35 tests pass. No scientific trajectory, detector, tracker, P, M, threshold, or substrate code changed.
- A final manifest is absent after an injected finalizer crash, present only after all runs and derived outputs complete, and unchanged on a verified idempotent re-entry.
- Corrupt raw bytes, derived outputs, changed plans, mismatched index metadata, or inconsistent row totals fail closed.
- The final post-audit local suite passes 35/35 tests, including reservoir plan drift and double-crash recovery-log detection/reconciliation.
- After the analysis and integer-lag regression tests, the local suite passes 37/37 tests.

## INFERRED

- The local recovery gate now passes; EXP02 execution still awaits a clean committed SHA and an independent verdict.

## HYPOTHESIS

- Immutable per-run directory publication can provide simpler crash recovery than flat append/truncate if orphan temporary directories are detectable, ignored by aggregation, and safely auditable.

## WHAT WOULD FALSIFY THIS?

- Any injected crash that publishes a partial final shard, duplicates a completed run, accepts corrupt bytes, or publishes `manifest.json` as `COMPLETE` before every indexed output verifies.

## FAILURES / DEAD ENDS

- None yet.

## DECISIONS MADE

- Preserve the existing immutable per-run shard architecture for the recovery test rather than changing scientific generation or observables.

## UNRESOLVED RISKS

- Clean-SHA CLI smoke and a short exact-SHA recheck remain open. Raw availability still depends on the local ignored directory; checksums are not a backup.

## HANDOFF

In progress.

## ENDING GIT STATE

In progress.
