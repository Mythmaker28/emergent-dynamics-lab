# Agent Journal — RUN-20260710-1850-EXP03A

## AGENT / ROLE

Primary operational agent for EXP03-A (CORE V0 + density preference ONLY). Resume from durable state at
d96b5ed (CORE V0 closed CASE A, D-017). Define + isolate + validate the density-preference mechanism,
preregister the protocol before screening, run the matched OFF/ON screen, analyze against CORE V0.

## MODEL

primary model: **claude-opus-4-8**. The user EXPLICITLY LIFTED the Fable 5 model lock for this and subsequent
work ("JE LÈVE EXPLICITEMENT LE MODEL LOCK FABLE 5 ... continue avec le modèle actuellement actif, y compris
Claude Opus 4.8"). Recorded transparently; no masking. No subagents.

## RUN ID / TIME

`RUN-20260710-1850-EXP03A`; start 2026-07-10 18:52 +02:00.

## STARTING GIT STATE

HEAD `d96b5ed` (then `907442c` model-check checkpoint). Branch main. Durable state verified: EXP02 COMPLETE
900/900 / 648,740 meas; nine screening laws; hold-out → {0,52}; ALIAS-INTERVENTION CASE A (D-017); laws {0,52}
CLOSED. Working tree clean apart from benign CRLF result churn. origin behind (no push auth here).

## SCOPE / GUARDRAILS

EXP03-A = CORE V0 + density preference ONLY. Mutation OFF, orbital/transverse OFF, neighbor type-transition OFF,
recycling OFF. P and M separate; no composite score; thresholds unchanged; frozen observers/normalization reused
(no recalibration). Do not reopen laws 0/52. Do not rerun EXP02. Preregister protocol before inspecting screen.

## REFERENCES FOUND

No prior density-preference mechanism exists. Only charter definitions (RESEARCH_CHARTER: "EXP03-A add density
preference only"; EXP02/HOLDOUT protocols keep it OFF; PROJECT_STATE hypothesis "density preference ... may shift
the joint P/M distribution"). Mechanism defined fresh here.

## OBSERVED / INFERRED / HYPOTHESIS / WHAT WOULD FALSIFY

(to be completed after the screen; five distinct levels kept separate: distributional shift / screening signal /
fresh-seed recurrence / alias rejection / causal re-establishment).

## ACTIONS (running log)

- Acquired lock; grepped references; confirmed no prior density mechanism.

---

## EXECUTION + DECISION (appended at completion)

### OBSERVED

- Mechanism validated (63/63 tests): neutral limit density_strength=0 == CORE V0 bit-for-bit on 5 controlled
  worlds; reference/vector density agreement at the frozen criterion; homeostatic sign-flip; periodic; guards;
  determinism; diagnostic-ID independence; detector/tracker/P/M run on density sims.
- Protocol preregistered and committed at **3406ef0 BEFORE** any screen result was inspected. PASS A (mechanism:
  many-body sign-flip distinguishes it from any interaction-matrix/range change) and PASS B (measurement confounds:
  densification/rigidity/bridge/mobility/slot-residence) journaled.
- Matched OFF/ON screen: 64 Halton laws x seeds {7001,7002,7003} x cadences {10,30,60} = 384 runs (chunked).
- Differential validity: OFF reproduces CORE V0 — r(P,M)=0.7328 (baseline 0.733162), permits {17,52} (52 = known
  survivor). ON lowers mean P (0.853->0.789), mean M (0.836->0.794), probe fraction (0.0163->0.0094), long tracks
  (2293->2042), raises censoring (15227->20511). Screening-permitted laws: OFF 2, **ON 0**.

### Five distinct levels

1 distributional shift: present but NEGATIVE (density depletes P/M/long tracks). 2 screening signal: none (ON 0).
3 fresh-seed recurrence / 4 alias rejection / 5 causal re-establishment: not applicable (no ON candidate).

### INFERRED / HYPOTHESIS / FALSIFY

Isolated density preference does not enrich turnover-individuality vs CORE V0; homeostatic settling reduces
coherent persistence (consistent with PASS B). Enrichment, if any, may need orbital (EXP03-B) or the combination
(EXP03-C). Falsifier F1 confirmed (ON did not exceed OFF eligible-law count).

### DECISION — EXP03-A SCREEN NEGATIVE (D-018)

Advance to EXP03-B (CORE V0 + orbital/transverse only). No hold-out triggered; thresholds unchanged; no composite;
laws 0/52 not reopened.

### MODEL

primary claude-opus-4-8; Fable 5 lock explicitly lifted by the user; no subagents. Recorded transparently.

### GIT / LOCK / HANDOFF

Results + docs + journals committed; lock released after commit; push pending remote auth (none in this env).
NEXT ACTION: preregister EXP03-B (orbital/transverse interaction only) with a neutral limit reducing to CORE V0,
validate, then screen under the same frozen discipline.
