# Agent Journal — RUN-20260710-2330-EXPRD00

## ROLE / MODEL
EXP-RD-00: qualify a genuinely OPEN reaction-diffusion substrate (Gray-Scott) + positive/negative measurement
controls. Model claude-opus-4-8 (Fable 5 lock lifted). Start HEAD 5d171f8. Authorized by D-027.

## RESULT — QUALIFICATION PASSED (12/12)
openness (total +99.4% from a non-equilibrium uniform state); EXACT closed limit F=k=0 conserves U+V (drift 0.0);
**HOMOGENEITY NULL ptp = 0.0** (uniform stays exactly uniform -> the homogeneous forcing CANNOT impose a spatial
pattern); tracer partition 0.0; FEED (external-origin) cohort grows; tracers passive (zeroing them leaves U,V
bit-identical); determinism 0.0; nonnegativity; reference-path (two Laplacians) 0.0; detector/tracker on real
dynamics max_track_len = 77 (spot regime F=0.025,k=0.055).
POSITIVE control (real dissipative organization): P=1.0, M_min=0.065, 83 probe rows, production 0.117, activity 0.085.
NEGATIVE control (frozen/imposed pattern): IDENTICAL P/M (P=1.0, M_min=0.065, 83 probes) -> **P/M alone cannot
separate an imposed pattern from a real organization** -- separated ONLY by the open-system rates (0.000/0.000).

## FIXES MADE DURING QUALIFICATION (before any claim)
(a) Gaussian seeding killed the pattern -> replaced with the standard square-patch seeding (U=0.5,V=0.25+noise).
(b) The openness check originally started at U+V==1 everywhere (the fixed point), hiding the source/sink -> replaced
with a non-equilibrium uniform probe. (c) Cohort Laplacian vectorized (identical operator, faster).

## NEXT: preregister and launch EXP-RD-01 blind low-discrepancy map (OPEN vs exact CLOSED limit).
