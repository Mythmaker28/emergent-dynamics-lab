# Agent Journal — RUN-20260710-2015-EXPREF01

## ROLE / MODEL
Measurement positive-control gate before any Particle Dynamics substrate decision. Model claude-opus-4-8 (Fable 5
lock lifted by user). No subagents. Start HEAD afaf293.

## PURPOSE
Before retiring Particle Dynamics, test whether the FROZEN detector/tracker/P/M stack can (a) recognize a KNOWN
persistent dissipative organization under constituent turnover and (b) separate it from the existing static-flux
null. If yes -> the EXP03 negatives are substrate results, retire PD + propose next substrate (Flow-Lenia
candidate). If no -> stop substrate switching, audit the measurement stack. Do NOT recalibrate P. Passive tracer
labels must not affect dynamics.

## DESIGN
Reference = stationary rotating packet (K=10 ring, real tangential velocities -> internal circulation + velocity
dispersion) with gradual membership turnover (window over a 20-ID pool shifts 1/snapshot). Static-flux null =
identical packet with rotation OFF and zero velocity (the frozen STATIC_MOTIF_WITH_MATERIAL_FLUX regime). Both are
scripted kinematically and fed to the frozen stack; diagnostic IDs are passive labels (positions scripted ->
labels cannot affect dynamics; frozen invariant -> labels do not affect detection/tracking/phenotype, only M).

## CONSTRUCTION VALIDATED (before verdict)
Reference: single entity/snapshot, 1 continuous track (61 obs), P=1.000, M_min=0.25 (turnover), 55 probe-positive
rows at lag 60 steps; mean |circulation|=0.147, velocity_dispersion=0.090. Static-flux null: identical
detection/tracking/P/M (P=1.0, M_min=0.25, 55 probe rows) but |circulation|=0.0, velocity_dispersion=0.0.

## OBSERVED/INFERRED/DECISION: in results section after the frozen batch + verdict.

---
## RESULT + VERDICT
- Frozen stack RECOGNIZED the reference (single continuous track 61 obs, P=1.000, M_min=0.25, 55 probe-positive)
  and SEPARATED it from the static-flux null on frozen dynamical observables (|circ| 0.147 vs 0.000; velocity
  dispersion 0.090 vs 0.000); raw P/M identical for both (so P/M alone cannot separate). P not recalibrated.
- VERDICT: RECOGNIZED=True, SEPARATED=True -> the measurement is adequate -> the EXP03 ladder negatives are
  SUBSTRATE results. **Retire Particle Dynamics (D-021).** Kill-switch review + next-substrate proposal (Flow-Lenia
  candidate, not assumed) written. Substrate NOT switched autonomously (user decision point).
- Scope caveat recorded: separation is vs the zero-velocity static-flux null; the flowing-occupancy alias is
  resolved only by the same-state causal intervention (carried into the next substrate).
- 95/95 tests. Model claude-opus-4-8 (lock lifted). NEXT: confirm substrate with user, then preregister EXP_FL_00.
