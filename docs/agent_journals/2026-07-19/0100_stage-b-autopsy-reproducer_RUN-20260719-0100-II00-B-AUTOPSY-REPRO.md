# Stage-B autopsy independent reproducer journal

- Role: independent engine-free autopsy reproducer
- Run ID: `RUN-20260719-0100-II00-B-AUTOPSY-REPRO`
- Start time: `2026-07-19 01:38:23 +02:00`
- Starting commit: `168b86c83db22999025b34a5e93aad299529037d`
- Branch: `codex/interventional-individuality-00-stage-b-autopsy`
- Starting Git state: clean
- Assigned scope: implement the independent analyzer only at `analysis/interventional_individuality_stage_b_autopsy_reproduce.py`, qualify it with deterministic code-only/synthetic fixtures, and stop before raw access.

## Pre-raw firewall

Until an explicit `RAW AUTHORIZED` message is received:

- do not open any allowlisted scientific `physics.npz`;
- do not open any `shard_manifest.json`, `online.json`, result-root classification, root manifest, engine, runner, primary analyzer, `edlab` source, production tests, or other scientific code;
- do not import `edlab` or any project module;
- edit only this journal and `analysis/interventional_individuality_stage_b_autopsy_reproduce.py`;
- use only deterministic hand-built arrays and temporary synthetic NPZ files for qualification;
- do not commit, modify frozen planning documents, or write results.

## Initial state

- OBSERVED: the sealed checkpoint is exactly `168b86c83db22999025b34a5e93aad299529037d` and the dedicated worktree is clean.
- INFERRED: implementation details must be derived exclusively from the three exact sealed plan/protocol/allowlist documents and retained independently qualified algorithm knowledge.
- WHAT WOULD FALSIFY READINESS?: any project import, forbidden path access, ambiguous protocol field, nondeterministic serialization, or synthetic parity/integrity failure.

## Static implementation

- Implemented only `analysis/interventional_individuality_stage_b_autopsy_reproduce.py`; no primary analyzer, production test, engine, runner, project package or scientific result was opened.
- The reproducer imports only Python standard-library modules and NumPy.
- Implemented the exact NPZ central-directory preinspection and logical array gate, native dtype/shape/finite/numerical qualification, accepted-parent Git blob authentication, and a casefolded allowlist guard rejecting traversal, absolute paths, symlinks and Windows reparse points.
- Implemented periodic component detection, frozen association metrics/selection, collapse-aware continuation/split/merge/appearance/dissolution/unresolved tracking, deterministic ID allocation, temporary-contact annotations and complete transition diagnostics.
- Implemented independent passive-cohort transport, per-observation face/boundary/bond/CV/seam metrics, prefix recomputation, frozen classifier precedence, ordered same-track developmental pathways, candidate episodes, terminal/censoring states, trajectory classes, atlas/estimands, signatures and the outcome truth table.
- Implemented accepted-parent preflight over every physics blob before the first NumPy load, a second-read immutability check, canonical finite JSON/JSONL, negative-zero normalization, closed seven-file inventory and exclusive partial-directory publication with `COMPLETE.json` last.
- OBSERVED: normative clarification fixed fingerprints as `onset_frame:ascending_row_major_cell_ids` and trajectory classes as `PATHWAY|TERMINAL_OR_NONE|lowercase_bin|SPLIT_MERGE=n|CENSORED=n`. These are centralized and covered by fixtures.
- INFERRED: the mechanics are raw-ready, but some nested output-presentation shapes are not fully specified by the frozen top-level schema. This is a byte-parity risk only; it must be resolved normatively before raw rather than by inspecting the primary output.

## Code-only qualification

Commands executed with the frozen interpreter:

```powershell
& 'C:/Users/tommy/Documents/ising v3/.venv/Scripts/python.exe' -m py_compile analysis/interventional_individuality_stage_b_autopsy_reproduce.py
& 'C:/Users/tommy/Documents/ising v3/.venv/Scripts/python.exe' analysis/interventional_individuality_stage_b_autopsy_reproduce.py --self-test
git diff --check -- analysis/interventional_individuality_stage_b_autopsy_reproduce.py docs/agent_journals/2026-07-19/0100_stage-b-autopsy-reproducer_RUN-20260719-0100-II00-B-AUTOPSY-REPRO.md
```

Final synthetic result at `2026-07-19 02:01:46 +02:00`: `SELF_TESTS_PASSED`, 17 umbrella fixtures:

1. periodic detector lift and canonical fingerprint;
2. continuation and 159 transition-diagnostic rows;
3. split ID allocation;
4. merge ID allocation;
5. co-primary onset-support tie break independent of track ID;
6. passive-cohort forced-neutral identity;
7. exact 32-strictly-post-turnover prefix duration;
8. multi-track anti-stitching;
9. no-episode null behavior;
10. freeze terminal-gap and censoring behavior;
11. zero-denominator and zero-baseline behavior;
12. every outcome truth-table branch, including both RAW_INSUFFICIENT routes;
13. classifier precedence;
14. 64/4 aggregation denominators;
15. casefold, dot-dot, absolute-path and symlink/reparse firewall behavior (Windows did not grant temporary symlink creation; production reparse detection remains explicit);
16. safe NPZ plus duplicate, nested, traversal, extra, object and structured member rejection;
17. canonical serialization and negative-zero normalization.

- OBSERVED: the first synthetic run exposed only a reversed argument order in the self-test/raw call sites for the independently defined detector; it was corrected before any raw access. A terminal-gap fixture initially expected rejection, while the frozen rule requires classification as non-frozen; the fixture was corrected to the normative behavior.
- WHAT WOULD FALSIFY THIS?: any later sealed hash mismatch, primary-independent byte mismatch, raw numerical/layout failure, committed classification mismatch, candidate-ID mismatch, or newly identified uncovered read path.
- Failures/dead ends: no scientific failure observed; no raw data were opened. Temporary symlink creation was unavailable under the current Windows privilege, so the fixture records that platform fact while the guard still rejects both `Path.is_symlink()` and `FILE_ATTRIBUTE_REPARSE_POINT`.

## Current handoff

- Current HEAD remains `168b86c83db22999025b34a5e93aad299529037d`; this independent agent made no commit.
- Raw firewall remains closed. No allowlisted `physics.npz`, root manifest or committed scientific classification was opened by this agent.
- Exact next authorized action: human/lead review of the independent source and normative closure of any nested serialization choices; then wait for explicit `RAW AUTHORIZED` before invoking raw mode.

## Normative nested-schema closure

The lead subsequently froze the remaining deterministic inner schemas and literals in the plan. I reread only that revised plan and updated the independent code without inspecting the primary implementation or outputs:

- exact five-key runtime object including `pytest=8.4.2`;
- exact Git-tree binding, raw-inventory and aggregate qualification-gate objects;
- exact world/track maintenance units and warning strings;
- exact outcome-dependent bounded-roadmap recommendation, allowed action and forbidden-action list.

Post-adjustment validation:

- `py_compile`: PASS;
- `--self-test`: `SELF_TESTS_PASSED`, 17 umbrella fixtures;
- direct runtime gate: exact canonical object `{"byteorder":"little","interpreter":"C:/Users/tommy/Documents/ising v3/.venv/Scripts/python.exe","numpy":"2.5.1","pytest":"8.4.2","python":"3.12.10"}`.

OBSERVED pre-raw reseal state at this checkpoint:

- current plan SHA-256: `dd11e9b22579664bbbf5743449a199d869269ed64833c3bdd61ba3d3a3252ada`;
- plan still binds protocol `a63048283aeab44f0e39d997dfe049b6f20ccf44ec9a54efcd59d7d595c9eead`, while the revised protocol is `ab47ac077924f146197e27b26f8bb5fa42d691460d5d72c945687942b4e6bbd0`;
- plan still binds allowlist `3b86f6559620c7130660b0daec84066a1be0c29657fafd266803e5e28ea431a0`, while the revised allowlist is `c8af265d28b898025b989025ddcf8c39f99c5fe5c48d03113ce7a410ba9ef670`.

INFERRED: this is a deliberate fail-closed pre-raw state pending lead reseal, not a code failure. Raw mode will reject these mismatches. Exact next action remains: reseal the plan bindings and checkpoint the pre-raw package, then wait for explicit `RAW AUTHORIZED`.

## Independent response to latest code-only red team

At `2026-07-19 02:11:42 +02:00`, the lead assigned a targeted re-audit against the latest red-team blockers. I read only the revised plan/protocol and red-team journal; I did not open the primary analyzer, primary tests or any raw/scientific input.

Corrections and affirmative checks:

- **Authenticated-byte identity:** added `authenticated_npz`, which performs the second size/SHA-256/Git-blob authentication and passes that same in-memory byte string directly into the safe loader. There is no path reopen between authentication and NumPy parsing. A mutation-after-preflight fixture proves substituted bytes are rejected before the loader is called.
- **Captured control hashes:** plan, protocol and allowlist hashes are captured exactly once from the same bytes that are parsed and are carried unchanged into `integrity.json` and `COMPLETE.json`. A synthetic publication fixture verifies those captured values survive sealing.
- **Exact independent root:** raw mode now requires the CLI spelling to equal `planned_outputs.independent_root` exactly, rejects absolute/primary/case-variant roots, rejects symlink/reparse parents, and requires the planned parent directory to exist. This reproducer has no comparison mode; comparison remains primary-owned.
- **Runtime:** the exact installed pytest version is queried with standard-library package metadata and gated at `8.4.2`; it is not asserted without verification.
- **Compact precursor availability:** all three precursors now require a gap-free representative pre-freeze record from onset through `terminal_freeze_onset-1`. Bond additionally requires at least eight pre-freeze observations; low heterogeneity requires the same complete record and at least eight observations; reduced exchange additionally requires exact consecutive first-16 and immediately-pre-freeze final-8 windows. Gapped/missing-frame fixtures fail closed.
- **Destructive law span:** factored exact qualifying support selection. A sole qualifying subtype supplies only its own support; when both subtypes qualify, support is the at-least-nine intersection; a nonqualifying subtype can add no laws. The red-team three-law counterexample now remains three-law support despite two extraneous laws and cannot satisfy the four-law actionability gate.
- **Publication:** exclusive root creation, `COMPLETE.json` last, captured control hashes and no-overwrite behavior are now exercised together in a temporary synthetic root.

Validation after these changes:

```powershell
& 'C:/Users/tommy/Documents/ising v3/.venv/Scripts/python.exe' -m py_compile analysis/interventional_individuality_stage_b_autopsy_reproduce.py
& 'C:/Users/tommy/Documents/ising v3/.venv/Scripts/python.exe' analysis/interventional_individuality_stage_b_autopsy_reproduce.py --self-test
```

Result: `SELF_TESTS_PASSED`, 21 umbrella fixtures. New coverage is authenticated-byte substitution rejection, exact independent-root enforcement, captured-hash/no-overwrite publication, verified pytest version, complete/gapped compact precursor windows, and qualifying-subtype-only destructive law span.

OBSERVED current binding state remains fail-closed: current plan SHA-256 `f971ae1d0684dec3e95a170536caf10e2aa40e3cc4b5bde9dc1ac593a64e7128` still embeds the old protocol and allowlist hashes while actual revised controls remain `ab47ac077924f146197e27b26f8bb5fa42d691460d5d72c945687942b4e6bbd0` and `c8af265d28b898025b989025ddcf8c39f99c5fe5c48d03113ce7a410ba9ef670`. Raw mode correctly refuses this state.

WHAT WOULD FALSIFY READINESS?: any failure of the final resealed control hashes, a substitution fixture reaching the loader, an accepted non-independent output root, a missing required precursor frame remaining available, an unqualified subtype contributing a law, or post-raw byte/classification/candidate mismatch.

Exact next action: lead reseals/checkpoints the final controls and code-only package; this reproducer remains stopped before raw until explicit authorization.

## Final pre-raw hardening and disclosed independence limitation

At `2026-07-19 02:23:03 +02:00`, the lead assigned another code-only pass. No raw, root manifest, committed classification, shard manifest, enrollment, engine or scientific world was opened.

Implemented gates, all before any physics-file read:

- CLI `--plan` and `--allowlist` are retained as raw strings and must equal the two exact forward-slash repo-relative frozen literals. Absolute, case-varied, `..`, `./` and backslash variants fail.
- Plan, allowlist and protocol are read through one symlink/reparse-aware control guard. The plan's protocol and allowlist path fields must themselves equal the exact frozen literals; captured bytes are parsed and their captured hashes are reused through publication.
- The authenticated root manifest now receives the exact plan-defined semantic gate: exact top keys and schema, `world_count=64`, `DEV_FEASIBILITY_FAIL`, manifest and classification hashes, exact captured classification byte count, positive enrollment/shard byte counts, valid lowercase SHA-256 fields, exactly 64 exact-schema `COMPLETE` shard rows and a unique world-ID set equal to `manifest.execution.world_ids`. Referenced enrollment and shard manifests remain unopened.
- Full canonical Stage-B classification is now independently constructed rather than copying committed `atlas` or `candidate_regions`: every lexicographically ordered world row, every nine-regime count including zeros, per-IC denominator/completeness, manifest-order law atlas, candidate regions and disposition are recomputed before byte comparison with the committed classification.
- Revised nested-output contracts are implemented: formation funnel `all_worlds` has no group key; candidate-duration `worlds` is the complete 11-row list; signature interpretation carries the full noncausal suffix; every signature now has the frozen support, numerator, denominator and direction semantics; compact freeze includes its aggregate gate; destructive qualification includes four-law support from only the qualifying subtype/intersection; unavailable reasons use only the two exact literals; empty/formation-failure worlds have null trajectory class; and trajectory-atlas regime/pathway/terminal count maps emit every frozen vocabulary key including zeros.

Synthetic suite after hardening: `SELF_TESTS_PASSED`, 26 umbrella fixtures, including new exact-control-path, root-manifest semantic rejection, full independent classification aggregation, complete zero-count atlas vocabularies and empty-world null-trajectory fixtures.

### Accidental limited primary-code exposure

This is a binding provenance limitation and is recorded without minimization.

- Intended command/scope: a targeted literal audit against only the frozen analysis plan and reconstruction protocol, issued after the independent mechanics and 21-fixture suite were already implemented and passing:

```powershell
rg -n 'literal|interpretation\"|signature_interpretation|precision_warning|recommendation|allowed_next_action|forbidden_actions|unit\"|schema_literal|encoding\"' docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B_AUTOPSY_ANALYSIS_PLAN.json docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B_AUTOPSY_RECONSTRUCTION_PROTOCOL.json
```

- Unexpected result: despite the intended two-file scope, tool output included workspace-wide match snippets. It exposed several isolated primary-analyzer lines: formation/opportunity field names, the full five signature-interpretation literals, already-frozen maintenance units/warnings, roadmap keys/strings and names/lines of primary path-validation helpers. It also exposed isolated primary-test names/lines, notably a compact-precursor continuity test name. No primary result, scientific outcome, classification value, raw array, physics path contents or prospective information appeared.
- Timing: all detector, association, tracking, cohort, classifier, developmental, aggregation, firewall and publication mechanics had already been independently authored; 21 code-only umbrella fixtures were passing. The accidental output occurred before this final control/root/classification/schema hardening pass.
- No primary analyzer or test file was deliberately opened, and no primary output was opened. Nevertheless, the command evidently caused limited snippets from those files to enter this agent's context, so strict clean-room independence is procedurally tainted.
- Code changed afterward: full signature suffix; exact plan/allowlist/protocol path guarding; root-manifest semantic gate; complete independently aggregated classification; candidate-duration row list; exact signature support/numerator/denominator/direction/unavailability semantics; compact aggregate and four-law destructive qualification; empty-world null trajectory; atlas funnel and complete zero-count vocabularies. These changes were implemented against newly frozen plan/protocol clauses and lead-provided root-manifest schema, but the signature/maintenance/path portions overlap the accidentally exposed snippets.
- Immediate action: the incident was reported to the lead as soon as recognized. Whether final two-implementation qualification requires a fresh clean reproducer is a human/lead provenance decision; this journal does not claim the exposure away.

OBSERVED current deliberately unsealed state: plan SHA-256 `721f1bd1e028f7d27c7ff0e1ec0e111c0b8daf9cbb7e576a8179663ec61974d0`; it still binds old protocol/allowlist hashes, while actual evolving controls are protocol `ee7c162755eebbcf57f2ed343c6f5d6bc916b1a971570f4687442cf7f6ff692f` and allowlist `75b23538e5c7820d96414696727d65baf4bdccde05b3a41c4e49ae686763061b`. The analyzer fails closed until resealed.

Exact next action remains final lead/red-team code-only review and reseal. No raw authorization is inferred.
