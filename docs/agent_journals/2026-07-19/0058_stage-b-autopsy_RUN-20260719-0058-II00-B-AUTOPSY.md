# INTERVENTIONAL-INDIVIDUALITY-00 Stage-B raw-only developmental autopsy journal

- **Role:** primary raw-only developmental-analysis integrator
- **Run ID:** `RUN-20260719-0058-II00-B-AUTOPSY`
- **Start time:** 2026-07-19 00:58 +02:00
- **End time:** IN PROGRESS
- **Starting branch:** `codex/interventional-individuality-00-stage-b-autopsy`
- **Starting HEAD:** `c31bf27ea80a6a3dcc60d0ec5380f668358671ff`
- **Starting Git state:** clean isolated sparse worktree at the exact accepted Stage-B parent
- **Ending Git state:** IN PROGRESS

## Assigned scope

Preserve `DEV_FEASIBILITY_FAIL` exactly. Freeze a machine-readable plan before opening any physics time series, then
perform a raw-only developmental autopsy of all 64 immutable Stage-B worlds. Recompute the frozen classification,
derive formation/activation/turnover/persistence/candidate/censoring trajectories, compare IC and law regions only
descriptively, separate formation from conditional maintenance, obtain independent reproduction and red-team review,
and return one authorized autopsy outcome without opening Stage C.

## Actions

- Verified the accepted parent and remote Stage-B tip are both exactly `c31bf27...`; the Stage-B worktree is clean.
- Preserved the unrelated dirty primary checkout without reset, clean, stash, restore or checkout-over.
- The first full Windows worktree checkout failed before registration because historical `_tomo_cache` filenames
  contain `|`, which Windows cannot create. No existing worktree or file was changed. Created the dedicated branch
  successfully with a sparse checkout limited to documentation, the exact Stage-B result root and new analysis code.
- Read the operating contract, Stage-B durable state/decision/index/journals, frozen manifest, root manifest and
  committed classification. No `physics.npz`, `online.json`, shard manifest, engine, runner or scientific module has
  been opened in this mission.
- Applied the data-analysis and validation procedures: freeze population/metrics first, retain original-world grain,
  validate counts/denominators and avoid selection, average-of-averages and incomplete-horizon claims.
- Froze a source allowlist, analysis plan and normative reconstruction protocol before loading any NPZ array. The
  final pre-raw hashes submitted for independent re-audit are plan
  `1088478a5e17ea8169143fe20715783cb0727a93727d9f6516313fbe37c5c0f9`, protocol
  `a63048283aeab44f0e39d997dfe049b6f20ccf44ec9a54efcd59d7d595c9eead`, and allowlist
  `3b86f6559620c7130660b0daec84066a1be0c29657fafd266803e5e28ea431a0`.
- The first independent pre-raw review returned `REVISE_PLAN_BEFORE_RAW`. Closed its binding counterexamples without
  opening raw: accepted-parent Git blob identity, all 160 detector frames, exact 46-key safe NPZ loader contract,
  detector/tracker/cohort/classifier equations, track-ID allocation, anti-stitching lineage rules, multiple-candidate
  aggregation, terminal/censoring semantics, observational signature windows/denominators, total outcome precedence,
  exact output schemas and whole-directory atomic publication.

## Important files read or changed

- Read only the operating-contract files and the allowlisted frozen Stage-B manifest, root manifest, committed
  classification and two completed Stage-B journals.
- Created `INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B_AUTOPSY_ANALYSIS_PLAN.json`.
- Created `INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B_AUTOPSY_RECONSTRUCTION_PROTOCOL.json`.
- Created `INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B_AUTOPSY_SOURCE_ALLOWLIST.json`.
- The independent reviewer created its separate append-only journal at the allowlisted 0100 red-team path.

## Reproducible commands and analyses

- PowerShell `ConvertFrom-Json`, exact manifest-world/allowlist sequence checks and `git diff --check` validate all
  three pre-raw JSON controls.
- `git ls-tree -r c31bf27... -- results/INTERVENTIONAL-INDIVIDUALITY-00-STAGE-B-DEV` yields 64 physics blobs; canonical
  sorted `path<TAB>blob_oid` records hash to
  `ad7e00a713d44568e5e64a695266e66b38a8324db12568786d5ccaa4aff13f71`.
- Runtime binding: Python 3.12.10, NumPy 2.5.1, pytest 8.4.2, little-endian, using the existing repository virtual
  environment. No raw time series was opened before this analysis-plan checkpoint.

## OBSERVED

- The committed Stage-B classification contains 64 complete worlds, 11 world-level candidates and zero qualified
  regions; the accepted disposition is `DEV_FEASIBILITY_FAIL` and cannot change.
- The stored result root contains outcome-bearing `online.json` and shard manifests, but the new firewall authorizes
  neither. The analysis must reconstruct measurements only from the exact 64 `physics.npz` files.

## INFERRED

World-level candidate labels conflate at least formation and conditional maintenance opportunity. A developmental
analysis can describe both denominators separately, but four replicates per law/IC cannot support precise formation
probabilities or causal parameter claims.

## HYPOTHESIS

The raw trajectories may distinguish a coherent compact-freezing, destructive-transition, transient-crossing or
horizon-censoring signature; alternatively, the candidate worlds may be heterogeneous or the stored variables may
leave mechanisms observationally unresolved.

## WHAT WOULD FALSIFY THIS?

- Any manifest/root/raw integrity mismatch or failure to reproduce the frozen 64 world labels returns `AUDIT_INVALID`.
- Missing persisted variables needed for the preregistered diagnostics returns `RAW_INSUFFICIENT`.
- Candidate pathways that fail the frozen coherence/actionability criteria return
  `TRANSIENT_OR_HETEROGENEOUS_CANDIDATES`, irrespective of visually suggestive examples.

## Failures and dead ends

- Full worktree checkout is impossible on Windows because inherited cache filenames use an invalid character. Sparse
  checkout is the non-destructive workaround; no cache path is in the autopsy allowlist.
- Two pre-raw validation commands failed closed without changing files: the first used an incorrect manifest path;
  the second checked a nonexistent `schema_version` key instead of the plan's `schema` key. The corrected validation
  confirmed valid JSON and 64 unique shard paths in exact manifest order. The early hashes recorded by that command
  were superseded transparently by the independently requested static amendments and the final pre-raw hashes above.
- While resolving the incorrect result-root spelling, a metadata-only directory listing exposed the filenames
  `online.json` and `shard_manifest.json` under the already-authorized Stage-B root. Their contents were not opened,
  hashed, parsed or used. This was a source-firewall process deviation, recorded here rather than concealed; all
  subsequent access is constrained to the explicit allowlist.
- A Python environment-report one-liner failed because PowerShell stripped nested JSON quotes; separate read-only
  version commands then produced the exact runtime binding. No file or scientific input was touched by the failure.

## Decisions

- The analysis plan, source firewall, developmental taxonomy and terminal outcome rule must be committed and pushed
  before any detailed `physics.npz` array is opened.
- No Stage-B threshold, candidate label, region rule, law or world may be revised or selected.

## Unresolved risks

- Prefix candidate status and mechanical freezing need exact pre-data definitions to avoid duration hindsight.
- Periodic seam proximity is coordinate-dependent and must be separated from true winding/percolation and finite-size
  diagnostics.

## Handoff

IN PROGRESS.
