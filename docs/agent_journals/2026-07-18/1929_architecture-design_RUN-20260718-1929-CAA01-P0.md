# CAUSAL-ADDRESSABILITY-ARCHITECTURE-01 Phase 0 integration journal

- **Role:** primary causal-access architecture and intervention-algebra designer
- **Run ID:** `RUN-20260718-1929-CAA01-P0`
- **Start time:** 2026-07-18 19:29:10 +02:00
- **End time:** 2026-07-18 20:10:00 +02:00
- **Starting branch:** `codex/causal-addressability-architecture-01-phase0`
- **Starting Git state:** clean at `a7d6adbb83dbd0f0219d21c0ba5af4fb57f2ef4c`
- **Ending Git state:** complete Phase-0 package ready for coherent commit on
  `codex/causal-addressability-architecture-01-phase0`; final commit/push recorded in the handoff response

## Assigned scope

Accept `STOP-OWNERSHIP-IDENTIFIABILITY` exactly. Independently reproduce the rank deficiency of the committed
12-row algebra and design/audit, without implementation or execution, the smallest conservative two-port extension
that could support a saturated single-target `Y^a(l,e)` local-by-environment access factorial. Keep the later
member-directed `Y_i^k(k_A,k_B)` factorial in a separate namespace and Phase D. Specify equations, DAGs,
counterfactual supports, shams, composition, no-feedback horizon, diagnostics, claims, staged revalidation, and an
adversarial GO/REVISE/STOP judgment. Permit `STOP-ARCHITECTURE` if no environmental port acts only on existing
environment-to-response edges without state overwrite, manufactured autonomy, or a new reporter path.

## Explicit source allowlist

Allowed durable/theory sources:

- `AGENTS.md`;
- `docs/RESEARCH_CHARTER.md`, `docs/PROJECT_STATE.md`, `docs/DECISION_LOG.md`,
  `docs/EXPERIMENT_INDEX.md`, `docs/RUN_INDEX.md`;
- `docs/agent_journals/2026-07-18/1847_theory-audit_RUN-20260718-1847-OI00-P0.md`;
- `docs/agent_journals/2026-07-18/1849_adversarial-identifiability-review_RUN-20260718-1849-OI00-REDTEAM.md`;
- `docs/individuation/OWNERSHIP_IDENTIFIABILITY_00_PHASE0_REPORT.md`;
- `docs/individuation/OWNERSHIP_IDENTIFIABILITY_00_INTERVENTION_ALGEBRA.json`.

Allowed exact source files:

- `edlab/substrates/scaffold/engine.py`;
- `edlab/experiments/sc_iom/engine.py`;
- `edlab/experiments/sc_mcm/engine.py`;
- `experiments/individuation/turnover_diag_engine.py`;
- `experiments/individuation/access_structure_operators.py`;
- `experiments/individuation/access_structure_noswap_operators.py`;
- `experiments/individuation/directed_causal_pair_phase05_executor.py`.

Forbidden: `results/`, exposed DEV paths, raw/world shards, checkpoints, reconstruction, scientific analyzers,
engine/test execution, seeds, genomes, V5/03G modification, prospective namespaces, and merge.

## Actions

- Read the operating contract and durable/current-experiment state from accepted commit `a7d6adbb...`.
- Verified the dirty primary checkout without changing it and created this isolated branch/worktree from the exact
  accepted commit, excluding only Windows-incompatible tracked `_tomo_cache` paths through sparse checkout.
- Spawned an independent adversarial architecture reviewer under the same explicit allowlist and no-execution
  boundary.
- Reproduced rank 2 from the accepted diagonal support and rank 4 for the saturated `L x E` support, with explicit
  null vectors and contrasts.
- Designed and red-teamed a transient first-update spatial `lambda_plus(x,t)` edge deletion and an unadmitted
  existing-parent dynamic-E sample-and-hold candidate.
- Integrated the critical namespace, claim, probe, passive-reader, architecture-stop, and A/B/C/D staging patch.
- Integrated every independent review blocker: conditional E semantics; exact exogenous `P(t)` separation;
  external `up_ref` numerator/denominator handling; ordinary `eta_w` preservation; forced-neutral active kernels;
  source/sink/boundary-work controls; first E-reachable `H*`; and one-update-only local treatment.
- Updated the project state, experiment/run indexes, and decision log without executing or implementing the engine.

## Important files read or changed

- Read: all files in the explicit allowlist above, with exact source hashes recorded in
  `docs/individuation/CAUSAL_ADDRESSABILITY_ARCHITECTURE_01_SOURCE_ALLOWLIST.json`.
- Created: `docs/individuation/CAUSAL_ADDRESSABILITY_ARCHITECTURE_01_PHASE0_REPORT.md`.
- Created: `docs/individuation/CAUSAL_ADDRESSABILITY_ARCHITECTURE_01_SPEC.json`.
- Created: `docs/individuation/CAUSAL_ADDRESSABILITY_ARCHITECTURE_01_SOURCE_ALLOWLIST.json`.
- Created: this journal and the independent red-team journal.
- Changed: `docs/PROJECT_STATE.md`, `docs/DECISION_LOG.md`, `docs/EXPERIMENT_INDEX.md`, and
  `docs/RUN_INDEX.md`.

## Reproducible commands and experiments

Static `git`, `rg`, `Get-Content`, `Get-FileHash -Algorithm SHA256`, `ConvertFrom-Json`, section-presence checks,
namespace/polarity scans, and `git diff --check` only. Both JSON files parsed; all seven exact source hashes matched;
required report sections were present; no trailing whitespace or diff-check error remained. No engine, module, test
runner, seed, scientific state, checkpoint, result, analyzer, or reconstruction was executed or opened.

## OBSERVED

- The accepted algebra has 12 intervention rows and no clean local/environment singleton access cells.
- The user requires strict separation of the single-target availability factorial `Y^a(l,e)` from the later
  member-directed cut factorial `Y_i^k(k_A,k_B)`.
- A `t0` sample-and-hold is exactly equal to the open parent on the first update, so a first-update response cannot
  identify E. The first E-reachable factorial horizon must be fixed from the static graph and is at least two updates
  in the audited explicit order.
- Holding the local gate closed through that later horizon would also gate newly written `Mf`; a one-update-only
  local treatment is required to address the already-formed state's existing edge while retaining ordinary writer
  parameters.

## INFERRED

The transient local `lambda_plus(x,t)` port is a valid candidate deletion of one pre-existing readout edge, but it
supports only a conditional edge claim. The environmental port remains an unadmitted candidate because it retains
baseline E information and may manufacture a quasi-autonomous boundary even without persistent state overwrite.

## HYPOTHESIS

A rank-4 mathematical port interface may be specifiable while still failing causal admissibility because the
frozen engine does not expose a unique modular environmental-to-response edge set independent of local state and
body dynamics.

## WHAT WOULD FALSIFY THIS?

An exact static decomposition of existing environment-to-response parents with a neutral open mode, a transient
state-preserving active gate, no state overwrite or new response path, explicit global-channel closure, and a
defensible exclusion argument would permit a conservative rank-4 architecture.

## Failures and dead ends

- Multiplying an alleged outside contribution was rejected: `_face_flux` is one nonlinear shared face function with
  no unique outside summand, and zeroing an additive Laplacian neighbour creates a sink.
- Literal first-update `L x E` response was rejected because sample-and-hold E is structurally inactive at `t0`.
- A local gate left active through `H*` was rejected because it would gate readout of newly written `Mf` rather than
  only the already-formed state.
- Symmetric permeability, all-field replay, active ghost/reporters, and passive shadow rank were rejected for the
  reasons recorded in the report.

## Decisions

- Keep `Y^a` and `Y_i^k` in separate namespaces, estimands, gates, schemas, and experimental phases.
- Treat a passive shadow reader as observational rank 0 and a localized `N` probe as admissible only through the
  existing `N` coupling with no `Mf`-specific reporter.
- Use `L0E0/L1E0/L0E1/L1E1` only for conditional local-edge versus post-`t0` dynamic-E-deviation availability;
  `E0` is not total environmental absence.
- Keep ordinary frozen parameters, including `eta_w`, identical in B and C. Apply `q_L` only in the first frozen
  update; interpret `Y_H*` local effects as later total effects of that transient `t0` deletion.
- Require direct open delegation and qualification-only forced-neutral active-kernel byte identity.
- Final disposition: **REVISE — CONSERVATIVE TWO-PORT CANDIDATE SPECIFIED, ENVIRONMENTAL PORT NOT ADMITTED**. This
  does not authorize Stage A. The binding Stage-A failure is `STOP-ARCHITECTURE`.

## Unresolved risks

- No exact implementation or complete existing-edge inventory has been qualified.
- Destination-specific sample-and-hold may create source/sink, boundary-work, body, or quasi-autonomy artefacts.
- `H*`, active-kernel equality, exhaustive E coverage, exact probe decomposition, and artificial-boundary exclusion
  remain code-only Stage-A proof obligations under a separate authorization.
- The unrestricted local/environmental/redundant/relational ownership taxonomy remains unidentified.

## Handoff

Independent review returned **PASS AFTER REQUIRED REVISIONS** for the Phase-0 `REVISE` recommendation. Human
architectural review of this package is the exact next action. Do not implement Stage A, run a scientific world,
initialize a seed, reconstruct a checkpoint, create a prospective namespace, modify V5/03G, merge, or treat this as
`GO_TO_A`. A separately authorized Stage A must fail to `STOP-ARCHITECTURE` if the E port cannot meet every stated
identity and exclusion condition.
