# INTERVENTIONAL-INDIVIDUALITY-00 Phase-0 journal

- **Role:** primary theory, substrate-comparison, and architecture integrator
- **Run ID:** `RUN-20260718-2115-II00-P0`
- **Start time:** 2026-07-18 21:15:12 +02:00
- **End time:** 2026-07-18 21:38:41 +02:00
- **Starting branch:** `codex/interventional-individuality-00-phase0`
- **Starting HEAD:** `81518cc7fcfa7ce4b9f0e7482480b290946dcbe0`
- **Starting Git state:** clean isolated sparse worktree at the accepted Stage-A STOP commit
- **Ending Git state:** coherent nine-path docs/spec package validated for a single intentional branch commit;
  clean worktree and remote push are required before handoff

## Assigned scope

Accept `STOP_ARCHITECTURE` without reinterpretation, close the current substrate, and perform Phase 0 theory/static
architecture comparison only for the smallest substrate in which interventionally addressable boundaries could
emerge without an ownership variable or entity identity. Compare phase-field droplets, conservative
contact-edge particle aggregates, and lattice matter with emergent membrane bonds; specify formal definitions,
minimal equations, conservation/rank obligations, anti-manufacture claims, a memory-free vertical slice, and a
staged A-F program. Do not implement, execute an engine, initialize a seed, modify V5, merge, or create a
prospective family.

## Actions

- Read the repository operating contract and Stage-A durable stop state in the required order.
- Verified the primary checkout is dirty in pre-existing Windows-incompatible cache paths and left it untouched.
- Created an isolated branch at exact accepted Stage-A commit `81518cc7...`.
- The first ordinary worktree checkout failed on tracked Windows-invalid `results/_tomo_cache` names. Git left the
  branch but no registered worktree. Recreated the worktree with no checkout, applied a sparse exclusion only for
  that incompatible cache directory, and populated the otherwise empty isolated tree with `git read-tree -mu HEAD`.
  No existing file or user change was reset, cleaned, stashed, restored, or discarded.

## Important files read or changed

Read under the exact source allowlist:

- governance/state: `AGENTS.md`, `docs/RESEARCH_CHARTER.md`, `docs/PROJECT_STATE.md`,
  `docs/DECISION_LOG.md`, `docs/EXPERIMENT_INDEX.md`, and `docs/RUN_INDEX.md`;
- accepted no-go: Stage-A qualification/report/spec and its final independent red-team journal;
- causal theory: the Phase-0 addressability report/spec, ownership-identifiability report, identity ontology,
  substrate proposals, droplet-access equivalence, and program/architecture separation report;
- static framework/source: `edlab/specs.py` and the particle, reaction-diffusion, scaffold, and Flow-Lenia engines.

Changed only the nine paths authorized by
`docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_SOURCE_ALLOWLIST.json`:

- created this journal, the independent journal, source allowlist, Phase-0 report, and machine-readable spec;
- updated `docs/PROJECT_STATE.md`, `docs/DECISION_LOG.md`, `docs/EXPERIMENT_INDEX.md`, and `docs/RUN_INDEX.md`.

## Reproducible commands and experiments

Static document/code inspection only. No engine or scientific world was run. Reproducible validation:

```powershell
Get-Content -Raw docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_SOURCE_ALLOWLIST.json | ConvertFrom-Json
Get-Content -Raw docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_SPEC.json | ConvertFrom-Json
Get-FileHash -Algorithm SHA256 -LiteralPath <each unchanged allowlisted source>
git diff --name-only
git ls-files --others --exclude-standard
git diff --check
```

The static validator confirmed all unchanged source hashes, both JSON parses, the authorization booleans, required
report sections, exactly nine authorized changed paths, and no whitespace error. No code test is relevant to this
docs/spec-only Phase 0; engine invocation is prohibited.

## OBSERVED

- Stage A is a binding architecture stop, not evidence against ownership or individuality.
- The current substrate failed because complete first-update E inactivity, common-probe separation, and a
  conservative localized environmental cut could not coexist under the frozen equations.
- A phase-field contour does not by itself enumerate high-order environmental parents or establish operational
  closure.
- Explicit particle contact fluxes are conservative, but without a physical surface state their boundary is a
  detector graph cut; maintaining a moving treatment risks IDs or tracker feedback.
- A uniform structural face state can host paired physical fluxes without encoding a component, but only if the
  same schema/law family admits no-boundary, open-sheet, percolated, dissolving, leaking, splitting, and merging
  regimes.

## INFERRED

The next substrate must expose antisymmetric, raw-observable physical edge fluxes before any history-dependent
state is introduced; otherwise an apparent boundary intervention can manufacture the decomposition being tested.
The smallest causally legible reference among the three audited families is Candidate C, lattice matter with a
uniformly dynamical structural bond/permeability state on every fixed face. This is an architecture preference,
not a prediction that entities will form.

## HYPOTHESIS

A substrate with locally formed physical boundary edges and pairwise conservative crossing ledgers can support a
rank-4 access design without encoding an entity label, provided the same equations also admit no-boundary,
percolated, dissolving, fusing, and splitting regimes.

## WHAT WOULD FALSIFY THIS?

If every candidate requires an imposed inside/outside label, a permanent entity identifier, unmatched work, or a
new reporter path to define L and E interventions, Phase 0 must return STOP rather than select a substrate.

## Failures and dead ends

- Ordinary `git worktree add` could not materialize Windows-invalid tracked cache filenames. Recovered without
  touching an existing worktree or scientific artefact, as recorded above.
- The first draft allowed the flux multiplied by the future environmental gate to serve as response. Independent
  review showed that this would algebraically guarantee the intervention's own effect. The final package makes
  crossing flux and direct local transduction compliance diagnostics only; primary `Q` is a separately frozen
  downstream native behavior.
- The first draft logged bond energy without a closed identity. The final equations couple positive bond formation
  to finite endpoint-resource withdrawal, log dissolution heat, and require an exact resource/bond/fuel/heat
  balance plus positivity/CFL proof before code.

## Decisions

- Keep persistent pattern, material component, operational entity, causally addressable individual, and
  reproductive lineage as five non-equivalent levels.
- Select Candidate C for human Phase-0 architecture review because it provides fixed physical faces, paired flux
  surgery, and the strongest explicit failure modes—not because it guarantees persistence or a positive result.
- Treat `b` as structural order only; it is neither memory nor `L`. Stage C must introduce a separate `h`, add no
  dedicated reporter, and revalidate the modified substrate from zero.
- Preserve separate `Y^a(l,e)` and `Y_i^k(k_A,k_B)` namespaces and phases.
- Final disposition: **GO FOR HUMAN PHASE-0 ARCHITECTURE REVIEW ONLY**. No implementation or Stage A is authorized.

## Unresolved risks

- Uniform local bond rules may still guarantee a membrane without an explicit closure reward; the must-fail family
  is binding.
- The future environmental gate can identify only dependence on one declared resource-crossing channel, never
  natural autonomy or total environmental access.
- Frozen `t0` supports limit the causal horizon for moving entities; tracker-following remains prohibited.
- Exact functions, parameters, compact admissible domain, numerical operation order, and tolerances remain
  intentionally unfrozen and require a separately authorized pre-code Stage-A design.

## Handoff

Human review may accept, revise, or stop Candidate C. The only next action is review of the Phase-0 report/spec.
Do not implement the substrate, open Stage A, search laws, initialize a seed, create a prospective family, or
transfer any 03G scientific result without separate explicit authorization.
