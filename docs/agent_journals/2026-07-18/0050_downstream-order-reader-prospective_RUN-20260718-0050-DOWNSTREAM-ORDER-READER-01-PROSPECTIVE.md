# Agent journal — DOWNSTREAM-ORDER-READER-01 PROSPECTIVE-RUN-00

- Role: sealed prospective execution and raw-only reproduction operator
- Run ID: `RUN-20260718-0050-DOWNSTREAM-ORDER-READER-01-PROSPECTIVE`
- Start: 2026-07-18 00:50 CEST
- End: 2026-07-18 02:10 CEST
- Starting Git state: clean exact sealed parent `0949ff7167082a894c12480e0b2c75b4cae1c7c6`
- Ending Git state: results and durable handoff committed on
  `codex/downstream-order-reader-prospective-seal-00`; push pending at journal creation
- Assigned scope: exact one-time execution of manifest
  `0d40765937ca203269bd7fa935f3db4c999576dabf2d6ca0f96223f777ba03e4`, seeds `58001-58048`, frozen
  classification, raw-only reproduction, commit and push; no scientific/code/protocol change, replacement,
  extension, merge, PR, V5/03G change or active reconstruction

## Actions and important files

1. Read the operating contract and durable project, decision, experiment, run, journal, manifest and seal state.
2. Verified exact parent, manifest, required seal branch, clean tree, absent output, unique ordered seed family,
   environment and all 15 bound hashes.
3. Ran the engine-free verifier before output creation: PASS 25/25; zero worlds initialized at preflight.
4. Saved the exact external manifest-bound authorization outside the repository and passed the runner's
   engine-free authorization check.
5. Invoked the exact sealed prospective command once. It ran W001-W048 in manifest order, exited 0 and wrote
   `status=COMPLETE`. No resume, retry, reserve, replacement or extension was used.
6. Opened scientific fields only after COMPLETE. Preserved all 13 incomplete-world records without zero coding.
7. Ran the independent raw-only reproducer twice. Both generations were byte-identical and recomputed every stored
   complete-world contrast and the classification exactly.
8. Ran the 24 sealed synthetic/instrumentation/contract tests: 24 passed.
9. Committed raw results separately, then prepared the scientific report, accounting, exact world table,
   environment/command record, claim boundary, inventory, decision and project indices.

## Reproducible commands

The exact execution and reproduction commands are preserved in
`docs/individuation/DOWNSTREAM_ORDER_READER_01_RUN_ENVIRONMENT.md`. The relevant post-run validation was:

```powershell
$py='C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe'
& $py -m pytest -q `
  experiments/individuation/test_downstream_order_reader_instrumentation.py `
  experiments/individuation/test_downstream_order_reader_prospective.py `
  experiments/individuation/test_downstream_order_reader_seal.py
# 24 passed in 1.98s
```

## OBSERVED

- All 48 fixed source worlds were attempted once; 35 were complete, 11 had incomplete history survival and 2 had
  incomplete deep turnover.
- All 48 worlds passed prehistory eligibility and exact clone identity. All 140 complete histories passed every
  frozen mechanical gate. No world had a manipulation or numerical failure.
- Primary `delta_A_O`: mean `5.0871520880175235e-08`, median `8.55489855591438e-11`, interval
  `[-3.874958897076355e-09, 1.0561800065742683e-07]`, 21 positive / 14 negative, required signs 27,
  `delta_num=8.97597327013001e-12`.
- The frozen classifier returned `NO_ACCESS_ESTABLISHED`.
- Direct source calibration was positive: 34/35 positive and interval wholly above zero.
- Secondary `lam_minus=0` response interval included zero, with 22/35 positive signs.
- Raw-only reproduction PASS; all stored complete contrasts exact; runner/engine not imported; second generation
  byte-identical.

## INFERRED

- The constitutive source step is responsive under this probe, but the one-update downstream flux interaction is
  not established under the frozen interval/sign rules.
- Because no scientific equivalence margin exists, the primary result cannot be interpreted as equivalence or
  absence.

## HYPOTHESIS

The turnover-surviving order information may be inaccessible to this first one-update internal face-flux reader,
too variable at the original-world scale, expressed in a different downstream relation, or absent at this probe.
These remain alternatives; the present result does not distinguish them.

## WHAT WOULD FALSIFY THIS?

- Any mismatch between committed raw shards and the immutable manifest.
- Any failure to reproduce the world contrasts, interval, signs, numerical floor or classification from raw only.
- Evidence of a failed frozen mechanical gate, duplicate/replaced world, changed code/protocol or extra scientific
  initialization.
- A valid independently preregistered experiment establishing a different downstream access result would update
  the broader mechanism, but is not authorized here.

## Failures and dead ends

- Before the final instruction to execute exactly as sealed, an isolated run-branch preflight failed the immutable
  required-branch gate and correctly initialized zero worlds. That failure was committed and pushed separately as
  `adfb189`; it is not part of this result branch and did not consume a scientific world.
- Initial staging of raw files was refused by sparse-checkout. No file was staged or changed. Repeating the same
  explicit path list with Git's `--sparse` option succeeded.
- PowerShell's case-insensitive JSON conversion rejected raw rows containing both scientific fields `C` and `c`.
  Read-only validation was repeated with standard-library Python; raw bytes were not changed.

## Decisions, unresolved risks and handoff

- D-098 records the consumed authorization and frozen `NO_ACCESS_ESTABLISHED` classification.
- The exact maximum claim is limited to failure to establish the one-update downstream interaction under this
  probe; never absence, equivalence, ownership, identity or active reconstruction.
- No code, protocol, seed, endpoint, threshold, classification, V5 or 03G artefact changed.
- Exact next action: human review of the completed result. No follow-up experiment is automatically authorized.
