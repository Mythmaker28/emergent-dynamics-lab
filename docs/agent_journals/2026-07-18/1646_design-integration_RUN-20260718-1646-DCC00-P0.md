# DEEP-CHECKPOINT-CAUSAL-CUT-00 Phase 0 integration journal

- **Role:** primary scientific audit and design integrator
- **Run ID:** `RUN-20260718-1646-DCC00-P0`
- **Start time:** 2026-07-18 16:46:09 +02:00
- **End time:** 2026-07-18 18:24:00 +02:00
- **Starting branch:** `codex/deep-checkpoint-causal-cut-00-phase0`
- **Starting Git state:** clean at `cce7a27955f5cfb1fc3d95388bba9378bc8d85eb`
- **Ending Git state:** scientific package committed at
  `84ee12a35cd159a83c41748f6360c1c79fd687eb`; this exact journal-state update is the closure-only follow-up commit

## Assigned scope

Accept `STOP_PAIR_MECHANICS` unchanged and perform Phase-0 audit/design only for a distinct question: whether an
already-formed, turnover-surviving causal state can be accessed directionally from one member of a viable pair
without writing new history. Audit only already-open DEV checkpoints and existing local-cut candidates; design an
exact-clone `C00/C10/C01/C11` cut factorial, separate future `Y_A/Y_B`, the full non-symmetrized causal matrix,
competing hypotheses, raw schema, kill switches, and DEV-only feasibility. Stop before any prospective execution.

## Actions

- Read the repository operating contract and durable state in the required order.
- Verified the completed `DIRECTED-CAUSAL-PAIR-00` Phase 0.5 stop, exact raw reproduction, and no-outcome boundary.
- Inspected the dirty primary checkout without changing it.
- Created this isolated branch from exact accepted Phase-0.5 tip `cce7a27955f5cfb1fc3d95388bba9378bc8d85eb`.
- Used sparse checkout to exclude only tracked `_tomo_cache` names that Windows cannot materialize; no scientific
  path or existing worktree was modified.
- Delegated independent checkpoint, local-operator, identification, factorial/raw-contract, and final-package audits;
  each retained its own journal and made no scientific-source change.
- Audited the four accepted outcome-blind pair candidates and reconciled their state digests across
  `ACCESS-STRUCTURE-00` Phase 0.5/0.6B.
- Audited global ablations, local `Mf` standardization, own/reference collar replay, and the off-branch
  `STOP-TRANSPLANT` report without checking out or copying that branch.
- Wrote the Phase-0 report, machine-readable DEV feasibility audit, fail-closed raw-schema draft, and durable state,
  experiment, run, and decision records.
- Hardened the schema after red-team findings so arm names bind cut vectors and sham status, outcome unlock binds
  all mechanical gates, integrated sham responses remain raw, clone/C11 checks are explicit, diagnostics are closed,
  and strict no-new-history behavior is hash-bound.

## Important files read or changed

Read, outcome-blind derivative/design/code evidence:

- `docs/individuation/DIRECTED_CAUSAL_PAIR_00_PHASE05_REPORT.md` and its fixed manifest/reproduction summary;
- `docs/individuation/DIRECTED_CAUSAL_PAIR_00_PHASE0_DEV_FEASIBILITY.json`;
- `docs/individuation/ACCESS_STRUCTURE_00_PHASE05_DEV_QUALIFICATION.json`;
- `docs/individuation/ACCESS_STRUCTURE_00_PHASE06B_NOSWAP_DESIGN.md` and `...RESULTS.json`;
- `experiments/individuation/access_structure_noswap_operators.py` and qualification/tests;
- `experiments/individuation/causal_confirm.py`, `turnover_engine_03g.py`, and
  `edlab/experiments/sc_mcm/engine.py`;
- off-branch `ACCESS_STRUCTURE_00_PHASE06A_BOUNDARY_REPORT.md` and result JSON at exact commit
  `bf5901a962bde2004dcd526a1eb083337d6e1410` through `git show` only.

Changed:

- `docs/individuation/DEEP_CHECKPOINT_CAUSAL_CUT_00_PHASE0_REPORT.md`;
- `docs/individuation/DEEP_CHECKPOINT_CAUSAL_CUT_00_PHASE0_DEV_FEASIBILITY.json`;
- `docs/individuation/DEEP_CHECKPOINT_CAUSAL_CUT_00_RAW_SCHEMA_DRAFT.json`;
- `docs/PROJECT_STATE.md`, `docs/EXPERIMENT_INDEX.md`, `docs/RUN_INDEX.md`, and `docs/DECISION_LOG.md`;
- this journal. Independent journals are preserved unchanged by the integrator.

## Reproducible commands and experiments

No engine, writer, future feeding probe, factorial arm, or prospective seed was run.

Read-only/code-contract validation:

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
& 'C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe' -m pytest -q `
  experiments/individuation/test_access_structure_noswap_operators.py `
  experiments/individuation/test_access_structure_operators.py `
  experiments/individuation/test_directed_causal_pair_phase05_mechanics.py
# 68 passed in 8.08s
```

- Both new JSON files parse under Python.
- The raw draft passes `jsonschema==4.25.1` Draft-2020-12 meta-schema validation from a pinned temporary install
  outside the repository.
- Synthetic in-memory contract fixtures verify that both locked and mechanically unlocked records validate, while
  an unlock with failed gates, an incorrect C10 cut vector, and a derived raw contrast field are rejected.
- Exact SHA-256 values for every in-branch input and both off-branch Phase-0.6A artefacts were recomputed from bytes.
- The final independent red-team resolved 106 local schema references, found no remaining open typed object or
  required/property mismatch, and returned PASS with `STOP-LOCAL-CUT` compelled.

## OBSERVED

- `STOP_PAIR_MECHANICS` is accepted as final for the history-writer path and will not be rescued or reinterpreted.
- The primary checkout contains unrelated user deletions and is not used for this run.
- Four worlds are observation-level deep candidates: 50002@847, 50004@793, 50005@831, and 50007@890. All pass the
  available endpoint pair-separation evidence and have empty accepted turnover censor records.
- No complete serialized checkpoint payload exists. State digests disagree across accepted ACCESS-STRUCTURE
  artefacts for 50002 and 50004; 50005/50007 agree but still have no payload. Continuous pair geometry, complete
  tracker-edge/gate logs, and a self-contained scalar joint P/M checkpoint binding are absent.
- `lam_plus=0` and `up_ref=0` are global. Local `Mf` erasure changes the stored state. Existing collar replay writes
  all eight state arrays on a fixed ring; its reference branch has maximum boundary/core disturbance
  1.8145423147669801/1.8655212794863028 against a zero/zero unprobed own replay. Dual C11 use is unqualified.
- The off-branch Phase-0.6A report explicitly returns `STOP-TRANSPLANT` and does not supply a rescued operator.
- Two independent over-broad read-only commands accidentally displayed fragments from pre-existing single-target
  outcome-bearing DEV files. Both were stopped; no pair Y/C/I contrast or displayed value was extracted, selected,
  or used. The deviations are disclosed in the report, audit JSON, and responsible individual journals.

## INFERRED

The no-new-history deep-checkpoint question is scientifically distinct, but the present record cannot instantiate
it. Four observational leads do not substitute for exact checkpoint bytes, and none of the existing operations is a
single local access-edge cut with a disturbance-matched sham. The only fail-closed disposition is
`STOP-LOCAL-CUT`; the scientific question remains unanswered.

## HYPOTHESIS

Falsified at the mechanical-audit level: no already-qualified no-swap/readout-ablation operator provides a local
cut from an exact serialized deep checkpoint without new history writing.

## WHAT WOULD FALSIFY THIS?

A counterexample would require existing persisted checkpoint bytes with exact continuation, complete continuous
pair/tracker evidence, and an already-tested physical-support operator that severs only one declared equation edge,
preserves common state/global terms/clock, composes order-independently at A+B, matches sham disturbance, and keeps
the separated pair viable and independently tracked. No such record was found.

## Failures and dead ends

- A normal Windows worktree checkout failed because the historical tree tracks cache filenames containing `|`.
  Git removed the incomplete worktree path; the branch remained intact. A clean `--no-checkout` worktree followed
  by sparse exclusion of only `results/_tomo_cache` succeeded.
- Initial hash commands used two wrong relative paths and changed no file; the actual engine/raw locations were
  resolved before recording hashes.
- Repository and bundled Python lacked `jsonschema`, and bundled Node lacked Ajv. A pinned `jsonschema==4.25.1` was
  installed into a task-specific temporary directory solely for read-only schema validation; no environment lock or
  repository file changed.
- The two accidental single-target raw displays described above are protocol deviations. They are not hidden or
  scientifically used; the audit stops independently on checkpoint/operator mechanics.

## Decisions

- Branch from the accepted stop commit rather than the dirty primary checkout.
- Do not execute a writer, prospective seed, future feeding probe, or retrospective Y/C/I contrast.
- Count the four worlds as observational candidates, not exact checkpoints; exact checkpoint admission is 0/10.
- Require strict future `eta_w=0`, zero external writer calls, and zero exposure-dependent history increment, while
  allowing only hash-bound unchanged passive copying/decay/transport of pre-existing `Mf`.
- Preserve the conditional `C00/C10/C01/C11` matrix only as a non-executable identification target.
- Return `STOP-LOCAL-CUT`, not GO/REVISE, because inventing a new operator would exceed this mission.

## Unresolved risks

- The scientific directional-access question is unanswered.
- No exact deep checkpoint can be executed from committed bytes, and two accepted reconstruction digests conflict.
- A future operator-development mission must resolve fixed versus physically comoving support without allowing
  diagnostic identity or treatment-dependent tracker association into physics.
- The two disclosed raw-display deviations preclude claiming perfect outcome-firewall process compliance, even
  though no pair outcome was computed or used and the mechanical stop is outcome-independent.

## Handoff

**STOP-LOCAL-CUT.** Exact next authorized action: human review of
`docs/individuation/DEEP_CHECKPOINT_CAUSAL_CUT_00_PHASE0_REPORT.md` and its two machine-readable companion drafts.
Any checkpoint serialization or local equation-edge operator development is a new separately authorized mission.
Do not execute this factorial, create a prospective namespace, open `58xxx`, change V5/03G, or reinterpret either
`STOP_PAIR_MECHANICS` or `STOP-TRANSPLANT`.

Scientific package commit: `84ee12a35cd159a83c41748f6360c1c79fd687eb`.
