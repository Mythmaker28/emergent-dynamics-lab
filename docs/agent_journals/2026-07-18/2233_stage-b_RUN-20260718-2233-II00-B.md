# INTERVENTIONAL-INDIVIDUALITY-00 Stage B journal

- **Role:** primary Stage-B instrumentation, DEV-map and evidence integrator
- **Run ID:** `RUN-20260718-2233-II00-B`
- **Start time:** 2026-07-18 22:33 +02:00
- **End time:** IN PROGRESS
- **Starting branch:** `codex/interventional-individuality-00-stage-b`
- **Starting HEAD:** `b0dbab7674816ebdf3f3f911694b2744ca4bfc76`
- **Starting Git state:** clean isolated sparse worktree at the exact accepted Stage-A parent
- **Ending Git state:** IN PROGRESS

## Assigned scope

First qualify a dynamically passive detector/tracker/tracer/diagnostic/classification stack on synthetic fixtures.
Only after B0 passes, freeze and commit the complete B1 DEV family, execute every fixed world without replacement,
publish atomic raw shards and a complete atlas, obtain raw-only reproduction and independent adversarial review,
then return exactly one authorized Stage-B outcome.

## Actions

- Read the human authorization and hash-bound it before using the engine.
- Verified exact parent, remote repository identity, clean Stage-A worktree, and dirty-primary isolation.
- Created a dedicated Stage-B sparse worktree/branch without altering the historical primary checkout.
- Read the operating contract, durable Stage-A state/decision/index/journals, and last Stage-A manifest/report.
- Used architecture and testing-strategy guidance to enforce a two-commit B0/B1 gate before DEV execution.
- Commissioned an independent reviewer who froze kill switches before inspecting B0 code.
- Implemented passive matter-only detection, geometry-only tracking, gross-flow cohort tracing, ordinary diagnostics,
  deterministic regime classification, and 26 new synthetic tests.
- Committed and pushed the independently qualified B0 checkpoint as `93f4a42d8972d2d4b9f8da6f1dc3c8161dc3c045`.
- Only after that checkpoint, drafted the B1 runner and raw schema. The runner records every state and complete
  Stage-A ledger, compares vectorized/reference paths at every step, repeats every update byte-for-byte, derives
  candidate activity from internal native flows only, publishes one terminal shard per enrolled world, and refuses
  any existing final or partial family root.
- A separate raw-only reproducer preregistered its source firewall before seeing the manifest, schema, runner or any
  output. Its exact journal path replaced the allowlist placeholder before B1 seal.
- The first independent reproducer draft correctly failed closed because the v1 contracts omitted the producer's
  nested manifest/shard layout, law-specific `dt`, exact classification object and numerical traversal order. No
  output existed. An outcome-free v2 contract amendment now specifies those fields before the reproducer may
  resume; the held draft is not qualification evidence.

## Important files read or changed

IN PROGRESS.

## Reproducible commands and experiments

```powershell
$env:PYTHONPATH = (Get-Location).Path
& 'C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe' -m pytest -q `
  tests/test_lattice_bond.py tests/test_lattice_bond_instrumentation.py
```

Current B0 result: `56 passed`. Static compilation, JSON parsing and the engine-to-instrumentation dependency
check also pass. No DEV world has been initialized.

## OBSERVED

- A detector based on `b` would make later boundary/conductance reasoning circular. B0 therefore uses only `m`
  threshold and periodic four-neighbour connectivity.
- Cumulative boundary flux is not turnover: recirculation can produce arbitrary gross traffic with complete cohort
  return. Exact current cohort retention is required.
- One-frame contact requires lookahead. When a unique pre/post bijection does not exist, the tracker must refuse
  rather than duplicate the contact component or force identities.

## INFERRED

The measurement stack can remain tracker-independent only if passive cohorts and final diagnostic values are
strictly downstream of association. This separation is structural in module dependencies and tested by byte-level
logged/unlogged identity.

## HYPOTHESIS

The qualified local lattice law may or may not yield bounded active turnover structures under neutral fresh DEV
initialization. A complete fixed map, not a visually selected world, can answer that feasibility question.

## WHAT WOULD FALSIFY THIS?

- Any B0 passivity, tracker-independence, periodic-geometry, lifecycle, tracer conservation or deterministic
  classifier failure returns `REVISE_INSTRUMENTATION` before B1.
- Any Stage-A conservation/positivity failure during B1 returns `MANIPULATION_OR_NUMERICAL_INVALID`.
- A complete valid B1 map with no predeclared reproducible candidate region returns `DEV_FEASIBILITY_FAIL`.

## Failures and dead ends

- The first diagnostic implementation iterated a three-dimensional work array as two-dimensional slices; three
  tests raised `TypeError`. Flattening the already-computed field fixed the local instrumentation defect without
  changing a law or threshold. The complete focused suite then passed.
- Two attempted test launches selected interpreters without the repository dependencies: the system Python lacked
  `pytest`, and this isolated worktree intentionally has no private `.venv`. Re-running the allowlisted command with
  the existing repository virtual-environment interpreter was read-only and passed.
- The independent pre-DEV audit rejected three successive B0 drafts. It found: unused maximum-area gating plus
  retrospective identity bridging and incomplete edge/tracer/raw assembly; trusted observation geometry and
  non-fail-closed invalid rows; and symmetric qualified association ties converted to dissolution/appearance.
  Each counterexample was added as a synthetic regression. No DEV initializer was opened during repair.

## Decisions

- The B1 manifest must be a committed, hash-addressable checkpoint before any initializer call.
- No `b` threshold, conductance, intervention or cut response may enter Stage-B detection/tracking/classification.
- No repository-wide regression command will be used in Stage B.
- After the repaired exact-code audit, the independent reviewer returned `PASS_B0_FOR_MANIFEST_FREEZE`. B0
  authorizes only construction and commit of the complete B1 manifest; it is not a scientific disposition and no
  world was initialized before this gate.

## Unresolved risks

- The B1 code and manifest remain unauthorized for execution until the independent reviewer closes the exact
  pre-manifest code audit and then the complete hash-bound manifest audit.
- The closed-resource law may yield only transients or dissolution; that is a legitimate fixed-family result, not
  permission to extend the family.

## Handoff

IN PROGRESS.
