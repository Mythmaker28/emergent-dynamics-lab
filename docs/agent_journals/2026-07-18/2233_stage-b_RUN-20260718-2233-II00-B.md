# INTERVENTIONAL-INDIVIDUALITY-00 Stage B journal

- **Role:** primary Stage-B instrumentation, DEV-map and evidence integrator
- **Run ID:** `RUN-20260718-2233-II00-B`
- **Start time:** 2026-07-18 22:33 +02:00
- **End time:** 2026-07-19 00:41 +02:00
- **Starting branch:** `codex/interventional-individuality-00-stage-b`
- **Starting HEAD:** `b0dbab7674816ebdf3f3f911694b2744ca4bfc76`
- **Starting Git state:** clean isolated sparse worktree at the exact accepted Stage-A parent
- **Ending Git state:** clean after the final evidence/index commit and remote push

## Assigned scope

First qualify a dynamically passive detector/tracker/tracer/diagnostic/classification stack on synthetic fixtures.
Only after B0 passes, freeze and commit the complete B1 DEV family, execute every fixed world without replacement,
publish atomic raw shards and a complete atlas, obtain raw-only reproduction and independent adversarial review,
then return exactly one authorized Stage-B outcome.

## Actions

- Read the human authorization and hash-bound it before using the engine.
- Verified exact parent, remote repository identity, clean Stage-A worktree, and dirty-primary isolation.
- Created a dedicated Stage-B sparse worktree/branch without altering the historical primary checkout.
- Read the operating contract, durable Stage-A state/decision/index/journals, and last Stage-A manifest/report.
- Used architecture and testing-strategy guidance to enforce a two-commit B0/B1 gate before DEV execution.
- Commissioned an independent reviewer who froze kill switches before inspecting B0 code.
- Implemented passive matter-only detection, geometry-only tracking, gross-flow cohort tracing, ordinary diagnostics,
  deterministic regime classification, and 26 new synthetic tests.
- Committed and pushed the independently qualified B0 checkpoint as `93f4a42d8972d2d4b9f8da6f1dc3c8161dc3c045`.
- Only after that checkpoint, drafted the B1 runner and raw schema. The runner records every state and complete
  Stage-A ledger, compares vectorized/reference paths at every step, repeats every update byte-for-byte, derives
  candidate activity from internal native flows only, publishes one terminal shard per enrolled world, and refuses
  any existing final or partial family root.
- A separate raw-only reproducer preregistered its source firewall before seeing the manifest, schema, runner or any
  output. Its exact journal path replaced the allowlist placeholder before B1 seal.
- The first independent reproducer draft correctly failed closed because the v1 contracts omitted the producer's
  nested manifest/shard layout, law-specific `dt`, exact classification object and numerical traversal order. No
  output existed. An outcome-free v2 contract amendment now specifies those fields before the reproducer may
  resume; the held draft is not qualification evidence.
- V2 then exposed only operation-order and terminal-file conventions. The outcome-free V3 amendment freezes the
  detector stack traversal, per-law physical fields, raw reference-scale gate, failed-shard inventory, sorted merge
  parents, cohort divergence parentheses and exact classification booleans/ID clearing before another independent
  implementation pass.
- The independent v3 implementation passed large synthetic parity sweeps, but final preflight identified two
  unbound provenance parents: the executing reproducer source and exact runtime environment. Before any manifest or
  result existed, the outcome-free v4 contract at `f29a6a5ef57e416d60439b1ba19d8b17099907e5` froze the exact
  four-key environment object and pre-shard self-source hash check. The independent implementation then returned
  `STATIC_REPRODUCER_QUALIFIED` with source SHA-256
  `c058e96697347f8af613159cdcb58de5ee3e254d201c237961f366cc1fd08b58`.
- Added durable producer/independent parity regressions covering weighted periodic detection; split, merge,
  symmetric-tie and collapse tracking; operation-exact passive cohort transport; every one of the nine classifier
  precedence paths; canonical family aggregation; nested manifest, environment and self-source bindings; and
  failed-shard identity/inventory gates. The exact three-file Stage-A/B focused suite now passes `88` tests.
- Committed and pushed the independent-reproduction checkpoint as
  `e63f8556b691f1fe20caf4d1e417bc6e94daecf3`. The independent reviewer reran compilation and all `88` focused
  tests, verified the standalone import firewall and exact v4 hashes, and authorized construction of one manifest
  for audit only. The canonical 64-world B1 manifest was then sealed with excluding-field SHA-256
  `6d65fd1d1b5ea2838081add7df163554ec72b99a3cee961e896dfc6f301de579`; its complete file SHA-256 is
  `55ff34ba4de1415c98c9b63d59b201cb566486df2f91a4f098527804eb31ee30`. Producer and independent loaders both
  accept exactly 64 enrolled worlds. Neither the final nor partial DEV namespace exists.
- **Correction before execution:** the first manifest preflight rejected the hash-bound allowlist because its raw
  reproducer command omitted three required CLI arguments and named no durable output. No namespace existed and no
  DEV command ran. The allowlist now freezes two exact independent invocations with distinct outputs and reads the
  expected manifest SHA from a separately authorized post-seal digest file, avoiding a circular manifest/allowlist
  hash. The manifest was re-sealed without changing any law, IC, world, horizon, tracker or classifier field. Its
  superseding excluding-field seal is `5681f02b746dc024ea5e1c96d7e8ff83934d96493edf4e9a441e8222909ef3ae`
  and complete file SHA-256 is `194e082f9d3809f2531912d825480fad5b683dbe9d9fceec8050260fe493dd50`.
- After the final independent preflight, made the one and only runner invocation. It atomically published 64/64
  complete shards and returned `DEV_FEASIBILITY_FAIL`; no retry, replacement or extension occurred.
- Ran the two separately frozen raw-only invocations. Production classification, RUN1, RUN2 and the durable docs
  classification are byte-identical at SHA-256
  `7b7cf200fd6cc7ccfbd77b19de0ca1231df22c1d2d9ab5d7548828df7c3ed14e`.
- Independent raw-only and red-team post-run audits passed without blockers. The raw-only ordered shard-binding
  aggregate is `298f86ee06182ad180e3110bc09bbe34d5c95e31bc9b0aa7a7e0a3e7a9e71927`.

## Important files read or changed

- `edlab/substrates/lattice_bond/instrumentation.py`
- `edlab/substrates/lattice_bond/stage_b.py`
- `edlab/substrates/lattice_bond/stage_b_reproduce.py`
- `tests/test_lattice_bond_instrumentation.py`
- `tests/test_lattice_bond_stage_b.py`
- `docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B_SOURCE_ALLOWLIST.json`
- `docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B0_*`
- `docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B1_*`
- `docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B_CLASSIFICATION.json`
- `docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B_REPORT.md`
- `results/INTERVENTIONAL-INDIVIDUALITY-00-STAGE-B-DEV/`

## Reproducible commands and experiments

```powershell
$env:PYTHONPATH = (Get-Location).Path
& 'C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe' -m pytest -q `
  tests/test_lattice_bond.py tests/test_lattice_bond_instrumentation.py tests/test_lattice_bond_stage_b.py

& 'C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe' -m `
  edlab.substrates.lattice_bond.stage_b `
  --manifest docs/individuation/INTERVENTIONAL_INDIVIDUALITY_00_STAGE_B1_MANIFEST.json
```

Current B0 plus B1 static result: `88 passed`. Static compilation, JSON parsing and the engine-to-instrumentation
dependency check also pass. The one authorized DEV invocation returned `DEV_FEASIBILITY_FAIL`. The two exact
raw-reproduction commands are retained verbatim in the hash-bound source allowlist.

## OBSERVED

- A detector based on `b` would make later boundary/conductance reasoning circular. B0 therefore uses only `m`
  threshold and periodic four-neighbour connectivity.
- Cumulative boundary flux is not turnover: recirculation can produce arbitrary gross traffic with complete cohort
  return. Exact current cohort retention is required.
- One-frame contact requires lookahead. When a unique pre/post bijection does not exist, the tracker must refuse
  rather than duplicate the contact component or force identities.
- All 64 enrolled worlds completed. The world-class totals are 30 persistent-no-turnover, 11 static, 10 dissolved,
  2 empty/gas and 11 bounded-active-turnover candidate classifications, with no invalid or unresolved world.
- No exact LawSpec region passed the fixed minimum separately in both IC classes. Production classification and two
  independent raw-only reproductions are byte-identical at
  `7b7cf200fd6cc7ccfbd77b19de0ca1231df22c1d2d9ab5d7548828df7c3ed14e`.

## INFERRED

The measurement stack can remain tracker-independent only if passive cohorts and final diagnostic values are
strictly downstream of association. This separation is structural in module dependencies and tested by byte-level
logged/unlogged identity.

The 11 isolated candidate worlds are not a reproducible region and cannot be treated as a selected surviving
subset. Under the predeclared aggregation rule, the complete fixed family compels `DEV_FEASIBILITY_FAIL`.

## HYPOTHESIS

The qualified local lattice law may or may not yield bounded active turnover structures under neutral fresh DEV
initialization. A complete fixed map, not a visually selected world, can answer that feasibility question.

## WHAT WOULD FALSIFY THIS?

- Any B0 passivity, tracker-independence, periodic-geometry, lifecycle, tracer conservation or deterministic
  classifier failure returns `REVISE_INSTRUMENTATION` before B1.
- Any Stage-A conservation/positivity failure during B1 returns `MANIPULATION_OR_NUMERICAL_INVALID`.
- A complete valid B1 map with no predeclared reproducible candidate region returns `DEV_FEASIBILITY_FAIL`.

## Failures and dead ends

- The first diagnostic implementation iterated a three-dimensional work array as two-dimensional slices; three
  tests raised `TypeError`. Flattening the already-computed field fixed the local instrumentation defect without
  changing a law or threshold. The complete focused suite then passed.
- Two attempted test launches selected interpreters without the repository dependencies: the system Python lacked
  `pytest`, and this isolated worktree intentionally has no private `.venv`. Re-running the allowlisted command with
  the existing repository virtual-environment interpreter was read-only and passed.
- The independent pre-DEV audit rejected three successive B0 drafts. It found: unused maximum-area gating plus
  retrospective identity bridging and incomplete edge/tracer/raw assembly; trusted observation geometry and
  non-fail-closed invalid rows; and symmetric qualified association ties converted to dissolution/appearance.
  Each counterexample was added as a synthetic regression. No DEV initializer was opened during repair.
- The first complete manifest preflight rejected an incomplete raw-reproduction CLI entry. The repair froze two
  full invocations and a post-seal digest path, then re-sealed and recommitted the unchanged scientific family.
  This occurred before the result namespace existed; no retry or discarded world resulted.

## Decisions

- The B1 manifest must be a committed, hash-addressable checkpoint before any initializer call.
- No `b` threshold, conductance, intervention or cut response may enter Stage-B detection/tracking/classification.
- No repository-wide regression command will be used in Stage B.
- After the repaired exact-code audit, the independent reviewer returned `PASS_B0_FOR_MANIFEST_FREEZE`. B0
  authorizes only construction and commit of the complete B1 manifest; it is not a scientific disposition and no
  world was initialized before this gate.
- The terminal Stage-B decision is `DEV_FEASIBILITY_FAIL`: zero of eight regions pass, even though 11 individual
  worlds receive the world-level candidate label. No threshold relaxation, adaptive extension or selected-world
  rescue is authorized.

## Unresolved risks

- This four-replicate DEV screen is not a reliable probability estimate and cannot establish global impossibility.
- Stage C remains closed. Any future action requires a new human decision; no current candidate-world subset may be
  carried forward automatically.

## Handoff

Human review of the completed Stage-B package only. Do not open Stage C, rerun or extend the DEV family, change
thresholds, select the 11 individual candidate worlds, add memory/history, or introduce causal interventions.
