# OWNERSHIP-IDENTIFIABILITY-00 Phase 0 integration journal

- **Role:** primary intervention-algebra and causal-identifiability auditor
- **Run ID:** `RUN-20260718-1847-OI00-P0`
- **Start time:** 2026-07-18 18:47:12 +02:00
- **End time:** 2026-07-18 19:12:00 +02:00
- **Starting branch:** `codex/ownership-identifiability-00-phase0`
- **Starting Git state:** clean at `afa05ee82e2663e252e401dded587bcf060833b2`
- **Ending Git state:** coherent Phase-0 delivery committed on `codex/ownership-identifiability-00-phase0`; exact
  commit and push status are reported in the final handoff

## Assigned scope

Accept without reinterpretation 03G Outcome B, `STOP-TRANSPLANT`, `STOP-CORE-SUFFICIENCY`,
`STOP_PAIR_MECHANICS`, `STOP-LOCAL-CUT`, and downstream `NO_ACCESS_ESTABLISHED`. Audit the exact frozen intervention
algebra and determine, theoretically and without engine execution or outcome analysis, whether local,
environmental, redundant, synergistic/relational, or null ownership models are identifiable. Formalize competing
models, prove equivalence or identifiability, derive minimal additional intervention properties, compare four
candidate measurement extensions, specify revalidation burden, obtain independent adversarial review, and stop
before implementation or prospective execution.

## Actions

- Read the operating contract and durable state in the required order from the accepted
  `DEEP-CHECKPOINT-CAUSAL-CUT-00` tip.
- Verified the dirty primary checkout and all existing worktrees without changing them.
- Created this isolated branch/worktree from exact accepted tip `afa05ee82e2663e252e401dded587bcf060833b2`, using
  sparse exclusion only for Windows-incompatible tracked `_tomo_cache` filenames.
- Bound the downstream `NO_ACCESS_ESTABLISHED` lineage read-only at
  `8da33041ed0d3c8d26b9c61b3aa6913022795ed1`; no merge was performed.
- Statically inspected the exact frozen engine terms, diagnostic writer-path toggles, local carrier edits,
  serialization/cloning, transplant, replay/clamp, non-compact Gaussian writer, and common probe.
- Recorded eight source SHA-256 bindings and a machine-readable 12-row intervention algebra.
- Formalized separate availability responses `Y^a` and member-cut responses `Y_i^k`, with an unambiguous
  `k_i=1` means cut convention for `C00/C10/C01/C11`.
- Constructed an explicit non-injectivity witness on `L=E=Z` and the charitable rank-2 versus required rank-4
  saturated design argument.
- Derived the minimal local-cut property set, proved that it is insufficient for the full ownership taxonomy
  without an orthogonal environment/common factor, compared four architecture extensions, and specified raw
  diagnostics, kill switches, and full revalidation burden.
- Integrated an independent adversarial review. The reviewer required corrections to polarity, theorem scope,
  `H_0`, writer support, writer-path inventory, source bindings, and the instrument/manufacture boundary, then issued
  `PASS AFTER REQUIRED REVISIONS`.
- Updated `PROJECT_STATE`, `DECISION_LOG` (`D-094`), `EXPERIMENT_INDEX`, and `RUN_INDEX`.

## Important files read or changed

Read the required operating/durable records, the accepted deep-checkpoint report, the exact implementation files
bound in the algebra JSON, and the accepted downstream claim boundary without using any scientific result in the
theorem.

Changed:

- `docs/individuation/OWNERSHIP_IDENTIFIABILITY_00_PHASE0_REPORT.md`;
- `docs/individuation/OWNERSHIP_IDENTIFIABILITY_00_INTERVENTION_ALGEBRA.json`;
- `docs/PROJECT_STATE.md`;
- `docs/DECISION_LOG.md`;
- `docs/EXPERIMENT_INDEX.md`;
- `docs/RUN_INDEX.md`;
- this journal.

Independent journal incorporated without rewriting its findings:

- `docs/agent_journals/2026-07-18/1849_adversarial-identifiability-review_RUN-20260718-1849-OI00-REDTEAM.md`.

## Reproducible commands and experiments

Static/read-only commands only:

- `git branch --show-current`, `git rev-parse HEAD`, `git status --short --branch`, and `git diff --check`;
- `rg`, `Get-Content`, and `Get-FileHash -Algorithm SHA256` on exact source and documentation paths;
- PowerShell `ConvertFrom-Json` for the companion algebra;
- field-by-field comparison of all eight JSON source bindings to fresh SHA-256 hashes;
- required-section and durable-index presence checks.

No Python module, test runner, engine, seed, checkpoint, reconstruction, raw/result artefact, scientific analyzer,
retrospective contrast, or prospective family was opened or run. Code tests were intentionally not executed because
this mission changes documentation only and forbids engine execution; static JSON, hash, and diff validation passed.

## OBSERVED

- The accepted stop chain leaves ownership unanswered; it does not supply a negative ownership result.
- The primary checkout has unrelated tracked cache deletions and remains untouched.
- The exact algebra includes global `lam_plus=0`, global `up_ref=0`, passive-copy disable, `eta_w=0`, local `Mf`
  erasure, local reference-`Mf` substitution, own replay, reference replay/no-swap clamp, transplant, the
  target-centered non-compact history writer, standardized probe, and exact cloning.
- None is a qualified, state-preserving independent local/environment access generator. The only clean ownership
  support is intact joint access; a global ablation is not a valid joint access cut.
- The independent reviewer verified all 12 rows, all eight source hashes, notation polarity, JSON syntax, durable
  state, and `git diff --check`.

## INFERRED

Identifiability must be decided from the intervention maps and observable sigma-algebra, not from any realized
outcome magnitude or sign.

On the clean reachable support, `H_L`, `H_E`, `H_R`, and `H_S` are interventionally equivalent. Even granting a
global ablation as availability cell `a00` produces only rank 2 for four saturated coefficients; the actual clean
ownership rank is at most 1. A valid A/B local cut can identify directional edge dependence, but cannot by itself
separate environmental ownership from an assay-level ownership null or distinguish every redundant/relational
allocation.

## HYPOTHESIS

The four non-null models `H_L`, `H_E`, `H_R`, and `H_S` induce the same interventional law on the clean support of
the current algebra because no qualified operation independently toggles local and environmental access while
holding source state and all other causal parents fixed.

## WHAT WOULD FALSIFY THIS?

An existing, provenance-bound pair of interventions that independently and non-destructively toggles local and
environmental/relational access, preserves the other causal parents, reaches all four availability cells with
positive support, composes order-independently, and admits matched shams would falsify the full non-identifiability
claim. A single clean local edge cut would falsify only the narrower no-directional-addressability claim.

## Failures and dead ends

- A read-only command intended to bind the accepted downstream classification printed numerical summaries from the
  prospective report. The values are quarantined and will not enter the theorem, tables, ranking, or recommendation.
  No raw file, world shard, contrast table, reconstruction, or engine was opened. This scope deviation will remain
  disclosed in the final report and journal.

## Decisions

- Root the new branch at the latest accepted local-cut stop and audit the downstream branch only through immutable
  Git objects; do not merge lineages.
- Quarantine all outcome-bearing downstream and DEV files. Use only code, contracts, claim boundaries, provenance,
  and algebraic constructions from this point onward.
- Issue `STOP-OWNERSHIP-IDENTIFIABILITY — CURRENT ALGEBRA INSUFFICIENT`, scoped to the frozen algebra. It is not a
  negative ownership result, a reinterpretation of 03G Outcome B, or a permanent research stop.
- Treat the seven local-cut properties as necessary for directional member addressability, not sufficient for the
  complete local/environment/redundant/relational taxonomy.
- Classify spatial `lambda_plus(x)` as a legitimate potential surgery on an existing pointwise edge, but limit any
  future claim to conditional edge necessity. A new `Mf` reporter/driver or dedicated port would manufacture a new
  causal path/compartment.
- Require full prospective formation, persistence, deep-turnover survival, and causal-response revalidation for any
  modified architecture. Grandfather no 03G result.

## Unresolved risks

- The current algebra cannot answer the ownership question. A new local edge gate plus an orthogonal
  environment/common gate or a proved exclusion/modularity theorem is required.
- Existing conflicting ACCESS digests and exposed DEV paths remain provenance limitations and were not resolved by
  reconstruction or outcome inspection.
- Any future support rule tied to body/tracker state risks endogenous localization and must remain independent of
  diagnostic IDs, final `P`, material `M`, and future response.

## Handoff

Phase 0 is complete with independent `PASS AFTER REQUIRED REVISIONS`. Exact next authorized action: human review of
the report and companion algebra only. Do not implement an extension, reconstruct a checkpoint, open a seed or
prospective namespace, inspect a retrospective scientific contrast, modify V5/03G, or merge this branch under the
authority of this mission.
