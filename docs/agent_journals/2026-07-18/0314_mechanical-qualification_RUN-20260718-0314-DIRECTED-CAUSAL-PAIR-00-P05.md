# Agent journal — DIRECTED-CAUSAL-PAIR-00 Phase 0.5

- Role: primary mechanical-qualification implementer and scientific-integrity coordinator
- Run ID: `RUN-20260718-0314-DIRECTED-CAUSAL-PAIR-00-P05`
- Start time: 2026-07-18 03:14 CEST
- End time: IN PROGRESS
- Starting Git state: clean isolated worktree on `codex/directed-causal-pair-00-phase0` at accepted commit `4bcb551092291b7383c4168f653818d4bade14f6`; dirty primary checkout explicitly not touched
- Ending Git state: IN PROGRESS
- Worktree: `C:\Users\tommy\Documents\ising-v3-directed-causal-pair-00-phase0`
- Runtime lock: not applicable; this is a direct human-requested run, not a scheduled heartbeat

## Assigned scope

Implement and execute only the authorized DEV mechanical closure for the four frozen pair worlds 50002, 50004,
50005, and 50007: passive continuous pair geometry; exact H00/H10/H01/H11 clones; pair-context no-swap, sham,
`up_ref=0`, and common schedule mechanics; an outcome firewall; strict resumable raw evidence; raw-only reproduction;
and, only if mechanically qualified, an unsealed seedless prospective candidate. Never open or reconstruct a
prospective namespace or any `58xxx` artefact, compute or retain pair feeding results or Y/C/I, alter frozen physics,
writer, probe, turnover, thresholds, pairs, V5, or 03G, or create a PR/merge.

## Operating-contract reads

Read in required order: `AGENTS.md`, `docs/RESEARCH_CHARTER.md`, `docs/PROJECT_STATE.md`,
`docs/DECISION_LOG.md`, `docs/EXPERIMENT_INDEX.md`, `docs/RUN_INDEX.md`, the latest Phase-0 journal, and the Phase-0
manifest/report summary. Inspected Git state before branching and independently verified that the isolated worktree
was clean at the accepted Phase-0 commit. Created exact branch `codex/directed-causal-pair-00-phase05` there.

## Actions and important files

IN PROGRESS. This section will record exact implementation, execution, validation, hashes, commits, and push state.

## Reproducible commands and experiments

IN PROGRESS.

## OBSERVED

IN PROGRESS.

## INFERRED

IN PROGRESS.

## HYPOTHESIS

If every fixed history arm and access regime remains continuously separated, tracker-valid, clamp-clean, and
schedule-valid without ever extracting a pair response, then the pair experiment is mechanically ready for a
separate human seal review; this alone says nothing about individuation.

## WHAT WOULD FALSIFY THIS?

Any branch-level separation, halo, contact, fusion, tracker, sentinel, clamp, clone, schedule, nonfinite-state, or
outcome-firewall failure; any raw-only reproduction mismatch; or any inability to verify the exact fixed plan and
hash chain.

## Failures and dead ends

- The isolated worktree intentionally has no local `.venv`; all Python commands use the read-only interpreter and
  installed environment at `C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe` while the working directory
  and every write remain in the isolated worktree.
- A combined pytest invocation included the legacy script-style `test_bijective_tracker.py`, whose import-time
  `SystemExit(0)` caused pytest collection to report an internal error despite its 10/10 checks passing. It was rerun
  correctly as a standalone Python script, and the remaining suites were run under pytest.

## Decisions

1. Freeze the exact Phase-0 four-world order and A/B/sentinel assignments in a hash-bound manifest.
2. Use the Phase-0 draft's first per-world writer-stream amplitude pair identically at A and B, with the same two
   target operations in all four arms and zero-valued sham bits.
3. Let H00 alone establish the unchanged material-turnover step; all other arms run exactly to it.
4. Persist compact masks for every component and selected body/core/halo/collar so the engine-free reproducer can
   recompute rather than trust geometry gates.
5. Make immutable deterministic-gzip world shards with an uncompressed canonical-JSON predecessor chain the resume
   authority; publish the derived index and terminal record atomically, terminal last.

## Unresolved risks

IN PROGRESS.

## Handoff

IN PROGRESS.
