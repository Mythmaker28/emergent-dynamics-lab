# DEEP-CHECKPOINT-CAUSAL-CUT-00 Phase 0 final package red-team journal

- **Role:** independent final package scientific, provenance, schema, and outcome-firewall red-team
- **Run ID:** `RUN-20260718-1800-DCC00-FINAL-REDTEAM`
- **Start time:** 2026-07-18 18:00 +02:00
- **End time:** 2026-07-18 18:21 +02:00
- **Starting branch:** `codex/deep-checkpoint-causal-cut-00-phase0`
- **Starting Git state:** HEAD `cce7a27955f5cfb1fc3d95388bba9378bc8d85eb`; the Phase-0 package and prior
  agents' journals were uncommitted concurrent work
- **Ending Git state:** HEAD unchanged at `cce7a27955f5cfb1fc3d95388bba9378bc8d85eb`; this journal is the only file
  created by this red-team; shared package fixes were made by the primary integrator

## Assigned scope

Read-only final red-team of the Phase-0 report, DEV feasibility JSON, raw-schema draft, and durable
project/decision/experiment/run-index updates. Check scientific consistency, exact numerical and Git provenance,
schema closure and outcome-lock semantics, outcome-firewall compliance, and whether `STOP-LOCAL-CUT` is compelled.
Do not run a simulation, open a prospective family, inspect pair feeding outcomes, or edit shared package files.

## Actions

- Read the operating contract and durable state in the required order, the current integration journal, the accepted
  Phase-0.5 manifest/report context, all four independent Phase-0 journals, and every new package artefact.
- Verified the branch and exact parent commit and confirmed the parent Phase-0.5 branch tracks the pushed remote.
- Recomputed every in-branch input SHA-256 listed by the report/audit and independently hashed the two off-branch
  Phase-0.6A artefacts directly from Git object `bf5901a962bde2004dcd526a1eb083337d6e1410`.
- Cross-checked the four deep steps, selected A/B/sentinel assignments, endpoint distances/halo gaps, state-digest
  conflicts, and Phase-0.6B disturbance maxima against the outcome-blind derivative records.
- Red-teamed the raw schema. Initial findings included invalid sham-arm identity, unenforced cut vectors, inverted
  gate/unlock logic, open diagnostic/response structures, missing clone/C11 evidence, missing sham responses, and no
  strict no-new-history contract. The primary integrator repaired all findings before this final pass.
- Re-read the repaired schema and verified JSON syntax, 106 resolvable local references, zero typed object schemas
  without `additionalProperties`, zero required properties absent from their schemas, strict arm-specific cut/sham
  constants, locked-null responses, qualified-gate/all-world/all-arm unlock requirements, closed active and sham
  response maps, clone/C11 audits, and per-step `eta_w=0`/zero-writer evidence.
- Ran `git diff --check`; it passed.

## Important files read or changed

Read:

- `AGENTS.md`, `docs/RESEARCH_CHARTER.md`, `docs/PROJECT_STATE.md`, `docs/DECISION_LOG.md`,
  `docs/EXPERIMENT_INDEX.md`, and `docs/RUN_INDEX.md`;
- all five Phase-0 journals existing before this one;
- `docs/individuation/DEEP_CHECKPOINT_CAUSAL_CUT_00_PHASE0_REPORT.md`;
- `docs/individuation/DEEP_CHECKPOINT_CAUSAL_CUT_00_PHASE0_DEV_FEASIBILITY.json`;
- `docs/individuation/DEEP_CHECKPOINT_CAUSAL_CUT_00_RAW_SCHEMA_DRAFT.json`;
- the accepted derivative/mechanical inputs and exact operator source paths named in the report.

Changed: this journal only.

Final package hashes observed before this journal was written:

| Artefact | Bytes | SHA-256 |
|---|---:|---|
| `DEEP_CHECKPOINT_CAUSAL_CUT_00_PHASE0_REPORT.md` | 18,723 | `2f030fcc6b6b9dedd745c140e4a5b94b098ecec0f28810cbc079b8a89065b30c` |
| `DEEP_CHECKPOINT_CAUSAL_CUT_00_PHASE0_DEV_FEASIBILITY.json` | 10,049 | `90ce754e1b0c6e9e3e83460d12a7482f7af5db19d76efc9b30cd38a6b7a44da5` |
| `DEEP_CHECKPOINT_CAUSAL_CUT_00_RAW_SCHEMA_DRAFT.json` | 38,402 | `e307cc7b66ac9c0f43da6140715070b650877ee5d258cf937722c21c3e306c4c` |

## Reproducible commands and checks

Read-only checks used `git status --short --branch`, `git rev-parse HEAD`, `git show -s`, `git branch -vv`,
`Get-FileHash -Algorithm SHA256`, standard-library JSON parsing, schema-reference/closure scripts, and
`git diff --check`. Off-branch bytes were hashed through `git show <commit>:<path>` captured by standard-library
`subprocess` and `hashlib.sha256`; the report/result hashes matched exactly.

`python -m json.tool` passed for both new JSON files. The available Python environments do not contain the optional
`jsonschema` package, so this red-team did not claim a separate meta-schema-library run; the static Draft-2020-12
reference, required-field, object-closure, conditional, and adversarial contract audit described above passed.

No engine, writer, checkpoint reconstruction, factorial, future feeding probe, or prospective seed was executed.

## OBSERVED

- The exact in-branch and off-branch provenance hashes support the report's numerical and operator claims.
- Four already-open worlds are observational deep-pair candidates, but none is an exact serialized checkpoint;
  50002/50004 additionally have conflicting accepted `ACCESS-STRUCTURE-00` state digests.
- No existing implementation is both member-local and a mechanically interpretable access-edge cut. The global
  ablations, carrier erasure, all-field collar replay, and failed transplant route cannot instantiate `C10/C01`.
- The repaired raw schema is fail closed and carries the strict no-new-history meaning: `eta_w=0`, no external
  writer, zero exposure-dependent increment, and only unchanged hash-bound propagation/decay of pre-existing `Mf`.
- The non-symmetrized estimands and competing hypotheses are coherent but explicitly non-executable.

## INFERRED

`STOP-LOCAL-CUT` is compelled independently of the checkpoint shortfall because the user required that disposition
when no genuinely localized, mechanically interpretable operator exists. It is an operator/checkpoint feasibility
stop and leaves the scientific access question unanswered.

## HYPOTHESIS

A separately authorized term-level local readout cut might make the conditional factorial identifiable, but the
current repository contains no such qualified mechanism and no admissible exact serialized starting checkpoint.

## WHAT WOULD FALSIFY THIS?

An already-existing, provenance-bound operator would need to delete one declared equation edge only on fixed
outcome-independent A/B supports; preserve state, common environment/boundary/probe/body/clock and `eta_w=0`;
compose exactly at A+B; have a disturbance-matched sham; and pass continuous pair, confinement, tracker, fusion,
and viability gates from exact serialized deep checkpoints. No audited candidate meets those conditions.

## Failures and dead ends

- A broad final-package text search intended to locate probe constants accidentally included
  `experiments/individuation/causal_confirmation_raw.json` and rendered fragments of pre-existing single-target
  outcome-bearing fields. The search was stopped; the raw file was excluded from further work. No pair `Y/C/I`,
  contrast, selection, or displayed value was computed or used. The primary report and audit JSON disclose this as
  the second accidental display, alongside the checkpoint auditor's earlier deviation.
- The local worktree does not contain its own `.venv`; JSON syntax used system Python, and optional meta-schema
  packages were unavailable. No package was installed and no environment was changed.

## Decisions

- Final red-team disposition: **PASS — `STOP-LOCAL-CUT` is scientifically and mechanically compelled.**
- The package must not be used to authorize an operator implementation, DEV factorial, prospective namespace, seed,
  or outcome access.
- The exact next action remains human review of the Phase-0 stop package only.

## Unresolved risks

- The source of the 50002/50004 digest disagreement remains unexplained.
- Exact checkpoint bytes, continuous precheckpoint geometry/association evidence, and scalar joint `P/M` checkpoint
  evidence remain absent.
- Any future fixed support risks censoring motion; a dynamic tracker-driven support risks treatment endogeneity and
  diagnostic identity entering physics. This is outside the current authorization.

## Handoff

No remaining package blocker after the integrated repairs. Add this journal to `docs/RUN_INDEX.md`, finalize the
primary integration journal, run final syntax/diff checks, commit the coherent Phase-0 stop package, and push only
`codex/deep-checkpoint-causal-cut-00-phase0`. Report **STOP-LOCAL-CUT**, exact commits, and remote push status.
