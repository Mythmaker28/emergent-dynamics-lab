# CAUSAL-ADDRESSABILITY-ARCHITECTURE-01 Stage A qualification journal

- **Role:** primary conservative-extension implementer and synthetic qualification integrator
- **Run ID:** `RUN-20260718-2020-CAA01-A`
- **Start time:** 2026-07-18 20:20:57 +02:00
- **End time:** 2026-07-18 20:59:56 +02:00
- **Starting branch:** `codex/causal-addressability-architecture-01-stage-a`
- **Starting Git state:** clean at accepted parent `9ec894fbeba7ec2974a917a182d152e9d8074431`
- **Ending Git state:** clean after the final coherent Stage-A qualification commit

## Assigned scope

Implement and qualify only the exact two-port candidate frozen in
`CAUSAL_ADDRESSABILITY_ARCHITECTURE_01_SPEC.json`. Use an isolated diagnostic wrapper; preserve direct frozen-engine
delegation for natural/open execution; limit the local gate to the first frozen update and the existing
`m_plus -> uptake` edge; implement the committed post-`t0` environmental sample-and-hold candidate; freeze the edge
inventory and first E-accessible horizon before fixtures; execute only hand-built deterministic synthetic states;
and return exactly `STAGE_A_QUALIFIED` or `STOP_ARCHITECTURE`. Do not open or prepare Stage B.

## Explicit evidence firewall

Frozen in `CAUSAL_ADDRESSABILITY_ARCHITECTURE_01_STAGE_A_SOURCE_ALLOWLIST.json` before fixture execution. It binds
the accepted durable inputs, frozen engine/dependency hashes, two synthetic style references, and every authorized
new/update path. Forbidden throughout:
`results/`, raw/scientific worlds, DEV outcomes, checkpoints, source-world initialization, scientific analyzers,
seeds, 500xx/570xx/580xx access, prospective namespaces, V5/03G modification, merge, and PR.

## Actions

- Read the repository operating contract and accepted Phase-0 durable state, report, exact spec, allowlist, and
  latest independent journal in the required order.
- Verified the primary checkout is dirty only in pre-existing Windows-incompatible cache paths and did not alter it.
- Created this isolated sparse worktree/branch from the exact accepted parent, excluding only
  `results/_tomo_cache/` from checkout.
- Selected architecture and testing-strategy procedures for explicit proof obligations and deterministic test
  coverage.
- Froze the exact existing-edge inventory, response-specific `H*=2` derivation, four-arm composition, and accounting
  identities before fixture execution.
- Implemented the exact preprobe-reference candidate in an isolated wrapper without modifying the frozen engine.
- Obtained an independent pre-fixture static derivation. It agrees that uptake-specific `H*=2` but complete-state
  `H*=1`, which predicts a binding first-update-invariance failure for the committed E port.
- Committed the complete pre-fixture static checkpoint as
  `62a3b22dce42d6d858eddc3323fe63685c0b051b` before running a fixture.
- Added deterministic heterogeneous interior and periodic-boundary fixtures through an import-isolated loader that
  does not execute the scientific analyzer package initializer.
- Qualified direct delegation, forced-neutral active arithmetic, local-edge confinement, state preservation,
  deterministic clone/order/polarity, dynamic-E slot coverage, operation-derived tolerance, and active-E0
  target-boundary per-face transport accounting.
- Confirmed three independent binding environmental failures and retained the exact committed port without tuning:
  first-returned-state E sensitivity, common external-P suppression through `up_ref`, and artificial one-sided
  transport-rate/per-step boundary contributions.
- Published the machine-readable qualification, Stage-A report, scientific claim table, and durable STOP decision.
- Integrated independent final red-team corrections that limit accounting claims to active-E0 transport and mark
  the complete ordered full-step source/sink/global identity unqualified.

## Important files read or changed

- Read: only the files hash-bound in the Stage-A allowlist.
- Created: Stage-A allowlist, edge inventory, static obligations, isolated wrapper, focused synthetic tests,
  qualification JSON, Stage-A report, and this journal.
- Updated: project state, decision log, experiment index, and run index.
- The independent reviewer created and finalized only its own allowlisted journal.
- A post-fixture allowlist edit corrected only the already-existing reviewer journal filename; it did not widen any
  readable source or evidence scope.

## Reproducible commands and experiments

Before fixtures: JSON parsing, SHA-256 binding, Python AST parsing with bytecode disabled, and `git diff --check`.
The only engine activity was deterministic synthetic qualification through an isolated loader:

`& 'C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe' -m pytest -q -p no:cacheprovider experiments/individuation/test_causal_addressability_stage_a.py`

Final focused result: `15 passed` (`0.21s` on the final primary rerun). The independent reviewer reproduced `15/15`
in `0.22s`. All 11 frozen-code hashes,
both synthetic style-reference hashes, the allowlist hash, and the four bound implementation/static artefact hashes
match. The 11 durable parent inputs matched before implementation; four durable indexes intentionally differ after
recording this Stage-A run.

## OBSERVED

- Phase 0 leaves the local port specified and the environmental port unadmitted.
- `E0` is limited to post-`t0` dynamic environmental innovation maintenance relative to retained references.
- The local and environmental treatment windows differ: one frozen update for `q_L`, through `H*` for E.
- The exact writer/toggle/N ordering makes the returned state E-sensitive during update 1 even though update-1
  target uptake is E-invariant.
- First-update target uptake difference is exactly `0.0`, but first-returned-state maxima are
  `N=1.174760506348349e-4`, `Mf=1.0082892237656821e-5`, `U=3.4383443953123205e-8`,
  `V=2.553650209347058e-8`, and `c=5.1641135812019456e-11`.
- At frozen `H*_uptake=2`, target uptake differs by `3.5574135979822043e-6`.
- An external-only common P pulse changes natural `up_ref` by `1.0592928753778812e-4` but changes selected E0
  `up_ref` by exactly `0.0`, because the exact port holds preprobe `S_B0`.
- Active E0 target-destination transport produces nonzero unpaired pre-`dt` rate ledgers. With synthetic `dt=0.1`,
  per-step contributions are `rho/C=0.009139115625597239`, `U=0.006283909247751598`,
  `V=0.0036028599812039704`, and `Mf=0.0010953608852929408`.
- Aggregate active-E0 transport residual is exactly zero and per-face aggregation residual is at most
  `6.938893903907228e-18`; this accounts for the artificial contribution but does not make the boundary paired.
- Complete ordered full-step source/sink/global closure, matched neutral-E1 per-face records, and complete raw
  stencil parent/reference/sham logs are not qualified.

## INFERRED

The narrow local port is mechanically implementable without erasing state. The committed environmental candidate
is not a valid independent instrument: it violates state-horizon, common-probe, and quasi-autonomy exclusions. Exact
transport accounting exposes those failures; it cannot rescue them.

## HYPOTHESIS

The pre-fixture hypothesis that E is structurally inactive for an unqualified first update is **falsified** for the
complete returned state. It remains true only for the explicitly response-specific first-update target uptake. The
transient local edge deletion remains confined to update 1 as predicted.

## WHAT WOULD FALSIFY THIS?

Any uncovered E-to-response path through `H*`, any natural/open or forced-neutral identity failure, any persistent
state overwrite, any unexplained source/sink/boundary work, any new reporter path, or any artificial autonomous
boundary compels `STOP_ARCHITECTURE`.

## Failures and dead ends

- The isolated worktree does not contain `.venv`; the first AST command failed before Python execution. Static
  validation was rerun successfully with the primary checkout's read-only virtual-environment interpreter and
  `PYTHONDONTWRITEBYTECODE=1`.
- The source allowlist's authorized red-team journal path initially omitted the second `2022` token. The actual
  journal already existed in the static checkpoint; the path-only typo was corrected without widening evidence.
- Complete first-returned-state E invariance fails by exact source order and deterministic fixture.
- The committed `S_B0` convention suppresses a common external-P effect through the existing global writer channel.
- Incoming target-destination substitution creates a one-sided artificial boundary with nonzero `dt`-scaled work.
- Full ordered source/sink/global accounting is not implemented or qualified; only active-E0 transport
  open/active/delta aggregation is closed.

## Decisions

- No alternative environmental construction may replace the committed sample-and-hold candidate during this run.
- No fixture output may be used to select `H*`, a tolerance, an edge inventory, or a preferred scientific sign.
- Final disposition: **STOP_ARCHITECTURE**. The local edge has a narrow code-level mechanical qualification; the E
  port and rank-4 causal-access factorial are not admitted.
- Stage B remains closed. No scientific conclusion, ownership claim, or reinterpretation of prior results is made.

## Unresolved risks

None within the authorized mission. Any lazy-reference, delayed, paired/symmetric, probe-aware, or otherwise
altered environmental construction is a new architecture requiring separate human authorization.

## Handoff

Human review of the Stage-A `STOP_ARCHITECTURE` package is the only next action. Do not prepare or execute Stage B.
