# Agent journal — HISTORY-TRANSPORT-00 DEV audit

- **Role:** independent scientific auditor and randomized-history identification engineer
- **Run ID:** `RUN-20260717-1337-HISTORY-TRANSPORT-00`
- **Start:** 2026-07-17 12:55 +02:00
- **End:** 2026-07-17 13:47 +02:00
- **Starting Git state:** clean isolated branch `claude/history-transport-00-dev` at exact required parent
  `8a690f64e31647e949a8cb74fbf7457f706cd437`
- **Ending Git state:** seven explicit new mission files verified and prepared for one coherent commit on
  `claude/history-transport-00-dev`; no existing file modified, no merge; exact commit/push status reported in the
  final handoff because a commit cannot contain its own hash
- **Scheduled-run lock:** not applicable; direct human-authorized DEV mission
- **Authorization boundary:** already-open seeds `50001-50010` only; audit gate before intervention; no prospective
  family, new seed, feeding probe, claim certification, V5/03G reinterpretation, merge, or active reconstruction

## Assigned scope

Audit the exact existing history intervention and determine whether it supplies a defensible prospectively assigned,
within-original-world `H_A/H_B` contrast. Only if the gate passes, implement and run the core-preserving
coupled/isolated factorial. Otherwise return `STOP-HISTORY-CONTRAST` and do not implement the intervention.

## Important files read

- repository contract chain: `AGENTS.md`, research charter, project state, decision/experiment/run indexes;
- latest agent journal and Phase 0.7 report/results/prereg/journal;
- attached HISTORY-TRANSPORT-00 mission;
- historical history sources: `exp1_prospective.py`, `causal_confirm.py`, `turnover_dev_runner.py`,
  `turnover_engine_03g.py`, `PREREGISTRATION.md`, confirmation addendum/preseal;
- C1c engine equations, turnover diagnostics, no-swap boundary design, and already-open DEV raw/diagnostic files.

## Actions and reproducible commands

1. Created isolated branch/worktree at exact parent. Windows rejected legacy `|` filenames; repaired only the new
   sparse worktree index from the clean Phase-0 worktree, then verified clean HEAD. Main and prior worktrees were not
   modified.
2. Traced the assignment in all four canonical generators and compared it with the historical preregistration.
3. Implemented `history_transport_dev.py` as an **audit-only fail-closed gate**. It contains no feeding/clamp arm.
4. Added six focused tests locking the seed guard, exact continuous draws, missing categorical/Latin-square
   allocation, rejection of retrospective dichotomies, unavailable estimands, and mixed-sign audit-only first stage.
5. Reconstructed post-history S0 and same-time no-drive states for every assigned DEV target. No nutrient
   standardization or future-feeding probe was run.
6. Generated `HISTORY_TRANSPORT_00_DEV_RESULTS.json` and the report, blocked prereg draft, and hypothesis table.

Commands:

```powershell
$py = 'C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe'
& $py -m pytest experiments/individuation/test_history_transport_dev.py -q
& $py -m experiments.individuation.history_transport_dev `
  --output docs/individuation/HISTORY_TRANSPORT_00_DEV_RESULTS.json `
  --seeds 50001 50002 50003 50004 50005 50006 50007 50008 50009 50010
```

Final validation:

- focused gate tests: `6 passed`;
- focused plus ACCESS/no-swap/Phase-0.7/03G/engine regressions: `41 passed in 60.26s`;
- `py_compile`: pass;
- machine-result schema/seed/count/estimand/exclusion assertions: pass;
- deterministic post-exit result SHA-256:
  `2dfc6b66f6a9be3ce2082f9ce1a37d229291faed459b292d594d6cf5a20b63e9`.

## OBSERVED

- The executable assignment is six sequential continuous uniform draws per eligible world, not categorical A/B.
- Continuous assignment occurs before future feeding and exactly reproduces from the seed.
- The historical preregistration promises a balanced Latin square, but no generator implements one.
- Eight worlds are initially eligible; four reach deep turnover; 50008/50010 are geometry-ineligible.
- A midpoint-dose split gives both labels in only 7/8 eligible worlds; an order-sign split also gives both in 7/8,
  and deep-valid 50005 is 3/0 on order sign.
- All 24 assigned targets change at least one of `rho/U/V/c/N` before the probe relative to the same-seed no-drive
  world. Body/geometry differences are material.
- The rejected midpoint split's deep `m_plus` A-B values are `+0.0346,-0.0313,-0.0150,-0.0949`.
- No future-feeding field was accessed for assignment, relabelling audit, or first-stage diagnostics.

## INFERRED

- No requested history-factor estimand exists in these worlds. Outcome-blind relabelling is still retrospective and
  does not repair categorical randomization.
- The nutrient intervention is not memory-specific; even a future assigned contrast needs a prospective `H_0`
  body/geometry/nutrient strategy.
- Reference-free history transport remains a worthwhile new question, but it requires a genuinely new protocol.

## HYPOTHESIS

A prospectively assigned local-history contrast might survive deep turnover under common standardized inputs even
though absolute zero-memory references are arbitrary. The existing worlds cannot test this hypothesis because they
lack the assignment and first stage that define it.

## WHAT WOULD FALSIFY THIS?

A pre-data implementation record showing actual categorical H labels, a frozen randomization/permutation schedule,
both conditions in every original world, and an implemented spatial blocking rule would falsify the assignment-gate
finding. None exists in the audited generators or raw records. A new future protocol could create such evidence but
would not retroactively change this audit.

## Failures and dead ends

- Initial sparse checkout hit known Windows-invalid legacy filenames containing `|`; no historical file was removed.
  The new worktree was recovered using the clean sparse index and explicit parent additions.
- The first exact-history unit-test fixture transposed two final draw values; the test failed, the expected constants
  were corrected to the raw/code values, and the rerun passed. Scientific code/results were not involved.
- The first checksum probe raced a background Windows process that had not fully exited. The generator was rerun in
  one blocking process and the report was bound only to the post-exit SHA-256
  `2dfc6b66f6a9be3ce2082f9ce1a37d229291faed459b292d594d6cf5a20b63e9`.
- Retrospective dose midpoint, order sign, within-world extremes, and maximum-distance pair rules were examined only
  as audit counterexamples and rejected before feeding.

## Decisions

- Return `STOP-HISTORY-CONTRAST`, entailing `STOP-HISTORY-TRANSPORT` for this mission.
- Do not implement or execute coupled/isolated, boundary-reference, `up_ref`, or `lam_plus` outcome arms.
- Report every causal estimand as unavailable, not zero.
- Preserve all ACCESS Phase 0-0.7 and 03G/V5 artefacts unchanged.
- Do not update existing indexes because the mission explicitly requires committing only explicit new files; this
  journal and new deliverables are the durable non-overwriting record.

## Unresolved risks

- A future binary nutrient-dose contrast may still change body and nutrient state along with memory.
- Only one common, on-manifold, history-independent two-cell boundary reference is currently qualified.
- The same numeric seed is reused for physics and history draws; a future randomized assignment needs an independent
  namespace.
- No prospective margins, valid-world minimum, family, decoder, or power route can be chosen before a valid DEV first
  stage.

## Handoff

Stop after DEV and request human review. The exact next decision is whether to accept the STOP or authorize a
conceptually new, pre-data randomized-history protocol. This mission does not authorize that design or any
prospective seed.
