# Agent journal — M_MINUS-ORDER-READER-00 DEV

- Role: independent mechanistic reader auditor and DEV intervention engineer
- Run ID: `RUN-20260717-2158-M_MINUS-ORDER-READER-00`
- Start: 2026-07-17 21:58:43 +02:00
- End: 2026-07-17 22:33:50 +02:00
- Starting Git state: clean isolated branch `claude/mminus-order-reader-00-dev` at exact accepted parent `ea6e6a0ab2ccc3e94eba364ddb459088c96d6033`
- Ending Git state: clean dedicated branch at the final result commit after explicit staging and push; before-output freeze commit `8349c55`; primary checkout unchanged
- Scheduled-run lock: not applicable; direct human-authorized bounded DEV mission
- Assigned scope: code-first derivation and, only if a valid reader exists and is frozen, one deterministic reader evaluation on already-open source worlds 57001–57024; no new seed namespace, prospective execution, family extension, V5/03G change, main merge, or active reconstruction.

## Files and evidence read

- Repository operating contract and durable state chain through the accepted `COUNTERFACTUAL-HISTORY-CORE-00` result.
- Frozen parent manifest, report, latest journal, result commit, and attached `M_MINUS-ORDER-READER-00` mission.
- Statistical-analysis skill instructions.

## Actions

1. Preserved the massively dirty primary checkout unchanged.
2. Created a no-checkout isolated worktree and the explicitly requested branch from the exact accepted parent; materialized only sparse code/document paths and verified index-tree equality with HEAD.
3. Completed Phase A code-only audit. `lam_minus` occurs only in the local attractant-production source. Movement, flux and morphology are downstream through `c` gradients and saturation, so their signs are not identifiable from `m_minus` alone.
4. Selected exactly one primary reader: the one-step radius-10-core integrated attractant-production source. Froze a symmetric fractional gain probe using the pre-existing `lam_minus={0.05,0.15,0.25}` grid, hence `epsilon=2/3`, with one-step duration/horizon and per-core-mass robustness normalization.
5. Froze the protocol and sign derivation before any 570xx reader output. Protocol SHA-256: `d0096f028b9d9e35d6cabb54b73c0e47ca8ee512efb8b0702c8ab8de86cf2ead`.
6. Implemented the bounded complete-world runner, deterministic same-manifest/deep-hash reconstruction, two-cell clamp, `up_ref=0`, `lam_minus=0` ablation, exact shams, atomic shards, fixed original-world aggregation, and independent raw-only reproducer.
7. Added synthetic code-only gates for sign derivation, symmetric source and `c` reversal, non-`c` identity, probe-disabled identity with and without collar clamp, ablated-arm identity, frozen coefficients, factorial arithmetic, raw-only classification, and seed refusal. No 570xx reader output has been generated or inspected.
8. Committed and pushed the complete protocol/code/test freeze at `8349c55` before producing any 570xx reader output.
9. Deterministically reconstructed only the 17 already-open parent-complete source worlds, using four processes and atomic per-world shards. No parent-incomplete world, new namespace or prospective seed was run.
10. Merged the complete fixed shard set once under the frozen classifier. Result: `ORDER_READER_CANDIDATE`.
11. Ran the independent raw-only reproducer. It matched every per-world contrast, summary and classification without importing the runner or initializing the engine.
12. Wrote the bounded DEV report and GO/NO-GO disposition; updated project, experiment, run and decision indexes without changing 03G, V5 or any parent artefact.
13. Ran the final reader, parent exact-clone and qualified-clamp regression set: 35 passed in 63.74 seconds. Repeated the raw-only reproduction and obtained a byte-identical artefact.
14. Moved the disposable atomic-shard working directory to `C:\Users\tommy\AppData\Local\Temp\codex-mminus-reader-work-RUN-20260717-2158`; consolidated results remain in the worktree.

## Reproducible commands

```powershell
$py='C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe'
& $py -m py_compile experiments\individuation\mminus_order_reader_dev.py `
  experiments\individuation\mminus_order_reader_reproduce.py `
  experiments\individuation\test_mminus_order_reader_dev.py
& $py -m pytest experiments\individuation\test_mminus_order_reader_dev.py -q
# 11 passed in 1.44 s
& $py -m experiments.individuation.mminus_order_reader_dev
# phase_a=VALID_READER; new_reader_output_generated=false
& $py -m experiments.individuation.mminus_order_reader_dev --run-all --jobs 4 `
  --shard-dir .mminus_reader_work/shards
& $py -m experiments.individuation.mminus_order_reader_dev --merge `
  --shard-dir .mminus_reader_work/shards `
  --output docs/individuation/M_MINUS_ORDER_READER_00_DEV_RESULTS.json
# ORDER_READER_CANDIDATE
& $py -m experiments.individuation.mminus_order_reader_reproduce `
  --raw docs/individuation/M_MINUS_ORDER_READER_00_DEV_RESULTS.json `
  --output docs/individuation/M_MINUS_ORDER_READER_00_RAW_REPRODUCTION.json
# ORDER_READER_CANDIDATE
& $py -m pytest experiments/individuation/test_mminus_order_reader_dev.py `
  experiments/individuation/test_counterfactual_history_core_dev.py `
  experiments/individuation/test_access_structure_noswap_operators.py -q
# 35 passed in 63.74 s
```

## OBSERVED

- Parent verdict remains `NO_MEMORY_FIRST_STAGE — STOP THIS PREREGISTRATION CANDIDATE`.
- The parent family contains 24 already-open original worlds and 17 complete four-history blocks.
- Exact engine path: `m_minus=tanh(newm1-newm2)` enters only `s*rho0*(1+lam_minus*m_minus)` in the `c` update. `c` then influences next-step face flux, transport, centroid and morphology.
- The reduced two-timescale recurrence predicts positive, not negative, EARLY-minus-LATE `m1-m2`: `Delta m1=-4.6377041581e-06`, `Delta m2=-2.9102795171e-05`, difference `+2.4465091013e-05`.
- The committed parent raw file is LF-normalized by Git; the runner binds both its exact Git-worktree SHA-256 `12a47621...` and the reported persisted CRLF SHA-256 `d4e6f2d9...` by deterministic newline reconstruction. Parsed content and parent membership remain unchanged.
- The structured deep `m_minus` order contrast is positive in 17/17 worlds: mean `0.003667894`, 95% t interval `[0.003063548,0.004272241]`.
- Raw isolated order susceptibility is positive in 17/17 worlds: mean `0.0003129292`, interval `[0.0002474090,0.0003784494]`. Low and high dose are each 17/17 positive.
- The frozen mass-normalized order endpoint is also positive in 17/17 worlds: mean `8.989063e-06`, interval `[7.884663e-06,1.009346e-05]`.
- Raw dose susceptibility is negative in 17/17 worlds; its normalized interval crosses zero. The raw and normalized dose-by-order interaction intervals cross zero.
- `lam_minus=0` gives an exact zero order susceptibility in all worlds and 100% attenuation. All manipulation and deterministic gates pass.
- Consolidated result SHA-256: `ebe6043d91aed3fefada489602e84f5affd43f0dd67873bb2fbff11bc4aff7f7`. Raw-only reproduction SHA-256: `61c51a7baf5fcf082e8ce764f3db1275a90e10c89e5a8a77821efcd2b490da5b`.

## INFERRED

- A response can be called a reader only if the engine contains a specific causal path from `m_minus` through `lam_minus` to the selected observable and the perturbation interrogates that path without history-specific boundary input.
- Local attractant production satisfies that requirement and has an algebraically fixed susceptibility; later movement endpoints do not.
- The previous negative sign was an algebraic orientation error under the unchanged EARLY-minus-LATE convention. This correction does not reinterpret the failed parent first stage as passed.
- The positive source susceptibility is a valid direct `m_minus/lam_minus` reader result under the frozen definition. Its exact ablation is partly a constitutive identity of the source equation and is not independent evidence of downstream movement or feeding.

## HYPOTHESIS

- A symmetric, externally imposed directional perturbation may expose a local susceptibility downstream of the `m_minus/lam_minus` term even though the uniform feeding endpoint has no order effect.

## WHAT WOULD FALSIFY THIS?

- No directly measurable term downstream of `m_minus` exists;
- the code-only sign is not identifiable without outcome selection;
- the frozen reader has no interpretable order susceptibility;
- or the susceptibility persists under `lam_minus=0`.

## Failures and dead ends

- The primary checkout was unsuitable because it was heavily dirty. A separate no-checkout sparse worktree was used; no user changes were overwritten.
- Windows could not materialize the entire historical tree because some Git paths are invalid on NTFS. Selective sparse materialization preserved exact parent index/tree identity for the required paths.
- PowerShell's case-insensitive `ConvertFrom-Json` rejected nested keys `C` and `c`; read-only Python JSON parsing was used for inspection. The experiment and raw-only reproduction themselves were unaffected.

## Decisions, risks, and handoff

- Phase-A disposition: `VALID_READER`; theory result: `SIGN_THEORY_CORRECTED`.
- Frozen primary reader: one-step fixed-core attractant-production source; `epsilon=2/3`; effective local coefficients `0.05/0.15/0.25`; 40-step standardized isolated settle; one-step probe; per-core-mass secondary robustness control.
- Final DEV classification: `ORDER_READER_CANDIDATE`; exact permitted claim: DEV evidence that temporal order changes a local `m_minus`-linked attractant-production response operator under standardized inputs.
- Parent `NO_MEMORY_FIRST_STAGE — STOP THIS PREREGISTRATION CANDIDATE` remains unchanged. No feeding-order effect, ownership, individual memory, identity, heredity, prospective confirmation or active reconstruction is established.
- Next authorized action: human review of whether this direct constitutive source assay is a prospectively defensible reader. No prospective seed, protocol seal, ownership pair or generic reader battery is authorized automatically.
