# Agent journal — ACCESS-STRUCTURE-00 Phase 0.6A

- **Role:** boundary-safe causal-operator engineer and independent scientific auditor
- **Run ID:** `RUN-20260717-0405-ACCESS-STRUCTURE-00-P06A`
- **Start:** 2026-07-17 04:05:32 +02:00
- **End:** pending
- **Starting Git state:** clean dedicated branch `codex/access-structure-boundary-repair-06` at exact required parent `fa261734300631f16ca5e0bacceba11d5f7ddc1e`
- **Ending Git state:** pending
- **Scheduled-run lock:** not applicable; direct human-approved DEV-only Phase 0.6A work
- **Authorization boundary:** seeds `50001-50010` only; no prospective namespace, active crossed feeding, storage inference, 03G/V4.1/V5/certified-result modification, merge, or active reconstruction

## Assigned scope

Reproduce and localize the Phase 0.5 seam/balance failure; freeze at most two serious operator families; implement
individual-arm physical balance, phase audit, payload-retention guards, minimum technical shams, and complete
DEV-only qualification; return TECHNICAL GO, REVISE, or STOP-TRANSPLANT; commit and push only this dedicated branch.

## Starting verification and important files read

- Printed root `C:/Users/tommy/Documents/ising-v3-access-structure-boundary-06`, branch
  `codex/access-structure-boundary-repair-06`, and HEAD `fa261734300631f16ca5e0bacceba11d5f7ddc1e`.
- Verified required-parent ancestry and clean worktree. The linked worktree initially inherited stale Windows index
  stat records from the safe sparse checkout; every apparent path hashed exactly to its HEAD blob, both worktree
  and cached diffs were empty, and explicit index refresh restored a clean status without content changes.
- Read `AGENTS.md`, `docs/RESEARCH_CHARTER.md`, durable state/index/decision files, the Phase 0.5 journal, Phase 0
  and Phase 0.5 reports, the Phase 0.5 machine summary, the exact mission, operator/test/qualification code, and
  the frozen engine/update implementation.

## Actions and reproducible commands

- Froze exactly two families/four configurations in
  `docs/individuation/ACCESS_STRUCTURE_00_PHASE06A_OPERATOR_SPEC.md` before DEV execution.
- Implemented local recipient-shaped total projection, base-cohort balancing, exact recipient outer interface,
  protected intensive-memory payload, phase refusal, and outcome-independent correction rules.
- Added a qualification runner that hard-refuses anything except the complete already-open DEV namespace, reruns
  Phase 0.5, audits `0+,1,5,10,20,40`, and never calls an active-arm feeding readout.
- Initial focused validation: `python -m pytest experiments\individuation\test_access_structure_operators.py experiments\individuation\test_access_structure_boundary_safe.py -q` -> `23 passed`.

## OBSERVED

Pending DEV execution.

## INFERRED

Pending DEV execution.

## HYPOTHESIS

A recipient-preserved outer interface plus a local constrained balance projection may reduce the hard-cut shock,
but a wide enough recipient collar may remove the donor body or the history contrast. Passing only one side of that
tradeoff is not a qualified operator.

## WHAT WOULD FALSIFY THIS?

One predeclared configuration that passes balance, all-interface seam, propagation, phase, tracking, and payload
gates together would falsify the anticipated incompatibility. Conversely, failure of all four configurations on
unchanged gates supports stopping the transplant route rather than scanning further variants.

## Failures and dead ends

- The first intensive-memory unit assertion included empty cells where balancing legitimately drove `rho` to zero;
  extensive `Mf` is then zero and intensive memory is undefined. The test was corrected to the declared occupied
  payload support. It then passed at `1.11e-16`; no operator rule or threshold changed.

## Decisions

- Preserve the Phase 0.5 `1.25x` seam envelope and report wider sensitivity; do not calibrate it to failures.
- Do not balance `Mf` totals: memory is the payload and is not a conserved physical quantity.
- Do not invent an engine energy invariant; report fixed `c/N` bulk and gradient proxies explicitly as diagnostics.
- Use no more than the frozen four configurations. No additional radius or taper will be introduced after execution.

## Unresolved risks and handoff

Pending DEV results, inherited regressions, final report, indexes, commits, and push.
