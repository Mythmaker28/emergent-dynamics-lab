# Agent journal — ACCESS-STRUCTURE-00 Phase 0.6A

- **Role:** boundary-safe causal-operator engineer and independent scientific auditor
- **Run ID:** `RUN-20260717-0405-ACCESS-STRUCTURE-00-P06A`
- **Start:** 2026-07-17 04:05:32 +02:00
- **End:** 2026-07-17 04:21:33 +02:00
- **Starting Git state:** clean dedicated branch `codex/access-structure-boundary-repair-06` at exact required parent `fa261734300631f16ca5e0bacceba11d5f7ddc1e`
- **Ending Git state:** Phase 0.6A report/results/durable-state update prepared for the final coherent commit on the dedicated branch; checkpoint commit `59b4dda1a5b74a14f9a268391d73ea0788181368` already pushed
- **Scheduled-run lock:** not applicable; direct human-approved DEV-only Phase 0.6A work
- **Authorization boundary:** seeds `50001-50010` only; no prospective namespace, active crossed feeding, storage inference, 03G/V4.1/V5/certified-result modification, merge, or active reconstruction

## Assigned scope

Reproduce and localize the Phase 0.5 seam/balance failure; freeze at most two serious operator families; implement
individual-arm physical balance, phase audit, payload-retention guards, minimum technical shams, and complete
DEV-only qualification; return TECHNICAL GO, REVISE, or STOP-TRANSPLANT; commit and push only this dedicated branch.

## Starting verification and important files read

- Printed root `C:/Users/tommy/Documents/ising-v3-access-structure-boundary-06`, branch
  `codex/access-structure-boundary-repair-06`, and HEAD `fa261734300631f16ca5e0bacceba11d5f7ddc1e`.
- Verified required-parent ancestry and clean worktree. The linked worktree initially inherited stale Windows index
  stat records from the safe sparse checkout; every apparent path hashed exactly to its HEAD blob, both worktree
  and cached diffs were empty, and explicit index refresh restored a clean status without content changes.
- Read `AGENTS.md`, `docs/RESEARCH_CHARTER.md`, durable state/index/decision files, the Phase 0.5 journal, Phase 0
  and Phase 0.5 reports, the Phase 0.5 machine summary, the exact mission, operator/test/qualification code, and
  the frozen engine/update implementation.

## Actions and reproducible commands

- Froze exactly two families/four configurations in
  `docs/individuation/ACCESS_STRUCTURE_00_PHASE06A_OPERATOR_SPEC.md` before DEV execution.
- Implemented local recipient-shaped total projection, base-cohort balancing, exact recipient outer interface,
  protected intensive-memory payload, phase refusal, and outcome-independent correction rules.
- Added a qualification runner that hard-refuses anything except the complete already-open DEV namespace, reruns
  Phase 0.5, audits `0+,1,5,10,20,40`, and never calls an active-arm feeding readout.
- Initial focused validation: `python -m pytest experiments\individuation\test_access_structure_operators.py experiments\individuation\test_access_structure_boundary_safe.py -q` -> `23 passed`.
- Committed and pushed the frozen implementation/specification checkpoint:
  `59b4dda1a5b74a14f9a268391d73ea0788181368`.
- Executed the complete qualification:

  `C:\Users\tommy\Documents\ising v3\.venv\Scripts\python.exe -m experiments.individuation.access_structure_boundary_safe_qualification --output docs\individuation\ACCESS_STRUCTURE_00_PHASE06A_RESULTS.json --seeds 50001 50002 50003 50004 50005 50006 50007 50008 50009 50010`

- Machine-result SHA-256:
  `8A3260EE48E0451C988E03F527D5FB507A1B684A345147A43569E38F5AE3A7BB`.
- Final validation:
  - 03G regression: `7 passed`;
  - Phase 0.5 plus new focused operators: `23 passed`;
  - bijective tracker: `10/10 PASS`;
  - tracer/periodicity: `6/6 PASS`;
  - machine namespace/schema/96-arm/no-feeding assertions: PASS;
  - `git diff --check`: pass apart from Windows line-ending notices.

## OBSERVED

- The exact Phase 0.5 maximum seam `22.872170460755093x`, 4/168 `1.25x` passes, exact-control zeros, viability,
  and individual-arm imbalance reproduced.
- The hard maximum is `rho` in seed 50002 `C_B_E_A`; physical face-flux RMS changes are 79.05x and 86.74x
  recipient-natural values. No missing persistent buffer exists.
- Every new candidate has 24 constructed arms and independently matches `rho,U,V,c,N` totals inside the float64
  criterion. Active-operation shams are exact. Base cohorts preserve `sum(C)=rho` to `2.55e-15`.
- Only `RIP_HARD_R8` remains viable/trackable in 24/24 arms. It passes the payload guard (`L` projection minimum
  0.679, memory projection minimum 1.0, body full-weight coverage minimum 0.949) and makes the original outer seam
  ratio 1.0.
- That candidate fails at its moved internal boundary: worst ratio `1.76e11` from singular `u/v`; raw
  `rho,c,N,m_plus` ratios remain 208.8, 9.56, 9.96, and 7.63. Its far step-40 RMS falls from the hard-cut 0.04425
  to 0.00112 while surgery-time core RMS rises to `2.55e11`.
- `RIP_HARD_R9` loses three arms. `CPP_QUINTIC_R8` and `CPP_QUINTIC_R7` each lose two; R7 also misses the 0.90
  body-coverage guard. Every configuration fails seams at every reasonable sensitivity conclusion.
- No active crossed future-feeding result, storage outcome, new seed, or prospective family exists.

## INFERRED

- The Phase 0.5 failure is a genuine mismatch in body geometry, physical fields, gradients and flux, not a
  coordinate or scheduler-phase defect.
- Matching scalar global totals independently is not an on-manifold body-state match. Active-set clipping can make
  `rho` zero while `U/V` remain positive, manufacturing extreme concentrations.
- A recipient collar reduces outward propagation by relocating rather than removing the discontinuity. Wider
  transitions trade payload/body coverage and viability for partial seam reduction without approaching the joint
  gate.
- The tested transplant route cannot identify local/environmental access; all storage-location hypotheses remain
  unresolved.

## HYPOTHESIS

A recipient-preserved outer interface plus a local constrained balance projection may reduce the hard-cut shock,
but a wide enough recipient collar may remove the donor body or the history contrast. Passing only one side of that
tradeoff is not a qualified operator.

## WHAT WOULD FALSIFY THIS?

One predeclared configuration that passes balance, all-interface seam, propagation, phase, tracking, and payload
gates together would falsify the anticipated incompatibility. Conversely, failure of all four configurations on
unchanged gates supports stopping the transplant route rather than scanning further variants.

## Failures and dead ends

- The first intensive-memory unit assertion included empty cells where balancing legitimately drove `rho` to zero;
  extensive `Mf` is then zero and intensive memory is undefined. The test was corrected to the declared occupied
  payload support. It then passed at `1.11e-16`; no operator rule or threshold changed.
- The first complete DEV execution revealed that independent non-negative projection of `rho/U/V` violates their
  intensive manifold. This is preserved as a scientific/engineering failure; the operator and grid were not
  patched or rerun after results.
- `CPP` donor weight reaches zero at continuous radius 10 but is nonzero on some discrete stencil-boundary cells,
  so its outer seam also fails. This discretization defect is reported, not post-result eroded or retuned.

## Decisions

- Preserve the Phase 0.5 `1.25x` seam envelope and report wider sensitivity; do not calibrate it to failures.
- Do not balance `Mf` totals: memory is the payload and is not a conserved physical quantity.
- Do not invent an engine energy invariant; report fixed `c/N` bulk and gradient proxies explicitly as diagnostics.
- Use no more than the frozen four configurations. No additional radius or taper will be introduced after execution.
- Record D-091 and **STOP-TRANSPLANT**. Do not reinterpret the stop as absence of a storage/access mode.

## Unresolved risks and handoff

- Whether a non-surgical, equation-level intervention can identify access is a fresh scientific/design question.
- `STOP-TRANSPLANT` is scoped to the tested spatial-graft route; it is not a proof that every conceivable
  counterfactual construction is impossible.

**Handoff:** Human review of the Phase 0.6A report. Do not seal the existing preregistration, open prospective
seeds, scan more graft variants, modify 03G/V5, merge, or begin active reconstruction. A future attempt requires a
fresh, conceptually different authorization.
