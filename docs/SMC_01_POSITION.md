# SMC-01 — Independent Scientific Position (before selection)

## What I accept from Phase C
Two history coordinates decode prospectively from C1c memory (h1,h2), shuffle-controlled, viable. Storage is
~2-D (σ₂/σ₁ 0.02→0.23 dev; 0.283 prospective, marginally under the 0.30 line). h2's causal expression failed
under mean-transplant (−0.04) and was weak in-place (0.30).

## My prior on the six hypotheses (SPECULATIVE)
- H4 (lab-frame artefact): **likely LOW risk** — the Phase C decoder used permutation-invariant statistics,
  which cannot read absolute grid position. I will still test translation/rotation/reflection explicitly, and
  compare a genuinely geometric raster vs a frame-free descriptor vs a lab-frame raster.
- H2 (material residue) / turnover: **the key open risk.** h2 sits substantially in value-dispersion of the
  memory; if that dispersion rides on old constituents / droplet growth, it will collapse with turnover. This
  is the load-bearing test.
- H5 (measurement bottleneck) vs H6 (stored but causally silent): Phase C already showed global-mean readout
  is ~1-D; the honest question is whether *any* preregistered spatial response expresses h2 (H5) or not (H6).
- H3 (environmental residue): the interrogation already erases into a common body for transplant, but the
  in-place decode could ride residual N/c fields; the reset audit decides.

## Most likely outcome (stated so it can be falsified)
I expect: storage replicates (P1), frame-independence holds (P2) because the decoder is already frame-free,
but **turnover survival (P4) and/or structured causal sufficiency (P5) fail** — i.e. Outcome B
(material-bound) or Outcome D (stored but causally silent). If structured transplant unexpectedly expresses
h2 (P5) while global means do not, that is Outcome E (spatial causal readout qualified) — the one result that
would justify continuing. I will not soften the verdict to reach it.

## Falsifier for the direction
If h2 is frame/material/environment-bound or causally silent even under structured transplant, "multi-
dimensional causal organizational memory" is not supported on this substrate by local write + existing
readouts, and the escalation toward reproduction/genome should halt.
