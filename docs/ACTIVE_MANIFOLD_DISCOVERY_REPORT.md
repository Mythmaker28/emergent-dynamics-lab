# ACTIVE MANIFOLD DISCOVERY

The observer discovers which source histories are **reachable** under the declared admissible repertoire, and
selects interventions specifically to close the gaps. Every row of every inferred law retains a pointer to the
episode, the timestamps, the source histories and the output that produced it — and the assertion validates the
**pair**, not merely the output.

## Coverage governs the claim

Full Cartesian product covered → a full table, `IDENTIFIED`.
Partial coverage → a **partial** function and `EQUIVALENCE_CLASS_ONLY`. Never an extrapolation.

## The off-manifold case, measured

`lag8_and` / `lag8_or` (development) and `lag8_dm` (prospective, unseen): two taps of **one** source separated by
exactly one clock period. Under every sustained regime — free running *and* constant clamps — they carry the same
value. Half the manifold **cannot be generated**.

| | coverage | verdict |
|---|---|---|
| development `lag8_and` vs `lag8_or` | 4/8 = **0.5** | `EQUIVALENCE_CLASS_ONLY`; the two have **identical observed tables** and are **not separated** |
| prospective `lag8_dm` (unseen), all 3 channels | **0.5** | `EQUIVALENCE_CLASS_ONLY` |

The observer reports the rows it has, refuses the rows it cannot generate, and does **not** round the coverage up.

## And the mirror image: no false abstention

`lag15_or` / `lag15_xor` emit **byte-identical free-running outputs**. Passive observation cannot separate them at
any length. Clamping the *source* reaches the separating row, coverage is complete, and the observer reports
**DIFFERENT**. Refusing to look would have been false abstention — the mirror image of false certainty, and
exactly as dishonest.

## Provenance

**Prospective: 0 fabricated rows, 0 excluded rows, at lags up to 56, on every channel.**
