# H2-CERT-01 — Independent Position (before power analysis)

This is a certification, not development. The frozen physics and all prior evidence point to a specific
prior: deep-turnover held-out h2 is ~0.43–0.50 (DMM prospective 0.48 at M≈0.37; dev 0.67 CI[-0.21,0.89]).
The certification requires the prospective lower CI bound to EXCEED 0.50 at M≤0.25. Because the point
estimate itself is at/below 0.50 on held-out data, I expect certification to FAIL regardless of power — you
cannot push a lower bound above 0.50 when the central value is ≤0.50. The power analysis must state this
honestly: if the true deep-turnover R² is ≤0.50, no sample size certifies; adequate power then means enough
samples to show the lower bound does NOT exceed 0.50 with confidence (i.e., to CLOSE, not to keep chasing).
I will run an adequately-powered sealed test to confirm the point estimate and its CI at M≤0.25, then apply
the decision matrix. The designed kill-switch outcome (close unless every gate passes) is the likely result.
