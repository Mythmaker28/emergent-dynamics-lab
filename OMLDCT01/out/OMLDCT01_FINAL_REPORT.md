# OMLDCT01 — FINAL REPORT

```
OMLDCT01_FINAL_DISPOSITION = OMLDCT01_TECHNICALLY_INVALID
```

That is the exact string. No softer one was invented, and none is available.

## What the mission was

One frozen matched-control experiment, issued by LDFMA01 at tip
`2101b301a2444a4a825a6cd338a8db7334c53c9f`. Fork each admissible world at the frozen trigger step
`t_m` into a `SELECTIVE_PARENT_REMOVAL` arm and a bit-exact `SHAM_NO_REMOVAL` arm, and compare the
locked daughter's post-intervention identity duration and particle-step exposure across the pair.

## What it produced

| | |
|---|---|
| confirmatory paired blocks | **0** |
| primary result | **NOT_REACHED** |
| endpoint values retained for confirmation | **none** |
| new scientific worlds entering any record | **0** |

## Why it ended

Four full-scale trajectories on frozen candidate seeds preceded the C2 master freeze, and the four
seeds were then retired on the strength of their own outcomes. The owner adjudicated them as
scientific-scale pilots. Section 5 makes that campaign-level technical invalidity. The full account
is in `OMLDCT01_PRE_C2_INCIDENT.md`.

## What the mission did establish, and what it did not

It stopped at its own first gate rather than inventing a missing decision rule: the committed
handoff said the paired test runs *"on both the primary and the co-primary"* without saying what
happens when they disagree, and the launcher forbade choosing. That STOP was correct, and the five
missing rules came back from the owner.

It built and self-tested the exact Pratt conditional sign-flip distribution — no normal
approximation, no scipy. It wrote and qualified a second, independent E3 classifier that agreed
with the first on all 22 developmental LAW_C removal worlds and refused the same 4 negative
controls. It found and declared its own sequencing violation, and then found and declared that two
of its own digests were unreproducible.

It established **nothing scientific**. Not one endpoint value was computed.

```
H3_STATUS                    = NOT_TESTED
REPRODUCTION_STATUS          = NOT_TESTED
HEREDITY_STATUS              = NOT_TESTED
AUTONOMOUS_COHESION_STATUS   = NOT_ESTABLISHED
X_LAWSPEC_BASELINE           = UNCHANGED
ARCHITECTURE_CHANGE_NECESSITY = NOT_ESTABLISHED
COMPANION_PAPER_V1_1_STATUS  = UNPUBLISHED__NOT_SUBMITTED__PUBLICATION_DEFERRED
```

## What carries forward

Reusable **implementation evidence only** — the owner's five-line decision addendum, the exact
Pratt implementation and its ten self-test cases, the second E3 classifier, the fixtures, the
locked-daughter reconstruction qualification, and the Windows durability chain. Each must be
**rebound** in OMLDCT02's own C1, not inherited by reference.

Explicitly **not** authoritative for OMLDCT02: the master freeze, the seed manifest and the methods
closure. The freeze postdates scientific-scale execution and the seed list is contaminated by
outcome-informed retirement.

## Successor

`OMLDCT02 — ONE-MATCHED-LOCKED-DAUGHTER-CONTROL-TEST-02`, authorised **once**, with entirely fresh
seeds, committed hash generators and a prospective freeze. The scientific design does not move: no
endpoint, parameter, law, alpha, direction, combination rule, zero treatment, minimum pair count or
resource ceiling changes. A second clean restart is forbidden.

Intentional commits used by OMLDCT01: **5 of 5**.
