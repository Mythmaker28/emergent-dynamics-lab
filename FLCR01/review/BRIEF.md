# FLCR01 — INDEPENDENT ADVERSARIAL REVIEW (the single authorized review)

You are an **independent adversarial reviewer**. Refute, do not praise. Uncertainty is not
evidence. Never write the bare word `REFUTED`; use `DEFECT_CONFIRMED`, `DEFECT_PLAUSIBLE`,
`ATTACK_REFUTED` (= the attack failed, the claim stands).

## HARD CONSTRAINTS
- **Read-only** on the candidate. Write ONLY under `/home/claude/FLCR01/review/`.
- **Zero new scientific runs.** Never construct or step a World. Reading `.npz`, reading source,
  reading Git objects and exact arithmetic are all fine. `NEW_SCIENTIFIC_RUNS_BY_REVIEW` must be 0.
- No `git push`/`commit`/`checkout`/`reset`.

## CANDIDATE
```
repo    /home/claude/edl
branch  codex/founder-versus-lineage-continuity-reconciliation-01
C1  e302cca12a3964ae8fcb6e64f9dbb02a33d8e297   PQEC01 correction addendum
C2  58c2c03532e77f0933e8786e31e20a73d5f7738c   criterion audit, state operator, regions
code    /home/claude/edl/FLCR01/code/{flcr01_bind,flcr01_correct,flcr01_science,flcr01_final}.py
out     /home/claude/edl/FLCR01/out/
raw     /home/claude/PQEC01/raw/*.npz  (128 archives)
parent  /home/claude/edl/PQEC01/  (do not re-review PQEC01 itself; review THIS correction of it)
```

## DISPOSITION UNDER ATTACK
`LINEAGE_CRITERION_SUPPORTED__OPERATOR_NOT_IDENTIFIED_FROM_PQEC01`, with
`ARCHITECTURE_CHANGE_NECESSITY = NOT_ESTABLISHED` and a non-empty lineage region of 563 grid
points, `kY ∈ [1.58e-05, 5.62e-04]`, `muY ∈ [1.00e-08, 1.19e-03]`.

## THE EIGHT ATTACKS — run every one, no others
**A1 — was founder survival discarded for scientific or for outcome-driven reasons?** The
candidate claims its rejection stands independently of any region. Test that: would the argument
survive if founder survival admitted a wide region? Look for any place the choice is justified by
the region it produces. Check `FLCR01_CRITERION_MATRIX.json` and the audit markdown.

**A2 — is lineage continuity operationally measurable without invented genealogy?** The states are
E/O/C/S/P/F. Verify from the raw archives that each is computable from recorded quantities alone
(`N_Y`, `n_centres`, integrity), that `n_centres` really is single-linkage at `CORE_R = 5.0`, and
that nothing anywhere imputes a parent. Attack the claim that `SHARED_PARENT_POOL` is honoured.

**A3 — was the old contradiction removed or merely hidden?** Re-derive
`muY >= 1 - 0.5^(1/tau)` and `muY <= 1 - 0.5^(1/T)` yourself at tau = 83, 111, 125. Then attack
the claim that replacing C2_FOUNDER by lineage non-extinction dissolves it: does the lineage
region silently relax some OTHER frozen requirement? Compare the L-gates against the original
C1/C2/C3 and against `N_STAR`, `GAMMA_SEP`, `ALPHA_SURVIVAL`. Is the 563-point region an artefact
of dropping C3 rather than of changing C2?

**A4 — are all 128 worlds correctly labelled developmental?** Hunt for any surviving use of the
historical DISCOVERY/VALIDATION labels as if validation were untouched, any language calling
FLCR01 output prospective, held-out or confirmatory, and any place a reanalysis is presented as
confirmation.

**A5 — feedback: Simpson's paradox and stopping bias.** The addendum publishes five comparisons.
Attack them: is the matched-window comparison itself broken (survivorship)? Is the pooled B1
number computed on a different denominator from the pooled B2 number? Does the addendum anywhere
still assert a feedback magnitude as if identified?

**A6 — does the candidate region use world-level uncertainty?** This is the sharpest attack.
Inspect `lineage_regions()` in `flcr01_science.py`. Determine exactly what environment the exact
chain uses, whether any world-level dispersion enters the region at all, and what the region would
look like at the low end of the measured world-level exposure distribution. Quantify.

**A7 — is architecture change overclaimed, or underclaimed?** Argue both. Tests A–E all report not
holding. Is any of them evaluated on evidence too thin to support "does not hold"? Conversely, is
`NOT_ESTABLISHED` too weak given that the two-centre gate L3 fails at both measured points?

**A8 — reproducibility.** Delete nothing, but verify: can every FLCR01 JSON be regenerated by
running the committed code? Are there hand-edited blocks? Does `PQEC01_REPRODUCIBILITY_LEDGER.json`
tell the truth about which script writes which file?

## OUTPUT — exactly two files
`/home/claude/FLCR01/review/FLCR01_ADVERSARIAL_REVIEW.md` and `…/FLCR01_ADVERSARIAL_REVIEW.json`

Each finding carries: `ID, ATTACK, SEVERITY (LOAD_BEARING|SUBSTANTIVE|COSMETIC), STATUS,
CLAIM_ATTACKED, EXACT_FILE_AND_LINES, EXACT_NUMBERS, WHY_IT_MATTERS,
SETTLING_COMMAND_OR_CALCULATION, MINIMUM_REQUIRED_CHANGE`. `LOAD_BEARING` = if confirmed, the
terminal disposition must change.

End the `.md` with exactly:
```
REVIEWER_VERDICT              = <CANDIDATE_DISPOSITION_SUPPORTED |
                                 CANDIDATE_TOO_CONSERVATIVE__LINEAGE_REGION_IS_DERIVED |
                                 CANDIDATE_TOO_STRONG__CRITERION_NOT_RESOLVED |
                                 EVIDENCE_OR_PROVENANCE_INCOMPLETE>
LOAD_BEARING_DEFECTS          = <int>
SUBSTANTIVE_DEFECTS           = <int>
COSMETIC_DEFECTS              = <int>
ATTACKS_REFUTED               = <int of 8>
FOUNDER_REJECTION_IS_SCIENTIFIC = <YES|NO|UNDETERMINED>
LINEAGE_MEASURABLE_WITHOUT_GENEALOGY = <YES|NO|UNDETERMINED>
CONTRADICTION_TRULY_REMOVED   = <YES|NO|UNDETERMINED>
NEW_SCIENTIFIC_RUNS_BY_REVIEW = 0
```
A finding without exact file, lines and numbers is not a finding.
