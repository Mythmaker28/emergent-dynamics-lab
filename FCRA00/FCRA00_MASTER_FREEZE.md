# FCRA00_MASTER_FREEZE

`FSQBT00_CORRECTIVE_RESIDUAL_AUTOPSY_00`. A **zero-engine-start** corrective and response-informed
secondary analysis. Written, hashed and committed **before any FSQBT00 npz, reader series, response
vector, score, residual, basis array, checkpoint array or threshold array is decoded**. Source
code, schemas, prose, git metadata, file names, sizes, hashes and access ledgers were inspected
before this commit; opaque candidate bytes were hashed only to locate byte-identical files; no
scientific statistic was printed or inspected.

Workdir `/home/claude/sweep/FCRA00`, branch `dev/fsqbt00-corrective-residual-autopsy-00`, created
from the exact FSQBT00 tip `b3f45ac7781e0dd48f34886b7c63840af520d502`.

## 0. Hard scope — zero starts, no regeneration

    ENGINE_STARTS_AUTHORIZED = 0
    PANEL/SHAM/ACTIVE/OTHER_OR_DIAGNOSTIC_STARTS_AUTHORIZED = 0   RETRIES_AUTHORIZED = 0
    CHECKPOINT_REGENERATION_AUTHORIZED = false
    NEW_SEED_STATE_GENERATION_OR_CANDIDATE_STATE_OPENING_AUTHORIZED = false
    COMMITTED_SEED_LEDGER_METADATA_READ_AUTHORIZED = true
    NEW_OPERATOR/NEW_DOSE/NEW_LAWSPEC/ENGINE_EQUATION/NEW_TRACER = false
    READER/MASK_REDETECTION/NORMALIZER/HORIZON/WEIGHT_CHANGE = false
    PARENT_OBJECT_MUTATION = PARENT_TUBE_MUTATION = THRESHOLD_REVISION = false
    PUSH/DRAFT_PR/WORKFLOW = false     TOMMY_ACTION/GIT_ACTION_REQUIRED = false

**Any engine invocation of any kind — timing probe, smoke test, identity continuation, or a seed
read that advances physics — is a fatal out-of-scope event. Stop and report; never hide it.**

Independent unit = the **twelve FSQBT00 ancestry blocks**. The 24 carrier rows, two channels, sites
and scored times are repeated conditions, never independent n. The four FWL2CF00 blocks calibrated
the old tube; they are not twelve more FSQBT00 units.

## 1. Parent binding (resolved from git objects, not prose)

    FSQBT00 tip     b3f45ac7781e0dd48f34886b7c63840af520d502  (subtree ab11f2c0…, bundle 0a7ce1e2…)
    SQDT00 tip      16717582e7f0dfd371f21c56465e11113d8b6675
    FWL2CF00 source 96c7d295e72106cd949d810fa92807c2514e7449
    Tommy main      f3921a4d2eb4f3c5d8c88855048d32bcd0c02a77  (never moved/checked out/merged/reset)

Immutable object `FWL2_RELATIVE_QUOTIENT_BASIS_V1` (owned by SQDT00): npz blob
`8f848411…`, json blob `07c9cf7e…`, arrays `mu, P1, P2, e1, e2`. Frozen tube
`TUBE_P2_LOBO = 1.2166510017869535e-07`. **None of `mu, P1, P2, e1, e2, tube` may be mutated,
refit, rescaled, recentered, rotated or re-versioned.**

## 2. Known response-informed facts (this analysis is NOT blind)

Frozen before this prompt: all 24 active cells materially responsive; native carrier contrast
material in 12/12 blocks; 3/12 blocks exceeded the parent P2 tube by reported factors 1.06–1.22;
all three reported exceedances NEAR; aggregate outside residual reported 0.70× tube; frozen-P2
projected energy reported 3.6× E_TAU; parent-e2 sign concordant in 10/12; a newly fitted fresh
quotient reported relative-at-least-two with its second increment below absolute materiality. **The
NEAR pattern is post-outcome discovery.** No nominal p-value from these twelve blocks converts a
post-outcome hypothesis into prospective confirmation.

## 3. Mandatory prose corrections (append-only corrigendum, not reclassification)

1. `DIRECT_CARRIER_CONTRAST_MATERIAL = 12/12` is the **magnitude** of the native contrast.
2. `PARENT_E2_SIGN_CONCORDANCE = 10/12` is **directional** agreement. **Never "direction replicated
   12/12"** — the FSQBT00 disposition string `CARRIER_DIRECTION_REPLICATED_12_OF_12` conflated the
   12/12 magnitude with direction and is corrected here.
3. Because P2 failed and e2 was below the absolute floor,
   `FROZEN_E2_TRANSFER_AS_FROZEN = NOT_TRANSFERRED`; 10/12 cannot rescue it.
4. The fresh quotient is a new fit on the **same 24 FSQBT00 rows**; "independent" means independent
   of the parent panel, **not** statistically independent of the carrier contrast or P2 analysis.
5. `STRICT_NO_REFIT_ALL_BLOCK_CONTAINMENT = FAIL_3_OF_12`; `POPULATION_NONTRANSFER = NOT_ESTABLISHED`.
   The original disposition is **not** silently changed; an append-only corrigendum narrows its
   interpretation only.

The frozen gate `FROZEN_P2_TRANSFER_AS_FROZEN = NOT_TRANSFERRED` is **not** reclassified: 3/12
exceeded the tube, so it failed exactly as written.

## 4. One legal gauge everywhere

Every residual analysis uses the exact FSQBT00 parent-P2 gauge-selection procedure frozen before
active decode (one optional linked A/B exchange per ancestry block, shared across both carriers and
all scored times). `g*` denotes each certified co-optimal global representative, used identically
across both carriers, all twelve blocks, NEAR/FAR and allocation summaries, all channels and times,
and both direction-arbitration calculations. **Never** a per-NEAR, per-FAR, per-breach, per-carrier,
per-channel, per-time or per-candidate gauge. The sole exception is the twelve training-only folds
of §9, which restrict the same immutable, label-blind parent-P2 criterion to eleven training blocks
and orient the omitted block by its own label-blind criterion. A deterministic lexicographic
tie-break among exactly co-optimal gauge bitstrings is frozen for reproducible bytes; it is not a
proof of gauge invariance. `ETA_AXIS_SERIALIZATION` is derived from coefficient/reader arithmetic
and frozen in Commit 3; it is not a fitted tolerance nor the 0.80 jackknife guardrail.

## 5. Recompute matrix (independent, twice)

Reproduce, through the production reader path AND an independent reference that imports neither the
production scorer, gauge optimizer nor direction fitter: 24/24 row materiality (exact M2², TAU²,
margins); 12/12 native direct carrier-contrast materiality (exact norm, 2·TAU); exact mu/P1/P2/e1/e2
hashes and reload; the four true-LOBO licences/alignments/eigengaps/contribution-fractions and the
exact rational tube; the full linked-gauge argmin set and every co-optimum; P2 projected energy,
aggregate outside residual, and all twelve block residuals in the exact mean-per-line units; the
per-block residual/tube trichotomy `PASS | CERTIFIED_EXCEED | NUMERICALLY_UNRESOLVED`; T0–T6 and all
twelve LOFO; e2 incremental energy, E_TAU, contributions, gates; CO0–CO3 with the exact 10/12
identities; fresh R0,R1,R2,I1,I2, intervals, ratios, common argmin and co-optima; the start ledgers;
support-restricted sufficiency and full-field status. **The report must include the actual numeric
quotient tuple.**

For the 10/12 parent-e2 sign diagnostic, distinguish the combinatorial reference from a licensed
p-value:

    COMBINATORIAL_ONE_SIDED_K_GE_10_OF_12   = 79/4096  = 0.019287109375
    COMBINATORIAL_TWO_SIDED_SYMMETRIC       = 79/2048  = 0.03857421875

A p-value is licensed only if CO0 and CO3 are proven and the null sign-flip/exchangeability symmetry
for the twelve blocks is explicitly justified. Otherwise `P_VALUE_STATUS = NOT_LICENSED` and 10/12
is descriptive with the two fractions kept only as combinatorial references.

## 6. Four-calibration / twelve-future order-statistic diagnostic (interpretation, not a p-value)

The tube = max of four true-LOBO out-of-sample scores; the fresh gate required all twelve future
scores below it. Under the hypothetical that four calibration and twelve future scores are
continuous and exchangeable, with K = #future above the max of four:

    P(K=k) = C(15-k,3)/C(16,4), k=0..12   P(K=0)=1/4   E[K]=12/5=2.4
    P(K=3)=11/91=0.120879…   P(K>=3)=11/28=0.392857…

Vocabulary: `FROZEN_ALL_BLOCK_GATE = FAILED_AS_PREDECLARED`;
`UNIFORM_FIXED_PANEL_CONTAINMENT = NOT_QUALIFIED`;
`POPULATION_P2_NONTRANSFER = INCONCLUSIVE_FROM_THIS_GATE_ALONE`. The tube is never replaced after
seeing the twelve scores; this reference never retroactively passes T4.

## 7. Residual anatomy (immutable parent affine object)

For block b, carrier o: `v[b,o] = z_g*[b,o] - mu`, `q[b,o] = (I-P2) v[b,o]`,
`T[b] = ½ Σ_o ‖q[b,o]‖²`. Certify:

    E_OUT_TOTAL = (1/24) Σ ‖q‖² ;  Q_BAR = (1/24) Σ q ;  E_OUT_INTERCEPT = ‖Q_BAR‖²
    E_OUT_CENTERED = (1/24) Σ ‖q - Q_BAR‖² ;   E_OUT_TOTAL = E_OUT_INTERCEPT + E_OUT_CENTERED
    C[b] = (q[b,1]+q[b,2])/√2 ;  D[b] = (q[b,2]-q[b,1])/√2 ;  ‖q1‖²+‖q2‖² = ‖C‖²+‖D‖²
    C_BAR = (1/12) Σ C[b] ;  E_OUT_INTERCEPT = ½‖C_BAR‖²
    E_COMMON_TOTAL = (1/24) Σ ‖C‖² = E_OUT_INTERCEPT + E_COMMON_CENTERED
    E_DIFFERENTIAL_TOTAL = (1/24) Σ ‖D‖²
    exclusive three-way:  E_OUT_TOTAL = INTERCEPT + COMMON_CENTERED + DIFFERENTIAL

Intercept diagnoses a transported-mean failure, not a new dimension. Common dominance ≠ carrier
specificity; differential dominance may motivate a carrier-conditioned axis but stays discovery. A
12/12 direct contrast does not imply its outside-P2 differential component is material. Decompose by
gauge-invariant A+B per time and the channel×time orbit; contributions sum to the parent within
certified error; coordinates already carry √w_h (never re-weight). Off-support spatial mapping is
forbidden unless full-field sham/active scored-time arrays are recovered, bound to their preactive/
active hashes and committed without rerun. Leave-one-ancestry influence for every aggregate; never
drop a block for exceeding the tube.

## 8. Nuisance 2×2 (descriptive, hypothesis-generating only)

Three blocks per cell (NEAR/FAR × alloc 0/1). On T[b]:

    G  = ½[(NEAR0−FAR0)+(NEAR1−FAR1)]      (geometry, oriented, upper tail G≥observed)
    H  = ½[(NEAR1−NEAR0)+(FAR1−FAR0)]      (allocation, un-oriented, |H|)
    GH = (NEAR1−NEAR0)−(FAR1−FAR0)         (interaction, no ½, |GH|)

`HYPOTHESIS = G>0`, `STATUS = POST_OUTCOME_DISCOVERY`. G calibration: permute NEAR/FAR within each
allocation stratum, `C(6,3)²=400`. GH omnibus: `12!/(3!⁴)=369600` as
`OMNIBUS_FOUR_CELL_EXCHANGEABILITY_REFERENCE` only. Every tail area is
`DESCRIPTIVE_EXCHANGEABILITY_CALIBRATION`, never a randomized causal p-value; Holm quantities are
`DESCRIPTIVE_HOLM_ADJUSTED_TAIL_AREAS`. Exact 2×2 count: NEAR 3/3, FAR 0/6,
`P_ALL_3_IN_NEAR = C(6,3)/C(12,3) = 1/11`, `FISHER_TWO_SIDED = 2/11`.

## 9. Three-branch discovery-only direction rule (fixed before decode)

Coordinates `c[b]=C[b]`, `d[b]=D[b]`. `DELTA_C = mean_NEAR c − mean_FAR c`, likewise `DELTA_D`.
`GEOMETRY_ENERGY_X = ‖DELTA_X‖²/8`. Conservative floor
`A_DELTA_TAU = (√2/6) Σ_b TAU_b`, `E_DELTA_TAU = A_DELTA_TAU²/8 = (Σ TAU_b)²/144` (both derivations
proved in the exact WL2 norm). Axis sign canonical toward the full-panel NEAR−FAR contrast; a delta
whose certified norm contains zero is unidentifiable. Gates:

    DX0 primary recomputation and residual identities pass
    DX1 ‖DELTA_X‖ certifiably nonzero
    DX2 lower(‖DELTA_X‖) > upper(A_DELTA_TAU)   (equiv. certified GEOMETRY_ENERGY_X > E_DELTA_TAU)
    DX3 allocation-specific projected contrasts AND energy shifts positive for h=0 and h=1
    DX4 training-only omitted-block geometry prediction correct in ≥10/12 resolved folds
        (unresolved never counts as correct)
    DX5 both cross-allocation predictions positive
    DX6 min full-vs-fold squared alignment ≥ 0.80 AND max PROJECTED_LEVERAGE_FRACTION < 0.50
    DX7 no allocation projected-contrast or energy-shift sign reverses under any single-block deletion
    DX8 co-optimal axes unique or projectively/covariantly equivalent within ETA_AXIS_SERIALIZATION
    DX9 production and reference agree

    C fail & D fail  -> M0_NO_UNIQUE_DIRECTION_LICENSED
    C pass & D fail  -> M1_NEAR_FAR_COMMON_SHIFT__DIRECTION_SERIALIZED
    C fail & D pass  -> M2_NEAR_FAR_DIFFERENTIAL_SHIFT__DIRECTION_SERIALIZED
    C pass & D pass  -> MULTICOMPONENT__NO_UNIQUE_DIRECTION_SERIALIZED
    any unresolved   -> NUMERICALLY_OR_GAUGE_UNRESOLVED

At most one qualifying direction is serialized as `FSQBT00_RESIDUAL_DIRECTION_DISCOVERY_V1`
(SOURCE = RESPONSE_INFORMED_FSQBT00_TWELVE_BLOCK_DISCOVERY, VALIDATION_STATUS = NOT_VALIDATED,
TRANSFER_STATUS = NOT_TESTED, PHYSICAL_DIMENSION_STATUS = NOT_CLAIMED). No P3, fresh PCA,
conditional plane, cell intercept, nonlinear model, dose, third carrier, response/TAU ratio or
selected subset. Never V2, a transferred axis, or a new physical dimension.

## 10. Stop rules, claim ceiling, delivery

Fatal stops: parent/ancestry/raw cannot bind; main moved; any engine/seed path invoked; freeze late;
production≠reference beyond certified bounds; parent mu/P2/tube/thresholds mutated; group- or
direction-specific gauge; a held-out block enters its training fit; a direction outside the three
branches; missing checkpoints regenerated; co-optimal gauges flip a categorical verdict unmarked;
evidence deleted/overwritten/hidden. Missing full-checkpoint bytes are a hard claim ceiling and
delivery failure, not a stop for exact reader-level autopsy when compact sufficiency re-proves.

Forbidden claims: P2 transferred despite T4 failure; population non-transfer proved by three mild
exceedances; direction replicated 12/12; e2 transferred/absolutely material; NEAR caused the
exceedances; a discovery direction validated; a new physical mode; 24 independent replicates; full
end-to-end reproducibility if checkpoint bytes stay missing; repaired protocol conformity after an
unauthorized start; absence of a mechanism from a failed representation.

Commit order: 1 freeze/provenance-intent; 2 provenance/start/evidence audit + recovered bytes +
corrigendum intent; 3 firewall/oracle/direction-rule freeze + lock readback; 4 independent primary
recomputation + finalized corrigendum; 5 residual anatomy + nuisance; 6 direction arbitration +
optional serialization; 7 report/manifest/closure. Append-only; no amend/rebase/reset. Build
`SHA256SUMS_SCOPE.json` over the non-self-referential subtree; verify from tree and extraction;
reproduce subtree under two git versions; verify a final bundle; report its digest out of band.
