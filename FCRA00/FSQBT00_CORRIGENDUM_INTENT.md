# FSQBT00_CORRIGENDUM_INTENT

Append-only. This file records, from provenance facts already proven **without opening any science
array**, the corrections FCRA00 intends to make. It never amends or rewrites an FSQBT00 commit. The
numerical prose corrections are announced here but asserted as independently verified only after the
Section 5 recomputation, in `FSQBT00_APPEND_ONLY_CORRIGENDUM.md`.

## Provenance facts proven now (no science decoded)

1. **Start count.** The authoritative charged total is **61** (construction 12 + sham 24 + active 24
   + diagnostic 1). Seed 70000 was a single unauthorized diagnostic start against a budget of 0;
   `DIAGNOSTIC_START_STATUS = ONE_UNAUTHORIZED_START__NO_SCIENTIFIC_OUTCOME_OPENED_PROVEN`. The
   committed raw-advance figure 60 excluded the probe; the inclusive raw-advance count is 62. Never
   report 60 as a total.

2. **Full-checkpoint evidence.** The FSQBT00 original tip is
   `INCOMPLETE_MISSING_FULL_CHECKPOINT_BYTES` (only digests committed). FCRA00 recovers the exact 12
   checkpoint bytes (12/12 match the committed digests) and commits them on the child branch. Masks
   were already committed. This never makes the historical FSQBT00 tip self-contained retroactively.

3. **Original tip vs child.** `FSQBT00_ORIGINAL_TIP_DELIVERY_STATUS =
   INCOMPLETE_MISSING_FULL_CHECKPOINT_BYTES`; `CURRENT_CHAIN_EVIDENCE_STATUS =
   COMPLETE_AFTER_APPEND_ONLY_CHILD_RECOVERY`.

## Numerical prose corrections ANNOUNCED (verified only after Section 5)

* **Magnitude vs direction.** `DIRECT_CARRIER_CONTRAST_MATERIAL = 12/12` is a magnitude statement;
  `PARENT_E2_SIGN_CONCORDANCE = 10/12` is a directional statement. The FSQBT00 disposition string
  `CARRIER_DIRECTION_REPLICATED_12_OF_12` wrongly attached 12/12 to direction and will be corrected
  to: magnitude 12/12, e2 direction 10/12.
* **Strict gate vs population.** `STRICT_NO_REFIT_ALL_BLOCK_CONTAINMENT = FAIL_3_OF_12` (the frozen
  gate failed exactly as written) but `POPULATION_NONTRANSFER = NOT_ESTABLISHED` — the tube was the
  maximum of only four calibration folds, and 3/12 exceedances are consistent with exchangeability
  (`P(K≥3) = 11/28 ≈ 0.39`). FSQBT00 prose that leaned toward "parent-panel-specific geometry" will
  be narrowed. The frozen gate verdict `FROZEN_P2_TRANSFER_AS_FROZEN = NOT_TRANSFERRED` is **not**
  reclassified.
* **Fresh-quotient claim ceiling.** The fresh quotient is a new fit on the **same 24 rows**;
  "independent" means independent of the parent panel, not statistically independent of the carrier
  contrast or the P2 analysis.
* **e2 transfer.** `FROZEN_E2_TRANSFER_AS_FROZEN = NOT_TRANSFERRED`; 10/12 sign concordance cannot
  rescue it.

The final corrigendum is a correction of record and interpretation. It does not reclassify the
frozen gate and does not repair protocol conformity: an append-only record correction never turns
`NONCONFORMANT` into `PASS`.
