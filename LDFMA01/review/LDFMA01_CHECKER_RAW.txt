I read every listed artefact, all five code files, the 26 per-world records, the three reconstruction passes, and the underlying engine/observer/world code that defines the archive's timing semantics. I ran no simulations and modified nothing.

**Verified correct, briefly, so the findings below are not misread as global doubt.** All 37 parent-binding artefacts match their declared sha256 and byte counts exactly (I recomputed all 37; zero mismatches). `muY=0.000740894982503035` and `kY=0.001004754572603833` match `MCTT01/out/MCTT01_PHYSICS_DIFF_FROM_B1.json` exactly. 11385 × muY = 8.43509 (reported 8.435). The exact-McNemar grid reproduces to three decimals (0.582 / 0.308 / 0.153 / 0.020 / 0.973 / 0.998), `PAIRS=22` from 512/2×22/256 is exact, and "5 discordant pairs → p = 0.031" is right (0.5⁵=0.03125). The ambient totals recompute (2018 complete, median 93, p̄ = 2018/19634 = 0.102781, 2017/2018 = 0.999504). `pq_ydeath` (`PQEC01/code/pqec01_observer.py:137-146`) records **only** the `_decay()` channel — hops go to `pq_yhop` and are not archived — so muY is the right rate and the "hops logged as deaths" failure mode does not exist. The step order (`ORR01/code/kinetics.py:154-162`; `TLMR01/code/tlmr01_world.py` run loop calls `_one_step()` then `tl_record(t)`) confirms the author's premise: `c_*` rows at step t are **post-step** while `ydeath` rows at step t are **intra-step**, so the frozen step-t attribution genuinely cannot see a decay that empties a cell. **The attribution mechanism is real.** The three internal passes (`work/ldf_out`, `ldf2_out`, `ldf3_out`) are identical on every core verdict — passes 2 and 3 only add diagnostic fields — so no bug-fix was folded in to make agreement happen. `ldfma01_raw.py` imports no `tlmr01_*` or `fimrcc01_*` module and never reads `c_cid` or `k_id`. "0 of 22 extinct" is supported (`contiguous`=True and `SPLIT_OR_TIE` in all 22). The one staged archive's sha256 matches `TLMR01_RAW_SHA256SUMS`.

---

## Findings

**C-01 — The t-1 probe is used as a verdict, in direct contradiction of the artefact that says it is not.**
`LDFMA01_FAILURE_PARTITION.json → AND_THE_REMOVAL_LEG_IS_MOSTLY_AN_ATTRIBUTION_ARTEFACT.NO_VERDICT_IN_THIS_MISSION_USES_THE_t_MINUS_1_RULE = true`, and `ldfma01_raw.py:445` ("reported only. The frozen rule is the one used for every verdict"). But `ldfma01_routeb.py:28-32` (B1 condition 1 = FAIL, "Section 3 measured 8 of the 8.44 removals … the removals happen and the daughters survive them"), `:70-73` (B3 condition 1 = PARTIAL, same evidence), `:86-92` (`WHY_NOT` / `IS_THAT_PRECONDITION_MET=false`), and `LDFMA01_ROUTE_ARBITRATION.json → STEP_2_ROUTE_B.why` all rest on the probe. `ldfma01_routes.py:27` sets `p_corr=5/22` from it. `ROUTE_B_CLASSIFICATION = NO_MINIMAL_CHANGE_JUSTIFIED` is therefore a probe-derived verdict, and it is the second gate of the arbitration that yields the terminal.
**LOAD_BEARING** (Route B classification → terminal disposition).
*Require:* either delete the "no verdict uses it" assertion and re-label every downstream conclusion as probe-conditional, or re-derive Route B without the probe and state what B1/B3 look like on the frozen record alone.

**C-02 — "The physics already produces locked-daughter constituent turnover. The measurement does not see it." overstates what the numbers show.**
`LDFMA01_MINIMAL_ARCHITECTURE_CANDIDATES.md` (bolded) and `.json → WHY_NOT`. Under the author's own repair the endpoint goes from 1/22 to 5/22. **17 of the 21 failures survive the repair**, and they survive for a physical reason, not a measurement one. "Mostly an attribution artefact" is true of the *event count* (7 of 8 events recovered) and false of the *world-level failure* (4 of 21 failures recovered). The bolded sentence appears with no status disclaimer and no probe caveat, in the artefact whose classification drives the terminal.
**LOAD_BEARING.**
*Require:* restate as "the frozen endpoint's step-t attribution is blind to ~86 % of the constituent removals the decay rate delivers; repairing it raises the measured rate from 1/22 to at most 5/22, and 17 of 22 worlds still fail" — and drop the unqualified physical claim.

**C-03 — The corrected rate 5/22 is exactly the exposure-limited Poisson expectation, which falsifies the alignment argument used to reject E3 and to downgrade B3.**
I computed, per world, λ_w = muY × `daughter_particle_steps_after_t_m`, then Σ_w [1(birth observed) × (1 − e^(−λ_w))] = **5.81 expected COMPLETE worlds; 5 observed under t-1**. Per-world λ ranges 0.061 to 1.268 (mean 0.383), so P(no removal) ≈ 0.68 per world by physics alone. Exposure = occupancy × identity lifetime is therefore the binding constraint on the *corrected* endpoint, and (via the multiply-occupied-cell fraction, C-07) on the frozen one too. This contradicts `ldfma01_routes.py:52-54` and `:61-65` ("persistence is NOT the binding constraint … the daughters persist a median 230 steps and up to 1472, and still fail") and `ldfma01_routeb.py:70-73` ("Extending the window is not the repair the evidence asks for"). The cited counter-example is self-defeating: i158 persisted 1472 steps at λ = 1.27, and P(0 removals | λ=1.27) = 0.28 — an ordinary Poisson outcome, not evidence that persistence is irrelevant.
**LOAD_BEARING** (E3's rejection, B3's condition-1 verdict, Route A eligibility, terminal).
*Require:* the per-world λ table, the Poisson prediction against the observed 5, and a re-derivation of E3 condition 1 and condition 7 against it.

**C-04 — The probe compares three different units and calls them one quantity.**
`ldfma01_raw.py:250-255` builds `e["yd"]` by appending **once per matching cell**; `:408` builds `yd_prev` by appending **once per step** (`any(c in dsets[t-1] …)`); the 8.435 expectation counts **particles** (`ydeath` column 3, `n_died`, is never read anywhere). So `n_Y_removals_attributed_at_step_t_frozen_rule` (cell-events) = 1, `…_at_step_t_minus_1` (steps) = 8, and 8.435 (particles) are set side by side in `LDFMA01_FAILURE_PARTITION.json` as if commensurable. In the one archive I can still read, all `n_died` = 1 and no step has two daughter deaths, so the numeric effect is probably zero — but it is unverified in the other 21 worlds, and i045 (nY up to 4) is the plausible exception, contributing 3 of the 8.
**NOT_LOAD_BEARING** on current evidence.
*Require:* recount all three in particles (sum `n_died`) and report whether any number changes.

**C-05 — The t-1 window overhangs the identity by one step; two of the five flipped worlds sit on that boundary.**
`ldfma01_raw.py:408` admits any ledger step t with `(t-1) in dsets`, so t = `last`+1 is always in range even though the identity has ended at `last`. In **i153** the sole recovered removal is at step **715** while `interval_end` = **714** — the event is outside the identity. In **i119** the recovered removal is at **1534** = `interval_end`, so `end − fd = 0` and the frozen `FUNCTIONAL` guard `(e["end"]-fd)>0` (`ldfma01_raw.py:414`) would reject it — but `WOULD_BE_COMPLETE_UNDER_t_minus_1` (`:444`) carries no such guard. The corrected **FUNCTIONAL** rate is therefore ≤ 4/22 and is never computed anywhere, while the mission's headline endpoint of record is FUNCTIONAL.
**LOAD_BEARING** for the "5 of 22" headline and for `p_corr = 5/22`; not for the terminal (a lower p₁ only strengthens Route A's rejection).
*Require:* apply the same persistence and local-X guards to the probe, publish the corrected FUNCTIONAL count alongside the corrected COMPLETE count, and exclude t = `last`+1 or justify including it.

**C-06 — The t-1 set is not the configuration the decay acted on, and the birth counts prove it.**
The decay at step t acts after that step's Y diffusion and reaction; `dsets[t-1]` is the end of step t−1. `p_hop_Y = 0.10263340389897246` (`PQEC01_MASTER_FREEZE.INHERITED_FROZEN_CONSTANTS`), so ~10 % of Y have moved in between, and the exact pre-decay field is **not** reconstructable from `TLMR01-ARCHIVE-1` (only end-of-step `c_*` rows are archived; `pq_ycells` is not). The evidence is in the artefact's own numbers: births fall from **33 (frozen) to 30 (t-1)** across the 22 worlds — the shift is a *different* attribution, not a recovery. `LDFMA01_FAILURE_PARTITION.json → MECHANISM` and both architecture artefacts present the t-1 rule as *the* repair rather than as a lower-bounding proxy.
**NOT_LOAD_BEARING** numerically; the direction is conservative.
*Require:* state that t-1 is a proxy for an unarchivable configuration, quantify the ~10 % hop-out false-negative rate, and stop calling it "the identical ledger rows attributed one step earlier" as though the window were exact.

**C-07 — The one calculation that would actually test the mechanism is absent, and it happens to support the author.**
Under the stated mechanism the frozen rule should still see every decay in a cell holding ≥2 Y. In the only archive still present (i001), the post-t_m window holds 445 cell-steps at nY=1 and 35 at nY=2 → **13.6 % of particle-steps are in multiply-occupied cells**, predicting 8.435 × 0.136 = **1.15 frozen-visible removals against 1 observed** (Poisson P(X≤1 | 1.15) = 0.68). That is a genuine quantitative confirmation, and it is the only one available. The artefacts instead compare 1 against 8.435 and let the reader infer the mechanism.
**NOT_LOAD_BEARING** (strengthens the author).
*Require:* compute the multiply-occupied particle-step fraction for all 22 worlds and report predicted-vs-observed frozen-visible removals as the mechanism's test.

**C-08 — Three of the eight recovered removals come from the single world where the daughter was not alone.**
`LDFMA01_REMOVAL_WORLD_LEDGER.csv` shows `parent_nY + locked_daughter_nY = world_nY_at_trigger` in **22 of 22** worlds — so after the removal the daughter is the world's entire Y population, and attribution is near-trivial. The exception is **i045**, the only world with `n_centres_at_termination_step = 2`, and it supplies 3 of the 8 t-1 removals. No artefact reports, per recovered event, how many components existed at that step or whether the event cell belonged to another component at step t.
**NOT_LOAD_BEARING** (removing i045 leaves 5 removals in 4 worlds and 4 flipped worlds).
*Require:* a per-event table: step, cell, component count at t−1 and t, and whether the cell was in another component's step-t set.

**C-09 — "recovers 8 of the 8.44" should be "recovers 7".**
`LDFMA01_FAILURE_PARTITION.json → MECHANISM`; `LDFMA01_MINIMAL_ARCHITECTURE_CANDIDATES.md` and `.json → WHY_NOT`. One of the 8 was already attributed by the frozen rule (i001). The re-attribution recovers 7. Repeated in three artefacts.
**NOT_LOAD_BEARING.** *Require:* the word "recovers" applied to 7, with 8 stated as the total under t-1.

**C-10 — The repeated-opportunity "test" is circular and cannot fail.**
`ldfma01_autopsy.py:114-115`: `pbar = Σn_complete / Σn_intervals` = 2018/19634, then `pred = 1-(1-pbar)**n` with the *same* n. With n ≥ 694, the prediction exceeds 0.99 for **any** p̄ ≥ 0.0066 — 15× below the observed value. It would fail only if the whole 22-world corpus contained fewer than ~20 complete intervals; it contains 2018. Worse, `LDFMA01_AMBIENT_SATURATION_MECHANISM.json → IDENTITIES_WITHIN_A_WORLD_ARE_NOT_INDEPENDENT_REPLICATES` explicitly denies the independence that the exponent n assumes, in the same file.
**LOAD_BEARING** for section 5's stated conclusion ("It is a repeated-opportunity effect" is presented as a tested inference).
*Require:* drop the model or label it as an illustration, and rest the conclusion on the direct, model-free observation that every world contains 65–117 complete intervals.

**C-11 — "2017 of 2018" is exactly right and materially understated; the one exception is the locked daughter itself.**
`ldfma01_autopsy.py:100-101`; `THE_DECISIVE_TIMING_FACT`. I traced the exception: it is i001's own daughter interval, `interval_start` = 693 = t_m − 249 (NEED = 250), which is why `steps_after_t_m.min = −249`. **Zero** non-daughter complete intervals start inside any daughter's window. So the true statement is "no ambient complete interval other than the locked daughter itself overlaps the daughter's window" — stronger and cleaner than "essentially every one". The flip side is that `ambient_complete_interval_count` **includes** the daughter, so "2018 ambient" and "1 daughter" are not disjoint sets and the fraction 0.999504 is computed against a denominator containing its own numerator's complement.
**NOT_LOAD_BEARING.** *Require:* name the exception, report ambient with the daughter excluded, and state the exact zero.

**C-12 — The partition's exclusivity/exhaustiveness check is vacuous and one bin is structurally unreachable.**
`ldfma01_analyse.py:162`: `PARTITION_IS_MUTUALLY_EXCLUSIVE_AND_EXHAUSTIVE = sum(cnt.values())==22`. `stage()` returns exactly one code per world by construction, so this is always true and tests nothing. Separately, `COMPLETE_TURNOVER = yb and yd and minNY>=1` (`ldfma01_raw.py:410`) has a vacuous third conjunct: `c_*` rows exist only for Y-occupied cells, so `gnY ≥ 1` always — I confirmed `minNY_over_the_whole_interval = 1` in all 22. Hence L5 ("both occur but not in one continuous nonempty identity", `:119`) is unreachable except in the measure-zero case where the identity ends exactly at the first removal. Of 13 declared codes, only L2, L3, L8 and SUCCESS ever fire; L0, L1, L4, L5, L6, L7, L9, L10, L11, L12 are all empty, and `SECONDARY_LATER_FAILURES` is the constant `L8@<step>` in 22 of 22 rows.
**NOT_LOAD_BEARING.** *Require:* a real exclusivity test, and a statement of which codes are unreachable by construction versus merely unobserved.

**C-13 — Demoting SPLIT_OR_TIE to "secondary" is defended on informativeness, not on causal priority, and it closes the exposure window the author's own model makes binding.**
`LDFMA01_FAILURE_PARTITION.json → WHY_TERMINATION_IS_NOT_THE_PRIMARY_CODE` and `ldfma01_analyse.py:120-121, 126-135`. Every one of the 22 identities ends by `SPLIT_OR_TIE` with **exactly 2 successor candidates within CORE_R**, at a median of 230 post-removal steps. Under C-03 the endpoint is exposure-limited and exposure is lifetime × occupancy, so the split is causally upstream of L3 ("birth but no removal"), which is the declared dominant stage. The author's argument — that making termination primary would put 21 of 21 in one bin — is an argument that the split code is *uninformative as a discriminator*, which is a different thing from its not being the mechanism. Note the split is also *not* what the words suggest: a fragment landing **outside** CORE_R leaves the identity intact; only a fragment landing **inside** it terminates the identity (inherited from `fdot01_centres.link`, not LDFMA01's doing).
**LOAD_BEARING** for "the dominant locked-daughter failure", which is the phrase Route B's condition 1 is scored against.
*Require:* report both orderings side by side and state explicitly that under the exposure model the split is upstream of L2/L3.

**C-14 — `DOMINANT_FAILURE_STAGE` is computed over a counter that includes SUCCESS.**
`ldfma01_analyse.py:174`: `cnt.most_common(1)[0][0] if fail else None`, while `:175` correctly excludes SUCCESS from the count. At 16 vs 1 it happens to be right; with a higher success rate it would report "SUCCESS" as the dominant failure stage while `DOMINANT_FAILURE_COUNT` reported a different bin.
**NOT_LOAD_BEARING.** *Require:* exclude SUCCESS from both.

**C-15 — Cross-artefact numerical contradiction in the autopsy.**
`LDFMA01_SINGLE_SUCCESS_AUTOPSY.md:19` says "Four of **30** comparable features". `LDFMA01_SINGLE_SUCCESS_AUTOPSY.json` contains **41** features of which **39** are `COMPARABLE:true` (only `first_Y_removal_after_t_m` and `first_third_centre_after_t_m` are not).
**NOT_LOAD_BEARING.** *Require:* correct to 39.

**C-16 — The autopsy prose names two mechanisms as falsified that the JSON does not falsify, and omits two that it does.**
`LDFMA01_SINGLE_SUCCESS_AUTOPSY.md:38-42` lists "the parent–daughter distance, and the ambient pressure it faced" among the seven. The JSON's `MECHANISMS_FALSIFIED_BY_THE_SUCCESS` contains neither. `parent_daughter_centroid_distance` has `inside_the_failure_interquartile_span = **false**`, success 7.810 against failure Q1 = 9.138 and **rank 2 of 22** — it is one of the features on which the success most separates, and the md asserts the opposite. No F-family (ambient) feature appears in the list at all; three of them (`mean_world_nY_after_t_m` rank 2, `ambient_complete_interval_count` rank 3, `ambient_functional_interval_count` rank 4) are outside the failure IQR. The md silently drops `world_nY_at_t_m` and `n_X_births_after_t_m`, which are in the list.
**LOAD_BEARING** for the autopsy's own headline ("Everything else about it is ordinary").
*Require:* the md must transcribe the JSON list, and must report that the success world sits at rank ≤ 3 of 22 and outside the failure IQR on four comparable features (parent–daughter distance, mean ambient Y, ambient complete count, ambient functional count).

**C-17 — Three of the seven "falsified" mechanisms have the success at the failure minimum and are called "inside the IQR".**
`ldfma01_autopsy.py:46`: `inside_iqr = q[0] <= sv <= q[2]`, inclusive, on heavily tied discrete features where Q1 equals the minimum. Result: `world_nY_at_t_m` (success 2, failure min 2, Q1 2.0, **rank 1 of 22**), `daughter_nY` (success 1, Q1 1.0, rank 1), `maximum_locked_daughter_nY_after_t_m` (success 2, Q1 2.0, rank 1) are all declared falsified while being joint-extremal. The falsification is inverted for three of seven.
**LOAD_BEARING** for the claim "Seven candidate mechanisms the success falsifies".
*Require:* exclude values at the failure min/max from the "inside IQR" test, or report rank alongside every falsification; re-state the count (at most four survive).

**C-18 — The falsification rule is near-vacuous, and which features count as "mechanisms" was fixed after the outcomes.**
`LDFMA01_SUCCESS_COMPARISON_PREREGISTRATION.json → FALSIFICATION_RULE`. **29 of 39** comparable features lie inside the failure IQR, so "falsified" is the default outcome for a single observation. Worse, the pre-registration (18:25) fixes only the *feature set*; the `MECH` dictionary that decides which features carry "an executable mechanistic interpretation", and therefore which can be falsified, is written in `ldfma01_autopsy.py:17-28` and generated at 18:42 — after the 22 reconstructions. `MECH` even contains an entry (`fraction_of_life_at_nY_1`) that is in no pre-registered family. The mission's honest limit statement covers the success identity, not this.
**LOAD_BEARING** for what the pre-registration is claimed to buy.
*Require:* declare the mechanism mapping as post-hoc, or re-run the falsification with the mapping fixed to the pre-registered families only.

**C-19 — The sole "unique available-before-outcome" feature is a boundary artefact of a folded statistic, and its unfolded twin ranks 13 of 22.**
`f5_ratio_at_trigger = min(xd)/max(xd)`, so it is capped at 1.0 and can only be "outside the failure range" by attaining the cap. Success = 1.0 because the two discs were exactly equal (77 and 77). The unfolded `xd_ratio_daughter_over_parent` is also 1.0, sits at **rank 13 of 22** inside failure range [0.643, 1.556], and is **not** unique. Two deterministic functions of the same quantity return opposite uniqueness verdicts. The md softens this in prose; the JSON does not.
**NOT_LOAD_BEARING** (the author already discounts it). *Require:* the JSON should flag capped/folded features and report both.

**C-20 — The null expectation for "unique" features is never stated, and it equals what was observed.**
With 22 values per feature, P(the success is the strict min or max) = 2/22; over 39 comparable features the expected number of UNIQUE hits is **3.5**, and 4 were observed (3 of which are outcome restatements). So the uniqueness scan contains no evidence of any separating feature whatsoever. This *supports* `WHAT_THIS_MEANS_FOR_A_PROSPECTIVE_DESIGN`, but the artefact presents "exactly one available-before-outcome feature is unique" as a finding rather than as chance.
**NOT_LOAD_BEARING.** *Require:* state the 3.5 null expectation next to the 4.

**C-21 — The Route-A budget arithmetic is correct but the design space is unexamined, and two allocations inside the same budget are decision-capable.**
`ldfma01_routes.py:13-14` hard-codes `BUDGET=512; ARMS=2; PAIRS=int(round(512/2*22/256))=22`. "Primary arm instance" is never defined. Two things follow that no artefact considers. (a) **29 pairs give power 0.822** at (0.227, 0.000) and 33 pairs give 0.899 — the shortfall is ~1.3×, not an order of magnitude, and the artefacts never say how far short they are. (b) The SELECTIVE and SHAM trajectories are **bit-identical up to t_m** (`TLMR01_PATH_COVERAGE.json → SHAM_IS_A_BIT_EXACT_NO_OP`, and the intervention leaves the RNG untouched — `rng_hash_before == rng_hash_after` in all 22). A fork-at-t_m design pays the shared pre-trigger prefix once: at median t_m ≈ 900 of 11000 steps, 256 seeds cost ≈ 276 full-horizon-equivalents for 22 pairs, so 512 equivalents buy ≈ **41 pairs, power ≈ 0.98**. If "arm instance" means a literal world object the author is right; if it means compute, the classification `MATCHED_CONTROL_TEST_NOT_DECISION_CAPABLE` is wrong by roughly 2×.
**LOAD_BEARING** (Route A classification → terminal).
*Require:* define "primary arm instance", and either evaluate the forked design or state why the arms must be independent full-horizon runs.

**C-22 — "Not decision-capable" is reported as a fact but is conditioned on an n=22 point estimate whose CI spans hopeless to well-powered.**
`ldfma01_routes.py:27` `p_corr=5/22`. Wilson 95 % CI = **[0.101, 0.434]**. Power at 22 pairs across that CI runs **0.065 to 0.988**; at 33 pairs, 0.238 to 1.000. `LDFMA01_MATCHED_CONTROL_POWER.json → CONCLUSION` states the negative without any envelope. (Note the choice of 5/22 is the *generous* one — at the frozen 1/22 the power is 0.003 — so the direction is honest; the omission is the uncertainty.)
**LOAD_BEARING** (conditional).
*Require:* a power envelope over the CI of p₁, and a sentence saying the classification is conditional on the retrospective point estimate.

**C-23 — "paired log-SD" is a marginal SD, not a paired one.**
`ldfma01_routes.py:26`: `logsd = statistics.pstdev([log(x) for x in lif])` over the 22 SELECTIVE lifetimes — the between-world spread, not the SD of the within-block SELECTIVE−SHAM difference. `LDFMA01_MATCHED_CONTROL_POWER.json → E3_CONTINUOUS_ALTERNATIVE.paired_log_SD_from_the_22_SELECTIVE_worlds = 0.997` and the md's "a paired log-SD of 1.00 detect roughly a two-fold shift" both inherit the error. No SHAM arm has ever been executed (`FIMRCC01_FINAL_DISPOSITION.json → SHAM_ARM_EXECUTED = false`), so the paired SD is **unmeasured**. The blocks share a bit-identical prefix, so the true paired SD is likely smaller and E3 better powered than stated — safe for the author's "E3 has the power" claim, but the number is not what it is labelled.
**NOT_LOAD_BEARING.** *Require:* relabel as a marginal SD and state that the paired SD is unmeasurable from the existing record.

**C-24 — "the arms already exist" is not true.**
`LDFMA01_ROUTE_ARBITRATION.json → SCORING`, implementation-risk row: "low; the arms already exist and the SHAM no-op is proved bit-exact". The second clause is supported (`TLMR01_PATH_COVERAGE.json`). The first is contradicted by `FIMRCC01_LOCKED_IDENTITY_QUALIFICATION.json` ("no such arm exists in TLMR01's 512 worlds") and `FIMRCC01_FINAL_DISPOSITION.json → SHAM_ARM_EXECUTED = false`. A proved-inert code path is not an executed arm.
**NOT_LOAD_BEARING.** *Require:* "the SHAM code path exists and is proved a bit-exact no-op; no SHAM arm has been executed".

**C-25 — B3 is dismissed on a wrong reason and on a standard not applied to B1.**
`ldfma01_routeb.py:79-80`, condition 6 = FAIL: "suppressing splits directly inflates identity lifetime, which is the denominator of the very endpoint being tested." The endpoint (`prospective_primary_endpoint`) is a **binary per-block** COMPLETE_TURNOVER; identity lifetime is not a denominator in it. It is the *exposure*, which under C-03 is precisely the causal pathway the endpoint is designed to detect — raising it does not make the endpoint true by construction. The identical objection applied to B1 (`:35-37`, "it removes the only way a small component can die, which inflates persistence") is scored **PARTIAL**, not FAIL. B3's other failure (condition 4, `:75-77`, a state-dependent `p_hop_Y` needs a swept value and `PARAMETER_SWEEP` is forbidden) is a hard launcher constraint and stands on its own.
**LOAD_BEARING** for how Route B was reasoned; not for its outcome, since condition 4 alone disqualifies B3.
*Require:* withdraw the denominator argument, or apply the same severity to B1.

**C-26 — The independence claim is substantively sound but overstated in two specific places.**
(a) `LDFMA01_INDEPENDENT_RECONSTRUCTION.json → INPUTS_USED` says "the s step array for the world Y total", but `ldfma01_raw.py:180` also reads `r[7]`, the **online component count**, for `component_count_disagreements_vs_archive_step_array`. It never enters a verdict, but it is an undeclared online input and it is the basis of the "0 disagreements" evidence — which is itself weaker than presented, since it compares only component *counts*, not partitions. (b) `DELIBERATELY_DIFFERENT_IMPLEMENTATION` is true for the component finder (BFS vs union-find) but not for `centroid` (`:107-118`) or `link` (`:120-130`), which are near-transcriptions of `FDOT01/code/fdot01_centres.py`. The centroid *must* be transcribed bit-for-bit or the declared k_xd physical match (`:218-219`, 1e-9 tolerance on the online `a0 + so/ncells`) cannot bind — the author's own docstring says so ("matching the parent's list comprehension exactly"). The declared single dependency on `k_xd` is genuine and I found no path by which `c_cid`, `k_id`, the online label or the online t_m reaches a verdict.
**NOT_LOAD_BEARING.** *Require:* declare `s[:,7]`, and narrow "deliberately different implementation" to the component finder.

**C-27 — The mission binds its parent with 37 hashes and binds none of its own inputs or outputs.**
`git status` shows `?? LDFMA01/` — the entire mission is **untracked**, zero of the declared `MAX_INTENTIONAL_COMMITS = 4` used, in a programme whose own `LDFMA01_ROLLBACK_INCIDENT_05.json` records a fifth container rollback. No artefact records the sha256 of the 26 `.npz` archives that `ldfma01_raw.py` read, nor of the 26 `work/ldf3_out` records that every downstream artefact consumes. Twenty-five of those 26 archives are no longer present in the container (only `TLMR01_LAW_C_MCTT01_P_i001_s1474284807.npz` remains under `/mnt/user-data/uploads/`), so the reconstruction is not re-executable here today and its input provenance rests on prose.
**NOT_LOAD_BEARING** scientifically; material given the rollback history.
*Require:* commit, and add a manifest with the sha256 of the 26 inputs (checkable against `TLMR01_RAW_SHA256SUMS`) and the 26 outputs.

**C-28 — The "vocabulary gap" note keeps a strong claim while flying a weak flag.**
`LDFMA01_ROUTE_ARBITRATION.json → A_VOCABULARY_GAP_IN_THE_FROZEN_TERMINAL_SET` asserts the mechanism "IS identified … and NO route is eligible", uses the pause terminal "for its PAUSE half", and instructs the reader to read the mechanism "not from the terminal string". Given C-01 (the identification is a probe verdict the mission says it does not use), C-02 (17 of 21 failures survive the repair) and C-03 (the residual is physical, not instrumental), the gap as described is not established: what is identified is an instrument defect plus an exposure limit, and the second half was mis-read. Relatedly, `NO_HANDOFF_IS_EMITTED = true` sits beside a paragraph in `LDFMA01_MINIMAL_ARCHITECTURE_CANDIDATES.md` specifying an instrumentation repair in implementable detail — a handoff in prose. No forbidden reproduction / heredity / life / autonomous-cohesion claim appears anywhere in the artefact set, including in denial; the only occurrences are `NOT_TESTED` status fields and explicit claim ceilings, and "life" appears only as a lifetime metric. That check is clean.
**LOAD_BEARING** for how the terminal will be read downstream.
*Require:* either drop the "understates the result" note or restate the identified mechanism as the two-part finding above, with the probe's status attached.

---

## Recommended terminal disposition

```
LOCKED_DAUGHTER_FAILURE_MECHANISM_IDENTIFIED__ONE_MATCHED_CONTROL_TEST_ELIGIBLE
```

My reasoning, stated so it can be overturned on its own terms. The reconstruction is sound — 37/37 parent hashes exact, three internal passes identical on every core verdict, zero component-count disagreements across ~286,000 step-comparisons, no online-id leakage — so `FIMRCC01_RECORD_NOT_INTERPRETABLE` is not available. Route B is correctly closed: B3 fails the launcher's own `PARAMETER_SWEEP = forbidden` independently of the reasoning I attack in C-25, so `ONE_MINIMAL_ARCHITECTURE_TEST_ELIGIBLE` is not available either. That leaves pause versus a matched control.

A mechanism **is** identified, though not the one the author names: after the removal the locked daughter is a 1-to-6 particle object that constitutes the entire world (`parent_nY + daughter_nY = world_nY` in 22 of 22); its identity window is closed by a nearby split in 22 of 22 at a median of 230 steps; the resulting 11,385 particle-steps predict 8.44 constituent removals, of which the frozen step-t attribution can see only the ~14 % occurring in multiply-occupied cells (1.15 predicted, 1 observed). Both legs are quantified and one of them (C-07) is corroborated independently.

The pause was reached by rejecting E3 on alignment. That rejection rests on "persistence is NOT the binding constraint", and C-03 shows the mission's own numbers say the opposite: the corrected rate 5/22 sits within Poisson noise of the exposure-limited expectation 5.81, so identity persistence × occupancy is the binding variable at both the corrected and the frozen endpoint. E3 requires no instrumentation repair, no post-outcome threshold, and no re-run to define — it is fully reconstructable from the committed archive — and the author already scores it PASS on decision-capability. Correct condition 1 and condition 7 against C-03 and E3 becomes aligned, non-arbitrary and decision-capable, which is exactly arbitration rule 1.

I would revert to `LOCKED_DAUGHTER_FAILURE_NOT_IDENTIFIABLE__LINEAGE_ROUTE_PAUSED` on one condition: if the author re-derives E3's alignment against the per-world λ table and it still fails condition 7 on grounds that do **not** depend on the claim that persistence is not binding. E1-corrected must not be the route that carries this — selecting it would be post-outcome endpoint selection, which the launcher forbids, and its own condition-6 verdict already concedes as much.
