# Existing audited Python 3.12.10 environment; no installation was needed.
numpy==2.5.1
scipy==1.18.0
matplotlib==3.11.0
pytest==8.4.2
# PDF text QA additionally requires pypdf (bundled runtime used; see ENVIRONMENT.json).
# PDF compilation: Tectonic 0.17.0; visual rendering: pdftoppm (Poppler).
