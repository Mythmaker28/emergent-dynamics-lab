# IsingV3 reproduction lockfile — exact versions used for the certified clean-room reproduction.
# Python 3.10.12. Install into a fresh venv:  python -m pip install -r requirements-lock.txt
# Only these three third-party packages are required by python -m reproduction.primary.
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
