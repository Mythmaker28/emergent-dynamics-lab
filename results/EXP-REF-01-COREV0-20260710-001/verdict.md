# EXP-REF-01 — Verdict

Protocol (frozen, preregistered, user-specified decision rule): `docs/experiments/EXP_REF_01_PROTOCOL.md` @0ab80d1.
Executed by RUN-20260710-2015-EXPREF01. Model: claude-opus-4-8 (Fable 5 lock lifted by the user).

## OBSERVED
- Reference (rotating packet, gradual constituent turnover), through the FROZEN detector/tracker/P/M stack:
  single entity per snapshot; one continuous track (61 obs); P_main = 1.000; M_min = 0.25 (turnover);
  55 probe-positive (P>0.8, M<0.5) endpoints; mean |internal circulation| = 0.147; mean velocity dispersion = 0.090.
- Static-flux null (identical packet, rotation OFF, zero velocity): IDENTICAL detection/tracking/P/M
  (single track, P=1.000, M_min=0.25, 55 probe-positive) but mean |circulation| = 0.000 and mean velocity
  dispersion = 0.000.

## FINDINGS
- RECOGNIZED = TRUE: the frozen stack detects, tracks, and flags the known persistent dissipative organization as
  probe-positive under constituent turnover (P frozen, not recalibrated).
- SEPARATED = TRUE: raw P/M are identical for reference and null (so P/M alone cannot separate them), but the
  frozen phenotype dynamical observables (circulation, velocity dispersion) separate them decisively
  (0.147/0.090 vs 0.000/0.000).

## SCOPE CAVEAT (honest, pre-declared)
Separation is against the ZERO-VELOCITY static-flux null. The harder sparse-look-alike / spatial-occupancy alias
in which replacement particles carry MATCHED velocities is not resolvable by observers alone — that is precisely
what the same-state matched-branch causal intervention (ALIAS-INTERVENTION) exists to resolve. EXP-REF-01
establishes observer adequacy for recognising persistent dissipative organization vs the static-flux null, as
specified; it does not claim observers resolve the flowing-occupancy alias.

## VERDICT (user-specified rule)
**RETIRE Particle Dynamics for the current question; propose the next substrate (Flow-Lenia as a candidate).**
The measurement stack is NOT blind to persistent dissipative organization, so the EXP03 negatives (D-017..D-020)
are substrate results, not measurement artefacts. See `docs/PARTICLE_DYNAMICS_KILL_SWITCH_REVIEW.md` and
`docs/NEXT_SUBSTRATE_PROPOSAL.md`.
