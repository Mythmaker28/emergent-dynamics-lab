# FTCTR01 — FINAL REPORT

**FUNCTIONAL-TWO-CENTRE-TIMESCALE-REDERIVATION-01**
Owner: Tommy Lepesteur · Date (UTC): 2026-08-19
**FINAL_DISPOSITION = `STOP__CRITICAL_EXECUTABLE_BYTES_MISSING`**
Engine runs: 0 · Worlds: 0 · Seeds: 0 · Trajectories: 0 · Sweeps: 0 · Interpolations: 0

---

## 1. One-paragraph summary

FTCTR01 was to re-derive the separation-and-maturation clock of a two-centre developmental system
from its executable architecture, replacing the inherited scalar `TAU_SEP = 125`. The mission's own
opening gate requires that the architecture survive **by bytes**. It does not. Two independent
byte-level searches of the entire repository — every ref, every commit, every object including
unreachable ones, every untracked and git-ignored working file, all 46 bundles including one orphan
history, and all 81 tar payloads — find **no Y species, no X species, no hop probability, no decay
constant, no centre classifier, no `TAU_SEP`, and none of the four declared parent programmes**.
The mission therefore never began, and by its own rule nothing was reconstructed from memory. The
scientific product is a falsifiable absence certificate and a closed list of the objects that must
be produced before the question can be asked at all.

---

## 2. What was actually asked of the bytes

| Required to survive | Verdict |
|---|---|
| exact engine scheduler | **ABSENT** |
| Y diffusion semantics | **ABSENT** |
| centre-classification rule | **ABSENT** (nearest: a continuum component detector) |
| qualified X source-response operator | **ABSENT** |
| `muX` value | **ABSENT** |
| `p_hop_Y` value | **ABSENT** |
| `CORE_R` definition | **PARTIAL, WRONG ARCHITECTURE** (`CORE_RADIUS = 10`, lattice_bond individuation geometry) |
| capacity and boundary rules | **PARTIAL, WRONG ARCHITECTURE** (`MMAX` amplitude cap; P07/P08 throughput capacity) |

| Declared parent | Resolved tip |
|---|---|
| `ORGANIZER-BOUND-FULL-OPERATOR-RESIDUAL-01` (OBFOR01) | **none** |
| OBFOR01 confirmatory seal | **none** |
| `PROSPECTIVE-Q-ENVIRONMENT-CALIBRATION-01` (PQEC01) | **none** |
| `FOUNDER-VERSUS-LINEAGE-CONTINUITY-RECONCILIATION-01` (FVLCR01) | **none** |
| `ORGANISER-BOUND-OPERATOR-PAPER-REPAIR-01` (editorial) | **none** |

`CLOC02` and `RSLOC03`, which the mission excludes as lost and invalid respectively, are also absent
— they were never in this repository either.

---

## 3. Evidence

Full protocol, counts and positive controls: `FTCTR01_BYTE_BINDING_CERTIFICATE.md`.
Independent re-derivation: `FTCTR01_INDEPENDENT_CHECK.json`.

Headline coverage (implementation 2, the wider of the two):

* 10 005 blobs fully decompressed and scanned, including 258 unreachable/dangling objects;
* 199 refs, 397 commits, full reflog, 9 528 historical paths;
* 1 203 tracked + 8 492 untracked + 5 792 git-ignored non-`.venv` working files;
* all 46 `.bundle` files byte-walked (each walk terminating exactly at `filesize − 20`, the pack
  trailer — proof of full traversal), including one orphan history invisible to `git grep`;
* all 81 tar payloads, up to a 1 553 920 000-byte escrow stream.

Positive controls returned real hits at every stage (151 `FCDDH01R|Clopper-Pearson`; 1 125 files
matching `sha256|seed`; 585 matches inside a 586 MB bundle; 16 391 lines in the 1.4 GB escrow;
20+ genuine `tau_*` symbols recovered). The negatives are true negatives, not broken patterns.

### The decisive structural fact

**`np.random.binomial` / `rng.binomial` occur zero times in the repository — worktree and full
history.** The mission's Y diffusion is *defined* by a binomial draw over co-located particles.
A draw that was never made cannot have been executed. Independently, "occupied" in this codebase
always means a continuum threshold (`occupied = state.m >= spec.matter_threshold`), never an integer
particle count — so destination-capacity blocking of a hopping particle has no implementation to be
exact about. These two facts alone close the question, independently of any token search.

### What the repository *is*

Nineteen engines were enumerated: Conway's Game of Life, a Boolean network, five continuum PDE
substrates (chemotaxis, Gray–Scott, motile-polar, multistable, scaffold), Flow-Lenia, a continuous
site chain (CTRANS), the three-field `lattice_bond` continuum, four off-lattice Newtonian
particle variants on a torus, two organizational-memory engines, and a FitzHugh–Nagumo network.
None has integer per-cell particle counts, ordered sub-shift diffusion, a stochastic mover count,
destination-capacity refusal, or an accept/reject birth process. The newest commit on **any** ref is
dated **2026-08-11** and belongs to the `AXMAT00` / `FCDDH01R` / `FCDDH00` line.

---

## 4. Why nothing was derived anyway

Every artefact FTCTR01 lists as a deliverable — the relative-Y operator, the blocking bound, the X
maturation operator, the function-threshold audit, the functional-time distribution, the
third-centre hazard, the lineage criterion, the developmental comparison — has the same load-bearing
input: **the exact one-step transition law of the executed process.** Without it:

* `K_REL` cannot be constructed, and cannot be *proved impossible* either — the proof would also
  need the implementation;
* the capacity correction cannot be classified as `EXACTLY_INCLUDED`, `CERTIFIED_NEGLIGIBLE`,
  `BOUNDED_BUT_NONNEGLIGIBLE` or `NOT_IDENTIFIABLE_WITHOUT_ENVIRONMENT`, because there is no Y
  species whose blocking could be bounded;
* the `101 births / 250 steps` threshold cannot be audited for its estimand, its independent units,
  or whether 250 corresponds to `1/muX`, because neither the threshold nor `muX` has source bytes;
* `T_GEOM` and `T_MATURE` have no joint state operator, so composing them — by convolution or by
  Fréchet bounds — would be arithmetic on invented objects.

Writing any of these documents would have produced numbers with the *form* of a derivation and none
of its content. That is the failure mode this programme has already paid for twice (the `C3` scalar
surrogate; the `DEV_05` "protected core" normalisation artefact). It was not repeated.

`FTCTR01_MASTER_FREEZE.md` / `.json` were therefore **not written**, and `FTCTR01_METHODS_HASH` does
**not exist**. A freeze is a commitment made before seeing outcomes; there is no derivation to
commit to. The binding protocol in §2 of the certificate stands in its place and is fully
reproducible.

---

## 5. Status of the inherited quantities

| Quantity | Status |
|---|---|
| `TAU_SEP = 125` | `UNSOURCED` — no byte referent anywhere; neither accepted, rejected nor replaced |
| `101 accepted X births / centre / 250 steps` | `NOT_IDENTIFIABLE` — no source, no estimand, no units |
| `C3` third-centre surrogate | remains rejected; **no replacement derived** |
| observed separation / hold times | `NOT_LOCATED` — no raw arrays survive; report-quoted values were not used |
| `(kY, muY)` executable point | none exists |

The correct reading of `TAU_SEP = 125` is not "wrong" and not "right": it is **not a measurement of
this repository**. Whether it is a geometric first-passage scale, a functional maturation scale, or
neither, is undecidable on the surviving bytes.

---

## 6. Independent check

The one permitted independent check could not verify a derivation, because none exists. It was spent
on the claim that actually carries weight: the absence itself. A second operator, given no access to
the primary operator's scripts or intermediates, chose its own primitives (independent pack-index
and loose-object enumeration, direct zlib inflation of bundle packfiles, end-to-end tar streaming)
and returned `ABSENT` on all nine probed objects, with wider coverage than the primary search and
its own positive controls.

**The two implementations agree.** No averaging was performed and none was needed.

---

## 7. Direct-test eligibility and architecture change

* **Direct test:** `NOT_ELIGIBLE`. Eligibility requires an exact or certified Y first-passage law, a
  derived X maturation, a derived third-centre hazard, and one current executable `(kY, muY)` point
  with positive certified margins. Zero of the five are available. No candidate point was selected,
  and `HANDOFF_FRESH_DIRECT_FUNCTIONAL_LINEAGE_TEST_01.md` was **not** created.
* **Architecture change:** `NOT_ESTABLISHED`. `EXISTING_ARCHITECTURE_TIMESCALE_CONFLICT_PROVED`
  requires the exact operator to show that *no* admissible point satisfies maturation, continuity and
  third-centre control simultaneously. Proving that also needs the missing implementation. Declaring
  an architecture change "necessary" here would be an unsupported claim in the opposite direction and
  is refused.

---

## 8. Unique next scientific action

`HANDOFF_FTCTR01_EXECUTABLE_BYTE_RECOVERY_01.md` — and it is **conditional**. It runs only if a
location outside `C:\Users\tommy\Documents\ising v3` is named. It is not a scientific mission: zero
engine starts, and re-implementation is explicitly forbidden, because a re-implementation is a new
architecture and a new architecture voids every inherited number attached to the old one.

If no such location exists, the honest conclusion is that the two-centre Y/X programme was never
executed as code. Its quantities are then conversational rather than executable and should be marked
`NON_EXECUTABLE__WITHDRAWN` wherever cited — a real result, because it removes a phantom clock from
the programme's critical path and returns the next scientific action to the substrates that do
survive by bytes.

---

## 9. Claim ceiling

This report claims exactly one thing: **that a named set of executable objects does not exist in a
named repository, as established by two independent byte-level searches with positive controls at
every stage.** It claims nothing about the two-centre system's physics, timing, or feasibility. It
does not assert that such code never existed anywhere; only that it is not here.
